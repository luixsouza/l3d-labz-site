---
phase: quick-260616-e8v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/catalog/templates/catalog/product_detail.html
  - static/js/app.js
  - static/css/theme.css
autonomous: false
requirements: [QUICK-260616-e8v]

must_haves:
  truths:
    - "Em um produto COM modelo 3D, o cliente vê um toggle Fotos / 3D acima da mídia"
    - "Ao clicar em 3D, o model-viewer aparece e o cliente gira/zoom/pan o modelo"
    - "O botão Baixar STL só aparece quando o produto tem model_stl"
    - "No celular, o botão AR (ar do model-viewer) abre a visualização em realidade aumentada"
    - "Um produto SEM modelo 3D mantém o layout atual (carrossel de fotos), sem toggle e sem viewer"
    - "O GLB usa poster + reveal/lazy — não baixa o arquivo 3D até o cliente abrir a aba 3D"
  artifacts:
    - path: "apps/catalog/templates/catalog/product_detail.html"
      provides: "Toggle Fotos/3D + bloco model-viewer gated por product.has_3d + extra_js gated"
      contains: "model-viewer"
    - path: "static/js/app.js"
      provides: "Lógica de toggle Fotos<->3D (progressive enhancement)"
      contains: "data-media-tab"
    - path: "static/css/theme.css"
      provides: "Estilos do toggle e do painel 3D no detalhe"
  key_links:
    - from: "apps/catalog/templates/catalog/product_detail.html"
      to: "product.model_3d_url / product.model_stl_url"
      via: "atributos src / href do model-viewer (já expostos por ProductMapper.to_detail)"
      pattern: "model_3d_url|model_stl_url"
    - from: "static/js/app.js"
      to: "botões data-media-tab e painéis data-media-panel no template"
      via: "click listener que troca a classe active"
      pattern: "data-media-tab"
---

<objective>
Embutir o visualizador 3D na página de detalhe do produto, com um toggle Fotos / 3D, controles de girar/zoom/pan, download do STL, AR no celular, e poster + lazy-load do GLB.

Purpose: É o core value do projeto (CLAUDE.md) — o cliente visualizar o modelo 3D de forma intuitiva direto na página do produto. Hoje o detalhe só mostra fotos; o 3D vive só na home e na galeria.
Output: product_detail.html com toggle + model-viewer (reuso do padrão da home), JS de toggle em app.js, estilos do toggle em theme.css. Sem mudança de view, service ou mapper — a camada de dados JÁ expõe tudo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md

@apps/catalog/templates/catalog/product_detail.html
@apps/core/templates/core/home.html
@static/js/app.js

<interfaces>
<!-- Dados JÁ disponíveis no template via {{ product }} (ProductMapper.to_detail). NÃO acessar o model diretamente. -->
<!-- Fonte: apps/catalog/mappers.py ProductMapper.to_dict (herdado por to_detail) -->

product.has_3d         # bool — produto tem GLB (model_3d)
product.has_stl        # bool — produto tem STL (model_stl)
product.model_3d_url   # str  — URL do .glb (vazio se não houver)
product.model_stl_url  # str  — URL do .stl (vazio se não houver)
product.image_url      # str  — foto principal (usar como poster)
product.name           # str
product.gallery        # list[str] — URLs das fotos (já usado no carrossel atual)

<!-- Padrão model-viewer já em produção, copiar de apps/core/templates/core/home.html (hero):
     src, alt, poster, camera-controls, auto-rotate, environment-image="neutral",
     exposure, shadow-intensity, shadow-softness, camera-orbit, field-of-view.
     CSS .detail-media model-viewer JÁ existe em static/css/theme.css:369. -->

<!-- Script CDN já fixado @4.3.1 (decisão Phase 03), carregar por página em {% block extra_js %} gated por has_3d:
     https://cdn.jsdelivr.net/npm/@google/model-viewer@4.3.1/dist/model-viewer.min.js -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle Fotos/3D + model-viewer no detalhe do produto</name>
  <files>apps/catalog/templates/catalog/product_detail.html, static/css/theme.css</files>
  <action>
    No product_detail.html: envolver a mídia (`.detail-media`) num container `.detail-media-wrap`.

    Adicionar um `{% block extra_js %}` no topo (após `{% block title %}`), gated por `{% if product.has_3d %}`, que carrega o script CDN do model-viewer @4.3.1 (mesma URL ES module usada em core/home.html). Sem `nomodule` (decisão Phase 03). Produto sem 3D não carrega o script.

    Dentro de `{% if product.has_3d %}`, renderizar uma barra de toggle ACIMA da mídia com dois botões: "Fotos" e "3D". Cada botão tem `data-media-tab="fotos"` / `data-media-tab="3d"`, `type="button"`, e o de Fotos começa com classe `active`. Copy pt-br. Use ícones do sprite quando fizer sentido (ex.: `#i-box` pra 3D), opcional.

    Estruturar a mídia em dois painéis irmãos:
    - Painel Fotos: `data-media-panel="fotos"` com classe `active` por padrão — contém o carrossel/foto ATUAL (não alterar a lógica de fotos existente, só envolver).
    - Painel 3D: `data-media-panel="3d"` (sem `active`) — contém o `<model-viewer>`.

    O model-viewer: `src="{{ product.model_3d_url }}"`, `alt="{{ product.name }}"`, `poster="{{ product.image_url }}"`, `camera-controls`, `auto-rotate`, `ar`, `ar-modes="webxr scene-viewer quick-look"`, `reveal="interaction"` (NÃO baixa o GLB até interação/abertura — atende a constraint de lazy-load), `environment-image="neutral"`, `exposure="1.15"`, `shadow-intensity="0.9"`, `shadow-softness="1"`, `camera-orbit="-25deg 75deg auto"`, `field-of-view="28deg"`, `style="width:100%;height:100%"`. Legenda "arraste pra girar · pinça pra zoom".

    Botão AR: usar o slot `<button slot="ar-button">` dentro do model-viewer com copy "Ver no seu espaço" e ícone; o model-viewer esconde sozinho em devices sem AR.

    Botão Baixar STL: APENAS dentro de `{% if product.has_stl %}`, link `href="{{ product.model_stl_url }}" download` com ícone `#i-download`, copy "Baixar STL". Posicionar dentro do painel 3D (abaixo do viewer).

    Em theme.css (após o bloco `.detail-media model-viewer` ~linha 380): adicionar `.detail-media-tabs` (flex, gap pequeno, margin-bottom) e `.detail-media-tab` (botão pill usando design tokens: `--bg-card`, `--border`, `--radius-sm`; estado `.active` usa `--accent`/`--accent-soft`). Adicionar `[data-media-panel]{display:none}` e `[data-media-panel].active{display:block}` (com fallback: ver Task 2 sobre no-JS). Estilos do `slot=ar-button` e do botão STL no painel (pode reaproveitar o visual de `.gh-viewer-stl`). Respeitar tema light com seletor `[data-theme="light"]` se necessário.

    NÃO mexer em views.py, services.py nem mappers.py — os dados já chegam no template.
  </action>
  <verify>
    <automated>cd /c/dev/l3d-labz-site && python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.dev');django.setup();from django.template.loader import get_template;get_template('catalog/product_detail.html');print('template OK')"</automated>
  </verify>
  <done>Template carrega sem erro de sintaxe; produto com has_3d mostra toggle + model-viewer (com ar e reveal="interaction") e Baixar STL só quando has_stl; produto sem has_3d não renderiza toggle/viewer e não carrega o script CDN.</done>
</task>

<task type="auto">
  <name>Task 2: Lógica de toggle Fotos/3D em app.js (progressive enhancement)</name>
  <files>static/js/app.js</files>
  <action>
    Adicionar um bloco IIFE-interno (seguindo o estilo dos outros blocos do arquivo, com comentário de seção pt-br `/* --- toggle fotos/3d (detalhe) --- */`).

    Comportamento: selecionar todos os `[data-media-tab]`; ao clicar, ler o valor do data-attr, marcar esse botão como `active` (e remover dos irmãos no mesmo grupo de tabs), e mostrar o painel `[data-media-panel="<valor>"]` correspondente (classe `active`) escondendo os outros. Operar por scoping no container pai mais próximo (ex.: `.detail-media-wrap`) pra não vazar pra outros toggles na página.

    Progressive enhancement: como `[data-media-panel]{display:none}` esconde tudo por padrão (Task 1), garantir no JS que o painel inicial correto fique visível no load — OU (preferível, sem-JS-safe) o CSS já dá `active` ao painel Fotos e o JS só TROCA. Confirme que sem JS o usuário ainda vê as fotos (o painel Fotos tem `active` no HTML). Não há dependência de fetch nem de rede.

    Não alterar os outros blocos (carrossel, cart, tema). O carrossel já roda no painel Fotos normalmente.
  </action>
  <verify>
    <automated>cd /c/dev/l3d-labz-site && node -e "const s=require('fs').readFileSync('static/js/app.js','utf8');if(!/data-media-tab/.test(s))throw new Error('toggle ausente');if(s.split('(function').length-1!==1)console.warn('IIFE count check');console.log('app.js OK')"</automated>
  </verify>
  <done>app.js contém o handler de `data-media-tab`; clicar em 3D mostra o painel 3D e esconde Fotos (e vice-versa); sem JS as fotos continuam visíveis (painel Fotos tem `active` no HTML).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Toggle Fotos/3D na página de detalhe do produto com model-viewer embutido (girar/zoom/pan), botão AR (celular), Baixar STL condicional, e poster + reveal="interaction" pra não baixar o GLB cedo. Produtos sem 3D ficam intactos.
  </what-built>
  <how-to-verify>
    IMPORTANTE (memória do projeto, STATE.md): o Chrome headless NÃO renderiza WebGL aqui — screenshot headless do viewer 3D NÃO é prova válida. A verificação do 3D é no NAVEGADOR REAL, a olho.

    1. `python manage.py runserver` e abrir um produto COM modelo 3D (ex.: um da galeria `/catalogo/modelos-3d/`).
    2. Conferir que o toggle "Fotos / 3D" aparece acima da mídia, começando em Fotos (carrossel funcionando).
    3. Clicar em "3D": o model-viewer carrega e dá pra girar (arrastar), zoom (scroll/pinça) e pan. O GLB só baixa ao abrir a aba (Network tab: o .glb não some na carga inicial da página).
    4. Conferir o botão "Baixar STL" (só se o produto tiver STL) e que o download funciona.
    5. No celular (ou DevTools device mode + device real pra AR), conferir o botão AR "Ver no seu espaço".
    6. Abrir um produto SEM modelo 3D: NÃO deve ter toggle nem viewer — só as fotos, layout idêntico ao atual.
    7. Conferir tema light e dark do toggle.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- Template renderiza sem erro (get_template).
- app.js contém handler `data-media-tab` e mantém IIFE única.
- Verificação visual no navegador real (WebGL não roda em headless).
- Produto sem 3D inalterado; produto com 3D mostra toggle + viewer + AR + STL condicional.
</verification>

<success_criteria>
- Cliente alterna Fotos<->3D e manipula o modelo (girar/zoom/pan) na página do produto.
- Baixar STL só aparece quando há STL; AR disponível no celular.
- GLB não é baixado até abrir a aba 3D (poster + reveal="interaction").
- Produtos sem 3D mantêm o layout atual; camadas view/service/mapper inalteradas.
- Copy nova em pt-br; estilos via design tokens; sem framework JS.
</success_criteria>

<output>
Create `.planning/quick/260616-e8v-3d-na-pagina-do-produto-embutir-model-vi/260616-e8v-SUMMARY.md` when done
</output>
