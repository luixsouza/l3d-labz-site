---
phase: quick-260612-gf7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/core/views.py
  - apps/core/templates/core/home.html
autonomous: true
requirements: [QUICK-260612-gf7]
must_haves:
  truths:
    - "Com promoção hero ativa, a home exibe badge/título/subtítulo/CTA da promoção no lugar do hero hardcoded"
    - "Sem promoção hero ativa, a home renderiza o hero hardcoded atual byte a byte"
    - "O CTA secundário 'Faça meu Lithophane' permanece sempre visível"
  artifacts:
    - path: "apps/core/views.py"
      provides: "hero_promo no contexto da HomeView via PromotionService.get_hero_promotion()"
      contains: "hero_promo"
    - path: "apps/core/templates/core/home.html"
      provides: "Renderização condicional do hero promocional com fallback hardcoded"
      contains: "hero_promo"
  key_links:
    - from: "apps/core/views.py"
      to: "apps.promotions.services.PromotionService"
      via: "deferred import dentro de get_context_data"
      pattern: "PromotionService.get_hero_promotion"
    - from: "apps/core/templates/core/home.html"
      to: "hero_promo"
      via: "{% if hero_promo %}...{% else %}...{% endif %}"
      pattern: "if hero_promo"
---

<objective>
Ligar o hero da home ao `PromotionService.get_hero_promotion()`. A `HomeView` passa `hero_promo` no contexto e `core/home.html` renderiza o hero promocional (badge/título/subtítulo/CTA) quando existir promoção hero ativa, mantendo o hero padrão hardcoded como fallback.

Purpose: Permitir que o admin destaque uma promoção no topo da home sem mexer no código, reutilizando o serviço de promoções já existente.
Output: `apps/core/views.py` e `apps/core/templates/core/home.html` atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Contratos já existentes — usar diretamente, sem explorar o codebase. -->

PromotionService.get_hero_promotion() -> dict | None (apps/promotions/services.py):
  - Retorna None quando não há promoção hero ativa (.live(): is_active + janela starts_at/ends_at).
  - Quando há, retorna o dict do PromotionMapper.to_dict.

PromotionMapper.to_dict shape (apps/promotions/mappers.py):
  {
    "kind": str,
    "title": str,
    "subtitle": str,
    "badge": str,
    "cta_label": str,
    "cta_url": str,
    "image_url": str,   # pode ignorar por ora (hero é tipográfico)
    "ends_at": datetime | None,
  }

Hero hardcoded atual (apps/core/templates/core/home.html, linhas 6-24) usa as classes:
  geek-hero geek-hero--clean reveal, gh-inner, eyebrow, lead, hero-cta, gh-btn primary/ghost, gh-deco.
  CTA secundário fixo: <a href="{% url 'lithophane:editor' %}" class="gh-btn ghost">Faça meu Lithophane</a>

Padrão de deferred import já usado na HomeView:
  from apps.catalog.services import CatalogService  (dentro de get_context_data)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Passar hero_promo no contexto da HomeView</name>
  <files>apps/core/views.py</files>
  <action>Em `HomeView.get_context_data`, adicionar um deferred import `from apps.promotions.services import PromotionService` (mesmo estilo do `CatalogService` já presente) e setar `context["hero_promo"] = PromotionService.get_hero_promotion()`. View permanece fina — apenas chama o service e injeta no contexto. Não alterar `AboutView`.</action>
  <verify>
    <automated>python manage.py shell -c "from apps.core.views import HomeView; from django.test import RequestFactory; v=HomeView(); v.request=RequestFactory().get('/'); v.kwargs={}; c=v.get_context_data(); print('hero_promo' in c)"</automated>
  </verify>
  <done>`get_context_data` retorna a chave `hero_promo` (valor dict ou None) sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar hero promocional condicional no template com fallback</name>
  <files>apps/core/templates/core/home.html</files>
  <action>Envolver o bloco hero (linhas 6-24) em `{% if hero_promo %} ... {% else %} ... {% endif %}`. No ramo `{% else %}`, manter o hero hardcoded atual EXATAMENTE como está (byte a byte). No ramo `{% if hero_promo %}`, replicar a mesma estrutura/classes (`section`, `geek-hero geek-hero--clean reveal`, `gh-inner`, `gh-deco`) mas substituindo: `eyebrow` por `{{ hero_promo.badge }}`, `<h1>` por `{{ hero_promo.title }}`, `p.lead` por `{{ hero_promo.subtitle }}`, e o CTA primário por `<a href="{{ hero_promo.cta_url }}" class="gh-btn primary">{{ hero_promo.cta_label }} <svg class="icon icon-arrow"><use href="#i-arrow-right"></use></svg></a>`. Manter o CTA secundário `Faça meu Lithophane` igual em ambos os ramos. Não criar CSS novo — reutilizar as classes existentes. Ignorar `image_url` (hero é tipográfico com `gh-deco`).</action>
  <verify>
    <automated>python manage.py shell -c "from django.template.loader import get_template; t=get_template('core/home.html'); src=t.template.source; print('hero_promo' in src and 'hero_promo.badge' in src and 'hero_promo.title' in src and 'hero_promo.cta_url' in src and 'Faça meu Lithophane' in src)"</automated>
  </verify>
  <done>Template carrega sem erro de sintaxe; ramo promocional usa badge/title/subtitle/cta_label/cta_url; ramo fallback mantém o hero original; CTA 'Faça meu Lithophane' presente.</done>
</task>

</tasks>

<verification>
- `python manage.py check` passa sem erros.
- Com `hero_promo=None`: home renderiza o hero hardcoded original.
- Com uma `Promotion` hero ativa no banco: home renderiza badge/título/subtítulo/CTA da promoção.
- Nenhuma alteração em arquivos CSS.
</verification>

<success_criteria>
- `HomeView` injeta `hero_promo` no contexto via `PromotionService.get_hero_promotion()` (deferred import).
- `core/home.html` renderiza o hero promocional quando `hero_promo` existe e o hero hardcoded como fallback.
- CTA secundário 'Faça meu Lithophane' permanece sempre.
- Zero CSS novo; classes existentes reutilizadas; copy em pt-br.
</success_criteria>

<output>
Create `.planning/quick/260612-gf7-ligar-hero-da-home-ao-promotionservice-h/260612-gf7-SUMMARY.md` when done
</output>
