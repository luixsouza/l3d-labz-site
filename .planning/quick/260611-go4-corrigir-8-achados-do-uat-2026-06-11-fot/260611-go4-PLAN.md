---
phase: quick-260611-go4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/css/theme.css
  - apps/core/templates/core/partials/navbar.html
  - apps/core/templates/base.html
  - apps/core/views.py
  - apps/core/urls.py
  - apps/core/templates/core/about.html
  - apps/core/templates/core/home.html
  - apps/core/templates/core/static_page.html
  - templates/404.html
  - templates/500.html
  - apps/orders/models.py
  - apps/cart/templates/cart/detail.html
  - apps/catalog/templates/catalog/product_detail.html
  - apps/catalog/render3d.py
autonomous: true
requirements: [UAT-2026-06-11]

must_haves:
  truths:
    - "Em viewport 390px o botão #navToggle é tocável e abre o menu (nada o intercepta)"
    - "No mobile os botões de conta/carrinho/tema não transbordam para fora da barra do header"
    - "O menu mobile aberto tem fundo sólido/opaco e links com contraste AA legíveis"
    - "Páginas v2 (produto, carrinho, checkout, perfil, pedidos, promoções, lithophane, login/cadastro) respeitam o toggle dark/light"
    - "Novos pedidos recebem número L3D-NNNNNN; pedidos existentes permanecem intactos"
    - "Acessar URL inexistente mostra 404 com marca L3D Labz; erro 500 mostra página própria sem tocar o DB"
    - "/ (home) mostra a vitrine com title sem marca duplicada; /sobre/ é página institucional distinta"
    - "Links do footer (privacidade, termos, fale conosco) levam a destinos válidos"
    - "model-viewer abre o GLB em ângulo 3/4 legível (não placa fina de lado) sem quebrar malhas reais"
    - "padronizar_fotos gera foto colorida (desvio padrão de pixels acima do limiar), não branca uniforme"
  artifacts:
    - path: "templates/404.html"
      provides: "Página 404 com marca e link pro catálogo"
    - path: "templates/500.html"
      provides: "Página 500 independente de context processors/DB"
    - path: "apps/core/templates/core/home.html"
      provides: "Home/vitrine separada da página Sobre"
    - path: "apps/core/templates/core/about.html"
      provides: "Página institucional própria"
    - path: "apps/catalog/render3d.py"
      provides: "Render colorido por categoria (paleta determinística por slug)"
  key_links:
    - from: "apps/core/urls.py"
      to: "HomeView / AboutView"
      via: "rotas distintas para / e /sobre/"
      pattern: "HomeView|AboutView"
    - from: "apps/orders/models.py"
      to: "número do pedido"
      via: "prefixo L3D"
      pattern: "L3D-\\{self.pk"
---

<objective>
Corrigir os 9 achados do UAT de 2026-06-11 do site L3D Labz, agrupados em 3 tasks coesas: (T1) mobile header + acessibilidade do toggle + contraste do menu + consistência do tema dark via tokens; (T2) rebrand visível + páginas 404/500 + separação home/sobre + destinos do footer; (T3) enquadramento do model-viewer + render colorido das fotos de produto.

Purpose: Fechar o UAT mantendo a arquitetura em camadas, design tokens e copy pt-br.
Output: CSS/templates/views corrigidos, render3d com cor, páginas de erro e institucionais novas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Arquitetura: views finas (sem ORM), camadas services/queries/mappers, tokens CSS, pt-br.
# theme.css: :root = dark (default); [data-theme="light"] sobrescreve tokens.
#   - Mobile nav: linha 197 .nav-toggle{display:none}; @media 720px (linha 434) mostra toggle e esconde .main-nav.
#   - .main-nav.open é alternado por app.js (#navToggle -> #mainNav.classList.toggle("open")).
#   - .header-actions (linha 184): flex com gap; no mobile precisa não transbordar nem sobrepor o toggle.
# base.html: <title>{% block title %}{{ SITE.name }}{% endblock %} · {{ SITE.name }}</title> -> duplica marca quando o bloco já contém a marca.
# settings TEMPLATES: DIRS=[BASE_DIR/"templates"], APP_DIRS=True -> 404.html/500.html ficam em templates/ na raiz.
# core/urls.py: hoje "" e "sobre/" usam ambos AboutView (que carrega vitrine).
# orders/models.py:78-83: save() gera number=f"NX-{self.pk:06d}" só quando vazio.
# cart/detail.html:93: placeholder "Ex.: NERD10".
# render3d.py: _MAT cinza fixo, _AMB 0.52, 1 luz; precisa paleta por categoria/slug + key+fill light.
# product_detail.html:15-22: model-viewer já tem camera-orbit="-25deg 72deg auto" (ângulo ruim p/ placa).

<interfaces>
De apps/catalog/render3d.py (assinatura a preservar):
```python
def render_thumb(mesh, nome: str = "", accent: str = "", tamanho: int = 1000) -> bytes
```
O comando padronizar_fotos chama render_thumb(mesh, ...). Manter a assinatura e o retorno (bytes JPEG). O parâmetro `accent`/categoria pode ser usado para derivar a cor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile header acessível + menu com contraste + tema dark consistente nas páginas v2</name>
  <files>static/css/theme.css, apps/core/templates/core/partials/navbar.html</files>
  <action>
  ACHADOS 1, 2, 3, 4.

  (1+2) Mobile header (viewport <=720px): no `.header-actions`, garantir que o `#navToggle` (.nav-toggle) fique sempre por cima e tocável (z-index acima dos botões; pointer-events ok) e que os demais botões (Entrar/Criar conta/tema/carrinho) NÃO sobreponham o toggle nem transbordem para fora da barra escura do header. Estratégia mais barata e robusta: numa media query mobile (reaproveitar/estender o bloco @media (max-width:720px) em theme.css ~linha 434), esconder no header os botões de texto "Entrar"/"Criar conta" (.header-actions .btn) — eles já ficam acessíveis dentro do menu mobile — e manter apenas tema + carrinho + toggle, todos cabendo na largura sem overflow. Para isso, adicionar no navbar.html (dentro de #mainNav, o painel mobile) os links "Entrar"/"Criar conta" (ou "Minha conta"/"Sair" quando autenticado) para usuários mobile, já que serão escondidos do header. Use as mesmas urls (accounts:login, accounts:register, accounts:profile, accounts:logout). Confirmar que nada (botão branco "fantasma") aparece fora do header em páginas de miolo claro: os .icon-btn no header já usam var(--bg-card)/var(--border); o "quadrado branco fantasma" vem de botão transbordando — eliminado ao esconder os .btn de texto e conter o header-actions (sem wrap para fora).

  (3) Menu mobile aberto: dar a `.main-nav.open` (painel) fundo SÓLIDO e opaco (use var(--bg) ou var(--bg-card), nada translúcido sobre o hero) e os links com cor var(--text) (não var(--text-muted)) para contraste AA. Inclua os links de conta adicionados acima no mesmo estilo legível. Garantir z-index do painel acima do hero.

  (4) Tema dark nas páginas v2: rodar cada página v2 (produto, carrinho, checkout, perfil, pedidos, promoções, lithophane, login/cadastro) no modo dark e localizar regras CSS que NÃO acompanham o toggle — cores claras hardcoded (ex.: backgrounds/textos em hex claro fixo em seções como blocos de conteúdo dessas páginas) que deveriam usar tokens. Substituir os hardcoded claros por tokens (var(--bg), var(--bg-card), var(--text), var(--text-muted), var(--border) etc.). NÃO alterar cores intencionalmente fixas sobre gradientes de accent (ex.: texto #06240f sobre fundo verde de hero permanece — é legível nos dois temas). Objetivo: o miolo dessas páginas escurece junto com home/catálogo/modelos-3d. Preferir tokens a duplicar regras [data-theme]. Não mexer no comportamento do toggle em app.js (já funciona).
  </action>
  <verify>
    <automated>cd C:\dev\l3d-labz-site; .venv\Scripts\python.exe -c "import re,sys; css=open('static/css/theme.css',encoding='utf-8').read(); body=re.sub(r'#[0-9a-fA-F]{6}|#[0-9a-fA-F]{3}','',css); m=re.search(r'\.main-nav\.open[^}]*\{([^}]*)\}',css); print('OPEN_RULE_FOUND' if m else 'MISSING'); assert m, 'falta regra .main-nav.open'; print('OK')"</automated>
    <human-check>Em 390px o #navToggle abre o menu (não é interceptado), header não transborda, menu aberto legível, e produto/carrinho/checkout/perfil/pedidos/promoções escurecem no dark.</human-check>
  </verify>
  <done>Toggle tocável em 390px, header sem overflow nem botões fantasma, menu mobile com fundo sólido e texto AA, e páginas v2 respeitam o toggle dark/light.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand visível (L3D) + páginas 404/500 + separar Home/Sobre + destinos do footer</name>
  <files>apps/orders/models.py, apps/cart/templates/cart/detail.html, templates/404.html, templates/500.html, apps/core/views.py, apps/core/urls.py, apps/core/templates/core/home.html, apps/core/templates/core/about.html, apps/core/templates/core/static_page.html, apps/core/templates/base.html</files>
  <action>
  ACHADOS 5, 6, 7.

  (5) Rebrand: em apps/orders/models.py:81-83 trocar o prefixo NX por L3D (number=f"L3D-{self.pk:06d}" e o comentário ex.: L3D-000042). Só afeta pedidos novos (o save só gera quando number vazio) — pedidos existentes ficam intactos. Em cart/detail.html:93 trocar placeholder "Ex.: NERD10" por "Ex.: L3D10". Adicionar admin.site.site_header/site_title/index_title = "L3D Labz" (em apps/core/admin.py se existir, senão criar; ou no AppConfig.ready do core — escolha o caminho que respeite a arquitetura, preferir um apps/core/admin.py simples). Docstrings/strings internas com "Nexora" (manage.py, settings docstring, urls docstring) podem ficar — só o que é visível ao usuário muda.

  (6) Páginas de erro: criar templates/404.html e templates/500.html (DIRS=[BASE_DIR/"templates"] já configurado). Ambos estendem base.html SE seguro — porém 500 NÃO pode depender de context processors que toquem DB. Verificar os context processors do core/cart: se algum faz query (ex.: cart_count, SITE), o 500.html deve ser um HTML autocontido mínimo (sem extends, com CSS inline básico da marca) para não falhar em cascata. 404.html pode estender base.html (request normal). Copy pt-br simpática, marca L3D Labz, link de volta pro catálogo (use href absoluto "/catalogo/" no 500 para não depender de {% url %} se optar por autocontido; no 404 pode usar {% url 'catalog:product_list' %}). Garantir DEBUG=False para Django servir esses templates (não criar handler customizado a menos que necessário — Django usa templates/404.html e templates/500.html por padrão).

  (7) Separar Home/Sobre: em apps/core/views.py criar HomeView (TemplateView template core/home.html, com a vitrine atual: featured/new_arrivals/home_categories) e deixar AboutView (template core/about.html) como página institucional SEM precisar da vitrine (ou com contexto mínimo). Atualizar core/urls.py: path("") -> HomeView (name="home"), path("sobre/") -> AboutView (name="about"). Mover o conteúdo de vitrine do about.html atual para o novo home.html; criar about.html institucional (quem somos, como funciona a produção sob demanda, contato/WhatsApp em pt-br). Corrigir o title duplicado: em base.html o <title> é "{% block title %}{{ SITE.name }}{% endblock %} · {{ SITE.name }}" — o block default já é a marca, então fica "L3D Labz · L3D Labz". Ajustar para o block title NÃO incluir a marca por padrão (ex.: block default vazio e formato condicional) OU simplificar para "{% block title %}{{ SITE.name }} — Impressão 3D sob demanda{% endblock %}" como title da home e remover a concatenação " · {{ SITE.name }}" duplicante quando o bloco já traz a marca. Garantir: home title = "L3D Labz — Impressão 3D sob demanda" sem marca duplicada; páginas internas mantêm "{Pagina} · L3D Labz" uma única vez. Atualizar navbar active: o link "Sobre" deve marcar active só em core:about (hoje marca em home também).
  Footer: criar destinos para "Política de privacidade", "Termos e condições" e "Fale conosco". Usar um core/static_page.html genérico + TemplateViews (ou rotas em core/urls.py) para privacidade e termos (copy pt-br curta). "Fale conosco" pode apontar para o link de WhatsApp já usado no site. Atualizar o footer (apps/core/templates/core/partials/footer.html) para apontar para esses destinos reais (não usar "#").
  </action>
  <verify>
    <automated>cd C:\dev\l3d-labz-site; .venv\Scripts\python.exe -c "import re; m=open('apps/orders/models.py',encoding='utf-8').read(); assert 'L3D-' in m and 'NX-' not in m.replace('# ',''), 'numero pedido nao migrado'; v=open('apps/core/views.py',encoding='utf-8').read(); assert 'HomeView' in v and 'AboutView' in v, 'falta HomeView/AboutView'; import os; assert os.path.exists('templates/404.html') and os.path.exists('templates/500.html'), 'faltam paginas de erro'; print('OK')"</automated>
    <automated>cd C:\dev\l3d-labz-site; .venv\Scripts\python.exe manage.py check</automated>
    <human-check>/ mostra vitrine com title "L3D Labz — Impressão 3D sob demanda" (sem duplicar); /sobre/ é institucional; 404 e 500 com marca; links do footer funcionam.</human-check>
  </verify>
  <done>Pedidos novos = L3D-NNNNNN, placeholder cupom L3D10, admin com header L3D Labz, 404/500 próprios (500 sem DB), home e sobre separadas com title correto, footer com destinos válidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: model-viewer enquadramento 3/4 + render3d fotos coloridas estilo Apple/Nike</name>
  <files>apps/catalog/templates/catalog/product_detail.html, apps/catalog/render3d.py</files>
  <behavior>
    - render_thumb(mesh, accent="...") retorna bytes JPEG com desvio padrão de pixels acima de um limiar (ex.: std > 12 num canal) — NÃO é branco/cinza uniforme.
    - A cor do material é determinística por slug/categoria: mesma entrada -> mesma cor entre renders.
    - A assinatura render_thumb(mesh, nome="", accent="", tamanho=1000) -> bytes é preservada (padronizar_fotos continua chamando igual).
    - model-viewer abre num ângulo 3/4 que mostra volume (não placa fina de lado).
  </behavior>
  <action>
  ACHADOS 8, 9.

  (8) model-viewer (product_detail.html:15-22): ajustar o enquadramento inicial para um 3/4 que funcione tanto para relevos (placas) quanto para malhas reais. Trocar camera-orbit para um ângulo 3/4 inclinado (ex.: camera-orbit="35deg 70deg auto") e adicionar/garantir shadow-intensity="1" (ou manter ~1.4 se ficar melhor), exposure adequada (~1.0-1.1) e environment-image="neutral" (já presente) para o material não ficar morto. Adicionar field-of-view ou min/max-camera-orbit se necessário para evitar zoom degenerado. NÃO quebrar AR nem auto-rotate. O objetivo: GLB de relevo deixa de aparecer como placa branca fina de lado.

  (9) render3d.py — fotos coloridas premium: substituir o material cinza fixo (_MAT) por uma cor derivada de forma DETERMINÍSTICA do slug/categoria do produto. Estratégia: paleta L3D (ex.: verde L3D ~#2FA84F, azul ~#2BA6E0, coral, âmbar) e hash estável do slug -> índice na paleta (não usar random sem seed; mesma foto a cada render). Aceitar a cor via parâmetro existente (`accent`) quando vier; senão derivar do nome/slug. Melhorar a iluminação: além da luz atual, adicionar uma segunda luz de preenchimento (fill) com direção/intensidade diferentes para dar contraste (key+fill), reduzindo o _AMB chapado. Manter fundo quase-branco com sombra de contato. O comando padronizar_fotos deve continuar funcionando idêntico (só o visual muda) — preservar a assinatura de render_thumb e o retorno bytes JPEG. Incluir teste/checagem: gerar a foto de um produto e confirmar que o resultado tem variação de cor (std de pixels acima de limiar) e não é branco uniforme.
  </action>
  <verify>
    <automated>cd C:\dev\l3d-labz-site; .venv\Scripts\python.exe -c "import io,numpy as np,trimesh; from PIL import Image; from apps.catalog import render3d; m=trimesh.creation.box(extents=(1,1,0.1)); b1=render3d.render_thumb(m,nome='produto-verde',accent=''); b2=render3d.render_thumb(m,nome='produto-verde',accent=''); a=np.asarray(Image.open(io.BytesIO(b1)).convert('RGB')); print('std=',a.std()); assert a.std()>12, 'imagem quase uniforme/branca'; assert b1==b2, 'render nao deterministico'; print('OK')"</automated>
    <automated>cd C:\dev\l3d-labz-site; .venv\Scripts\python.exe -c "t=open('apps/catalog/templates/catalog/product_detail.html',encoding='utf-8').read(); assert 'camera-orbit' in t and 'environment-image' in t, 'model-viewer sem ajustes'; print('OK')"</automated>
    <human-check>Abrir um produto com GLB: o modelo aparece em 3/4 legível (não placa de lado). Fotos do catálogo coloridas e premium, não brancas.</human-check>
  </verify>
  <done>model-viewer abre em 3/4 sem quebrar malhas reais; render_thumb gera fotos coloridas determinísticas (std>limiar) preservando a assinatura, e padronizar_fotos segue funcionando.</done>
</task>

</tasks>

<verification>
- `python manage.py check` passa.
- Smoke: rodar server dev em porta livre e abrir / (home), /sobre/, um produto, /carrinho/, e uma URL inexistente (com DEBUG=False para ver o 404 real).
- render3d: teste automatizado de std de pixels e determinismo.
</verification>

<success_criteria>
Os 9 achados do UAT fechados: (1) toggle mobile tocável, (2) header sem overflow, (3) menu legível, (4) tema dark consistente nas v2, (5) rebrand L3D visível, (6) 404/500 próprios, (7) home/sobre separadas + title + footer, (8) model-viewer 3/4, (9) fotos coloridas. Arquitetura em camadas e copy pt-br preservadas. SEM tasks de deploy.
</success_criteria>

<output>
Create `.planning/quick/260611-go4-corrigir-8-achados-do-uat-2026-06-11-fot/260611-go4-SUMMARY.md` when done
</output>
