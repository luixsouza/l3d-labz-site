---
quick_id: 260615-s0l
title: Redesign do PDF de orçamento — clean e profissional
status: ready
date: 2026-06-15
---

# Quick 260615-s0l — Redesign do PDF de orçamento (clean & profissional)

## Objetivo
Reescrever `apps/calculator/pdf.py` com um layout **clean e profissional** (validado visualmente via protótipo renderizado no contexto Django real). Manter a assinatura pública `gerar_orcamento_pdf(dados: dict) -> bytes` e a **trava de segurança** (só dados públicos: cliente_nome, peca_descricao, quantidade, prazo_entrega, observacoes, preco_venda, total — NUNCA custos internos).

## Decisões (já tomadas/aprovadas)
- **Marca:** monograma minimalista "L3D" (quadrado verde arredondado) + wordmark — escolhido pelo usuário em vez do logo.png (Luigi, risco de marca). Desenhado no canvas, sem depender de arquivo.
- **Layout:** letterhead (monograma + nome + tagline à esquerda; ORÇAMENTO + nº + data à direita; hairline verde) → card de dados 2×2 (cliente/prazo/descrição/validade) → tabela de itens (header verde, números à direita) → total em destaque (box verde-claro à direita) → observações (opcional) → rodapé com contatos (site · @instagram · e-mail) vindos do `settings.SITE`.
- **Nº de orçamento:** determinístico `ORC-YYYYMMDD-XXXX` (hash do nome do cliente).
- Paleta: verde `#2FA84F`/`#1E8C3E`, neutros cinza. Fonte Helvetica (built-in).

## Tasks

### Task 1 — Substituir `apps/calculator/pdf.py` pelo módulo redesenhado
- **files:** `apps/calculator/pdf.py`
- **action:** Substituir o conteúdo inteiro do arquivo pelo módulo verificado em `C:\Users\luixs\AppData\Local\Temp\pdf_final.py` (ler esse arquivo e escrever o conteúdo VERBATIM no `apps/calculator/pdf.py`). O módulo já está completo: docstring de segurança preservada, imports, paleta, `_format_brl`, `_ref_orcamento`, `_monograma`, `_cabecalho_rodape`, `gerar_orcamento_pdf`.
- **verify:** `DJANGO_SETTINGS_MODULE=config.settings.dev python -c "import django; django.setup(); from apps.calculator.pdf import gerar_orcamento_pdf; b=gerar_orcamento_pdf({'cliente_nome':'Teste','peca_descricao':'peça','quantidade':2,'prazo_entrega':'3 dias','observacoes':'','preco_venda':50.0,'total':100.0}); assert b[:4]==b'%PDF' and len(b)>2000; print('PDF OK', len(b))"`
- **done:** o comando acima imprime "PDF OK" com tamanho > 2000 bytes; `gerar_orcamento_pdf` mantém a mesma assinatura e contrato (a view `apps/calculator/views.py` não precisa mudar).

## must_haves
- `apps/calculator/pdf.py` exporta `gerar_orcamento_pdf(dados: dict) -> bytes` (assinatura inalterada).
- PDF gerado sem custos internos (só campos públicos do dict).
- Render bem-sucedido no contexto Django (settings.SITE lido para marca/contatos).
