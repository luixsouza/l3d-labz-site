---
phase: quick-260611-n3w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/catalog/models.py
  - apps/catalog/migrations/0XXX_productimage.py
  - apps/catalog/admin.py
  - apps/catalog/mappers.py
  - apps/catalog/queries.py
  - apps/catalog/management/commands/importar_makerworld.py
  - apps/catalog/templates/catalog/product_detail.html
  - apps/catalog/templates/catalog/partials/product_card.html
  - apps/core/templates/base.html
  - static/js/app.js
  - static/css/theme.css
autonomous: false
requirements: [QUICK-260611-N3W]

must_haves:
  truths:
    - "Nenhum botão 'Ver em 3D', nenhum <model-viewer> e nenhum 'Baixar STL' aparecem na UI"
    - "O detalhe do produto mostra todas as fotos (principal + galeria) num carrossel quando há 2+ fotos, ou imagem estática quando há 1"
    - "O importador salva TODAS as fotos na galeria (principal primeiro como image, restante como ProductImage) de forma idempotente"
    - "Sem OPENAI_API_KEY o import mantém o nome em inglês e emite aviso no stdout; com a chave o nome é traduzido pra pt-br"
    - "O produto 'mecha' importado das fixtures tem 5 imagens na galeria (6 fotos − 1 principal)"
  artifacts:
    - path: "apps/catalog/models.py"
      provides: "Model ProductImage (FK product related_name=gallery, image, order)"
      contains: "class ProductImage"
    - path: "apps/catalog/management/commands/importar_makerworld.py"
      provides: "Galeria completa + traduzir_nome + flag --com3d"
      contains: "def traduzir_nome"
    - path: "apps/catalog/templates/catalog/product_detail.html"
      provides: "Carrossel de fotos no .detail-media (sem model-viewer)"
      contains: "carousel"
  key_links:
    - from: "apps/catalog/queries.py"
      to: "Product.gallery"
      via: "prefetch_related no detail"
      pattern: "prefetch_related"
    - from: "static/js/app.js"
      to: ".carousel do detail"
      via: "handler genérico querySelectorAll('.carousel')"
      pattern: "querySelectorAll\\(\\\".carousel\\\"\\)"
---

<objective>
Três frentes no catálogo L3D Labz: (1) remover toda a visualização 3D da UI (dados/campos do model ficam), (2) substituir o viewer por uma galeria de fotos em carrossel no detalhe do produto, (3) traduzir os nomes pra pt-br no importador do MakerWorld.

Purpose: Decisão do dono — o site fica todo em pt-br, foto-centric e minimalista, sem 3D na interface.
Output: Model ProductImage + migration + admin inline; importador com galeria completa, tradução via OpenAI e GLB opcional; detalhe com carrossel; card/base/JS/CSS sem 3D; verificação com fixtures + screenshots Playwright.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Padrão de camadas e contratos já existentes. Use direto, sem explorar o codebase. -->

ProductMapper.to_detail (apps/catalog/mappers.py) — retorna dict; já expõe image_url (principal).
Adicionar chave `gallery: [url, ...]` com a PRINCIPAL primeiro, sem duplicar.

ProductQuery.detail_by_slug (apps/catalog/queries.py) — usa cache_utils.get_or_set + with_relations() (select_related category).
A galeria precisa de .prefetch_related("gallery") no producer.

Carrossel genérico já pronto em static/js/app.js (linhas ~78-107):
  document.querySelectorAll(".carousel").forEach(...) — espera:
    .carousel  (root)
    .carousel-track  (flex; transform translateX)
    .slide  (min-width:100%; um por foto)
    [data-carousel-dots]  (wrapper opcional; o JS injeta os botões)
    [data-carousel-prev] / [data-carousel-next]  (setas opcionais)
  Se houver < 2 .slide o handler retorna cedo (sem setas/dots) — ok p/ 1 foto.

CSS existente (static/css/theme.css):
  .carousel / .carousel-track / .slide / .carousel-dots / .carousel-arrow  (linhas ~569-579) — estilizados p/ banners full-bleed (slide com .bg img cover). Para o detail precisamos de slides quadrados com a foto inteira visível → adicionar regras escopadas em .detail-media .slide.
  .detail-media (linha 361) — aspect-ratio:1/1, place-items:center.
  Blocos a REMOVER: .viewer3d* (linhas ~2146-2211) e .btn-3d (linhas ~2215-2228).

importador (apps/catalog/management/commands/importar_makerworld.py):
  _foto_para_jpeg_quadrado(caminho: Path) -> bytes  — normalização JPG quadrado (reusar p/ cada foto da galeria).
  flag atual: --sem3d (action="store_true"). Inverter a semântica de default: hoje gera GLB por padrão; passar a NÃO gerar por padrão e adicionar --com3d.
  Idempotência por slug de pasta; foto principal = sorted(fotos)[0]; meta.get("titulo_en") / meta.get("titulo").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Model ProductImage + migration + admin inline + mapper/query + importador (galeria, tradução, GLB opcional)</name>
  <files>apps/catalog/models.py, apps/catalog/admin.py, apps/catalog/mappers.py, apps/catalog/queries.py, apps/catalog/management/commands/importar_makerworld.py</files>
  <action>
**models.py** — adicionar `class ProductImage(TimeStampedModel)`: `product = ForeignKey(Product, on_delete=CASCADE, related_name="gallery", verbose_name="produto")`; `image = ImageField("imagem", upload_to="products/gallery/")`; `order = PositiveSmallIntegerField("ordem", default=0)`. `Meta`: `verbose_name="imagem da galeria"`, `verbose_name_plural="imagens da galeria"`, `ordering=("order",)`. `__str__` retorna `f"{self.product.name} #{self.order}"`. Docstring/labels em pt-br.

Gerar migration: `python manage.py makemigrations catalog` (cria 0XXX_productimage). NÃO remover model_3d/model_stl — campos ficam.

**admin.py** — adicionar `class ProductImageInline(admin.TabularInline)`: `model = ProductImage`, `extra = 1`, `fields = ("image", "order")`. Registrar em `ProductAdmin.inlines = (ProductImageInline,)`.

**mappers.py** — em `ProductMapper.to_detail`, adicionar chave `gallery`: lista de URLs com a foto PRINCIPAL primeiro (`instance.image.url` se houver, senão `instance.image_url`), seguida das `instance.gallery.all()` (ordenadas por order via Meta), sem duplicar a principal. Filtrar entradas vazias. LIMPAR as chaves de 3D do dict: remover `model_3d_url`, `has_3d` de `to_dict`; remover `model_3d_url`, `stl_url`, `has_3d_model`, `has_stl` de `to_detail`. Atualizar o comentário "campos de visualização 3D no card" (remover).

**queries.py** — em `ProductQuery.detail_by_slug`, no `producer()`, adicionar `.prefetch_related("gallery")` ao queryset antes do `.filter(slug=slug).first()`. Pode remover o método `with_3d` (não é mais usado pela UI) — confirmar com grep que nada o referencia; se referenciado, deixar.

**importar_makerworld.py**:
- Inverter flag 3D: remover/ressignificar `--sem3d`; adicionar `--com3d` (action="store_true", help "Gera GLB do modelo.3mf (UI 3D foi removida; padrão é não gerar)."). A geração de GLB passa a ocorrer SÓ quando `options["com3d"]` e existir modelo.3mf. As `dimensoes_mm` do 3mf continuam preenchendo `dimensions` SEMPRE que houver modelo.3mf (mesmo sem --com3d), pois dimensão é metadado barato — coletar mesh só p/ dimensões quando há 3mf; gerar GLB só com --com3d.
- `def traduzir_nome(titulo_en: str) -> str`: lê `OPENAI_API_KEY` de `os.environ`; sem chave → retorna `titulo_en` e escreve aviso no stdout ("OPENAI_API_KEY ausente — mantendo nome em inglês: ..."). Com chave → POST via `urllib.request` (stdlib) a `https://api.openai.com/v1/chat/completions`, JSON: model `"gpt-4o-mini"`, temperature `0.2`, messages = [system, user]. System (pt-br): tradutor de nomes de produtos de impressão 3D para pt-br de e-commerce, curto e natural, mantém marcas e nomes próprios (Star Wars, Pokémon), responde SÓ o nome traduzido sem aspas. User = `titulo_en`. Header `Authorization: Bearer <chave>`, `Content-Type: application/json`. Timeout ~20s. Em QUALQUER exceção → retorna `titulo_en` + aviso no stdout. Resposta: `data["choices"][0]["message"]["content"].strip().strip('"')`.
- Cache anti-retradução: antes de chamar `traduzir_nome`, checar produto existente por slug — se `existente` E `existente.name != (titulo_en or titulo_pt)` (ou seja, o nome atual já difere do título do meta.json → já foi traduzido/editado), NÃO chama a API: reusa `existente.name`. Caso contrário, `nome_produto = traduzir_nome(titulo_en or titulo_pt or pasta.name)`.
- Galeria: após salvar a foto principal (`fotos[0]`), apagar a galeria existente do produto (`p.gallery.all().delete()` — idempotência) e recriar: para cada foto em `fotos[1:]` (ordem por nome, já `sorted`), normalizar com `_foto_para_jpeg_quadrado`, criar `ProductImage(product=p, order=idx)` e `.image.save(f"{slug}-{idx}.jpg", ContentFile(bytes), save=True)`. Pular fotos que falharem na normalização (warning, não aborta).
- Import de ProductImage no topo (`from apps.catalog.models import Category, Product, ProductImage`).
- Atualizar docstring do comando (pt-br): refletir galeria + tradução + --com3d, e que a UI 3D foi removida.
  </action>
  <verify>
    <automated>cd C:/dev/l3d-labz-site; python manage.py makemigrations catalog --check --dry-run; python manage.py migrate; python -c "import ast,sys; ast.parse(open('apps/catalog/management/commands/importar_makerworld.py',encoding='utf-8').read()); ast.parse(open('apps/catalog/mappers.py',encoding='utf-8').read()); print('AST OK')"</automated>
  </verify>
  <done>Migration criada e aplicada; ProductImage existe com related_name=gallery; admin tem inline; to_detail expõe gallery sem chaves 3D; detail_by_slug faz prefetch da gallery; importador tem traduzir_nome, flag --com3d e grava galeria completa idempotente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remover 3D da UI + carrossel no detalhe</name>
  <files>apps/catalog/templates/catalog/product_detail.html, apps/catalog/templates/catalog/partials/product_card.html, apps/core/templates/base.html, static/js/app.js, static/css/theme.css</files>
  <action>
**product_card.html** — remover o bloco `{% if product.has_3d %}...botão .btn-3d...{% endif %}` (linhas ~33-40). Resto do card intacto.

**base.html** — remover o bloco do modal `<div class="viewer3d" id="viewer3d" ...>...</div>` (linhas ~40-60) E o `<script type="module" ... model-viewer ...>` global (linhas ~63-65). Manter o `<script src="app.js" defer>` e o `{% block extra_js %}`.

**product_detail.html** — substituir o `.detail-media` (linhas ~13-33): remover o `{% if product.has_3d_model %}<model-viewer>...{% endif %}` inteiro. Renderizar carrossel a partir de `product.gallery`:
  - Se `product.gallery|length > 1`: estrutura `<div class="carousel">` → `<div class="carousel-track">` com um `<div class="slide">` por url contendo `<img src="{{ url }}" alt="{{ product.name }}">`; adicionar `<div class="carousel-dots" data-carousel-dots></div>`, `<button class="carousel-arrow prev" data-carousel-prev>` e `<button class="carousel-arrow next" data-carousel-next>` (com `<svg class="icon"><use href="#i-chevron-left"></use></svg>` e `#i-chevron-right`; usar ícones que existam no sprite — verificar em core/partials/icons.html, fallback p/ texto se faltar).
  - Senão (1 foto ou nenhuma): manter imagem estática — `<img src="{{ product.gallery.0|default:product.image_url }}" ...>` com o mesmo `onerror` de fallback do picsum já usado; manter o `.thumb-mono` como placeholder quando sem foto.
  - Remover o bloco "Baixar STL" (`{% if product.has_stl %}...{% endif %}`, linhas ~74-78).
  - Remover o `{% block extra_js %}` com o script do model-viewer (linhas ~99-104) inteiro.

**static/js/app.js** — remover o bloco IIFE `/* ----- modal 3D ----- */ (function viewer3d() {...})();` (linhas ~149-204). O handler genérico de `.carousel` (linhas ~78-107) já cobre o carrossel do detalhe — NÃO mexer nele.

**static/css/theme.css** — remover os blocos `/* modal 3D */`: todas as regras `.viewer3d*` (linhas ~2146-2211) e `.btn-3d` (linhas ~2215-2228). Adicionar regras escopadas p/ o carrossel dentro de `.detail-media` para que a foto apareça inteira e quadrada (a regra global `.slide .bg img` é p/ banners; aqui usamos `<img>` direto): `.detail-media .carousel { width:100%; height:100%; border:0; border-radius:inherit; }` e `.detail-media .slide { display:grid; place-items:center; aspect-ratio:1/1; }` e `.detail-media .slide img { width:100%; height:100%; object-fit:cover; }`. Manter `.carousel-arrow`/`.carousel-dots` globais (reusados).
  </action>
  <verify>
    <automated>cd C:/dev/l3d-labz-site; python -c "import re,sys; b=open('apps/core/templates/base.html',encoding='utf-8').read(); d=open('apps/catalog/templates/catalog/product_detail.html',encoding='utf-8').read(); c=open('apps/catalog/templates/catalog/partials/product_card.html',encoding='utf-8').read(); j=open('static/js/app.js',encoding='utf-8').read(); css=open('static/css/theme.css',encoding='utf-8').read(); assert 'model-viewer' not in b and 'viewer3d' not in b, 'base.html ainda tem 3D'; assert 'model-viewer' not in d and 'Baixar STL' not in d, 'detail ainda tem 3D/STL'; assert 'btn-3d' not in c, 'card ainda tem btn-3d'; assert 'carousel' in d, 'detail sem carrossel'; assert 'viewer3d' not in j, 'app.js ainda tem modal 3D'; assert 'btn-3d' not in css and 'viewer3d' not in css, 'css ainda tem 3D'; print('UI 3D removida + carrossel OK')"</automated>
  </verify>
  <done>Nenhuma referência a model-viewer/viewer3d/btn-3d/Baixar STL na UI; detalhe usa .carousel quando há 2+ fotos e imagem estática com 1; CSS do carrossel escopado no .detail-media adicionado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Import das fixtures `.uat/fixtures-mw/` SEM OPENAI_API_KEY (nomes em inglês mantidos com aviso), migrate aplicado, e screenshots Playwright do detalhe com carrossel e do card sem 3D.</what-built>
  <how-to-verify>
1. Garantir que `OPENAI_API_KEY` NÃO está setada no ambiente (`Remove-Item Env:OPENAI_API_KEY -ErrorAction SilentlyContinue` no PowerShell).
2. Rodar o import das fixtures:
   `cd C:/dev/l3d-labz-site; python manage.py importar_makerworld --base .uat/fixtures-mw --categoria "Casa & Organização"`
   - Esperado no stdout: aviso "OPENAI_API_KEY ausente — mantendo nome em inglês" para mecha e aurashell; nomes ficam em inglês.
3. Verificar a galeria do mecha (deve ter 5 imagens — 6 fotos − 1 principal):
   `python manage.py shell -c "from apps.catalog.models import Product; p=Product.objects.get(slug='mecha-style-self-locking-stackable-drawer-storage'); print('gallery:', p.gallery.count())"`
   - Esperado: `gallery: 5`
4. Subir o servidor: `python manage.py runserver` e abrir:
   - `http://127.0.0.1:8000/catalogo/` → cards SEM botão "Ver em 3D".
   - Detalhe do mecha → `.detail-media` mostra carrossel (setas + dots), navegação funciona, fotos quadradas inteiras. Detalhe do aurashell (2 fotos) → carrossel com 2 slides.
5. Screenshots Playwright em `.uat/screenshots/`:
   - `N3W-01-catalogo-sem3d.png` (lista de produtos)
   - `N3W-02-detalhe-carrossel.png` (detalhe do mecha com carrossel)
  </how-to-verify>
  <resume-signal>Digite "approved" se galeria=5, nomes em inglês com aviso, e nenhum 3D na UI; ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importador → OpenAI API | título_en (de meta.json scrapado) sai do processo via HTTPS; resposta entra como nome do produto |
| filesystem fixtures → ImageField | fotos arbitrárias normalizadas via Pillow antes de salvar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n3w-01 | Information Disclosure | OPENAI_API_KEY em traduzir_nome | mitigate | chave só de os.environ; nunca logada; sem chave degrada (mantém inglês) |
| T-n3w-02 | Tampering | resposta da OpenAI vira product.name | accept | uso interno (comando admin), nome é texto exibido; .strip('"') limita ruído; sem execução |
| T-n3w-03 | Denial of Service | POST sem timeout trava o import | mitigate | urllib com timeout ~20s; exceção → fallback p/ título original |
| T-n3w-04 | Tampering | fotos de fixtures via Pillow | mitigate | _foto_para_jpeg_quadrado re-encoda p/ JPEG (descarta payloads); falha → pula a foto |
| T-n3w-SC | Tampering | nenhum pacote novo instalado | accept | reusa urllib (stdlib) e Pillow já presente — sem npm/pip install |
</threat_model>

<verification>
- `python manage.py makemigrations catalog --check --dry-run` → sem mudanças pendentes após a migration.
- `python manage.py migrate` → aplica 0XXX_productimage sem erro.
- AST parse dos arquivos Python editados OK.
- Asserts de strings: model-viewer/viewer3d/btn-3d/"Baixar STL" ausentes na UI; "carousel" presente no detail.
- Import das fixtures sem chave: galeria do mecha = 5; nomes em inglês com aviso.
- Screenshots Playwright capturadas em .uat/screenshots/.
</verification>

<success_criteria>
- ProductImage existe (related_name=gallery, CASCADE, order) + migration aplicada + admin inline.
- Importador grava TODAS as fotos (principal + galeria) idempotentemente, traduz com OPENAI_API_KEY e degrada sem ela, e só gera GLB com --com3d.
- Toda a UI 3D removida (card, base, detail, app.js, theme.css); "Baixar STL" removido.
- Detalhe mostra carrossel com 2+ fotos (handler/CSS genéricos reusados) e imagem estática com 1.
- Verificação com fixtures confirma galeria=5 no mecha e nomes em inglês com aviso.
- Copy nova em pt-br; commits conventional sem nenhum trailer.
</success_criteria>

<output>
Create `.planning/quick/260611-n3w-catalogo-sem-3d-carrossel-de-fotos-tradu/260611-n3w-SUMMARY.md` when done
</output>
