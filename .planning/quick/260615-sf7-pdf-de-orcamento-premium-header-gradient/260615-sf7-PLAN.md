---
quick_id: 260615-sf7
title: PDF de orçamento premium (header gradiente, marca d'água, total card)
status: ready
date: 2026-06-15
---

# Quick 260615-sf7 — PDF de orçamento PREMIUM

## Objetivo
Elevar `apps/calculator/pdf.py` para um layout **premium** (validado visualmente no contexto Django real, inclusive com total de 6 dígitos e caso sem observações). Manter a assinatura `gerar_orcamento_pdf(dados: dict) -> bytes` e a **trava de segurança** (só dados públicos — nunca custos internos).

## Design (aprovado/validado)
- **Header full-bleed com gradiente verde** (`#34B85A`→`#1B7E37`), monograma branco "L3D", wordmark + tagline, e à direita "O R Ç A M E N T O" + pill branca com o nº.
- **Marca d'água** "L3D" gigante e suave no espaço vazio.
- Blocos **"FATURAR PARA"** (cliente) e **"DETALHES"** (emissão / validade +7d / prazo) com hairlines.
- **Tabela de itens** header verde, cantos arredondados no topo, números à direita.
- **Resumo à direita:** Subtotal + **card TOTAL verde-escuro** com valor grande branco (largura aguenta até 6 dígitos sem quebrar linha).
- **Bloco "CONDIÇÕES & OBSERVAÇÕES"** (verde-claro): observações do cliente + condições padrão.
- **Faixa de rodapé escura** (`#0F172A`) com borda verde, "Obrigado pela preferência." + contatos (site · @instagram · e-mail) do `settings.SITE`.
- Nº determinístico `ORC-YYYYMMDD-XXXX`. Fonte Helvetica (built-in, sem dep nova).

## Tasks

### Task 1 — Substituir `apps/calculator/pdf.py` pelo módulo premium
- **files:** `apps/calculator/pdf.py`
- **action:** Ler `C:\Users\luixs\AppData\Local\Temp\pdf_final2.py` e escrever o conteúdo VERBATIM em `apps/calculator/pdf.py` (substituição integral). O módulo já está completo e validado: docstring de segurança, paleta, `_format_brl`, `_ref_orcamento`, `_monograma`, `_decor`, `gerar_orcamento_pdf`.
- **verify:** `DJANGO_SETTINGS_MODULE=config.settings.dev .venv/Scripts/python.exe -c "import django; django.setup(); from apps.calculator.pdf import gerar_orcamento_pdf; b=gerar_orcamento_pdf({'cliente_nome':'Teste','peca_descricao':'peça','quantidade':2,'prazo_entrega':'3 dias','observacoes':'obs','preco_venda':50.0,'total':100.0}); assert b[:4]==b'%PDF' and len(b)>3000; print('PDF OK', len(b))"`
- **done:** imprime "PDF OK" (>3000 bytes); assinatura/contrato preservados (view não muda).

## must_haves
- `gerar_orcamento_pdf(dados: dict) -> bytes` (assinatura inalterada).
- PDF sem custos internos (só campos públicos).
- Render OK no contexto Django (settings.SITE lido para marca/contatos).
