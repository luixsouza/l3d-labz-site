---
phase: quick-260616-f2c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/core/templates/base.html
  - static/css/theme.css
  - static/js/lithophane-editor.js
autonomous: false
requirements: [CONV-FAB-IG, CONV-LITHO-AR]

must_haves:
  truths:
    - "Em qualquer página, um botão flutuante de Instagram aparece fixo no canto inferior direito e abre SITE.instagram em nova aba"
    - "O botão flutuante é acessível (aria-label pt-br) e não cobre conteúdo importante no mobile"
    - "No editor de litofane, depois de gerar o 3D, o model-viewer expõe AR (atributos ar + ar-modes + botão 'Ver no seu espaço')"
  artifacts:
    - path: "apps/core/templates/base.html"
      provides: "FAB de Instagram global fora dos blocks"
      contains: "SITE.instagram"
    - path: "static/css/theme.css"
      provides: "Estilo .fab-ig usando design tokens"
      contains: ".fab-ig"
    - path: "static/js/lithophane-editor.js"
      provides: "Atributos AR + slot ar-button no model-viewer do litofane"
      contains: "ar-modes"
  key_links:
    - from: "apps/core/templates/base.html"
      to: "settings.SITE.instagram"
      via: "context processor site_settings (SITE no contexto global)"
      pattern: "SITE\\.instagram"
    - from: "static/js/lithophane-editor.js"
      to: "<model-viewer> #litho-viewer"
      via: "setAttribute ar/ar-modes + ar-button slot injetado"
      pattern: "ar-modes"
---

<objective>
Dois itens leves de conversão de front-end:

(A) **Botão flutuante (FAB) de Instagram global** — fixo no canto inferior direito, presente em todas as páginas, abre `SITE.instagram` em nova aba. Minimalista e elegante, usando os design tokens da marca (verde accent), acessível e seguro no mobile.

(B) **AR no preview 3D do editor de litofane** — espelhar o padrão já feito em `product_detail.html` (quick 260616-e8v): `ar` + `ar-modes="webxr scene-viewer quick-look"` + botão `ar-button` em pt-br ("Ver no seu espaço"). Como o `<model-viewer>` do litofane é criado dinamicamente em JS (`_mostrarViewer`), os atributos AR e o slot vão no JS, não no template.

Purpose: aumentar conversão (canal direto pro Instagram + experiência AR no editor) sem peso de framework.
Output: FAB global verificável headless no HTML; AR do litofane com markup verificável no JS (funcionamento real depende de device com AR → checkpoint humano).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Tudo que o executor precisa já está no projeto — NÃO recriar nem explorar. -->

SITE chega aos templates via context processor (apps/core/context_processors.py):
  def site_settings(request): return {"SITE": settings.SITE}
  → {{ SITE.name }} já é usado em base.html. Usar {{ SITE.instagram }}.

settings.SITE (config/settings/base.py ~226):
  {"name": "L3D Labz", "accent": "#2FA84F", "instagram": "https://instagram.com/l3d_labz"}
  Nota: "instagram" pode estar vazio em algum ambiente ("vazio = some do footer").
  → Renderizar o FAB só quando SITE.instagram tiver valor: {% if SITE.instagram %}.

Ícone Instagram JÁ EXISTE no sprite (apps/core/templates/core/partials/icons.html):
  <symbol id="i-instagram" ...> → usar <svg class="icon"><use href="#i-instagram"></use></svg>
  NÃO adicionar ícone novo.

base.html — estrutura relevante (apps/core/templates/base.html):
  ...
  {% include "core/partials/footer.html" %}
  <script src="{% static 'js/app.js' %}" defer></script>
  {% block extra_js %}{% endblock %}
  </body>
  → O FAB entra logo APÓS o include do footer e ANTES do <script>, fora de qualquer {% block %}.

Tokens disponíveis em theme.css (:root): --accent #2FA84F, --accent-strong #1E8C3E,
  --accent-glow rgba(47,168,79,.26), --radius/--radius-lg, --shadow, --ease, --header-h, --bg-card, --border.
  Classe .detail-ar-btn (theme.css ~422) JÁ existe e estiliza o botão AR (fundo --accent, hover --accent-strong,
  .icon 15x15). Reusar essa classe no litofane (não criar CSS novo pro AR).

Padrão AR de referência (apps/catalog/templates/catalog/product_detail.html ~64-84):
  <model-viewer ... ar ar-modes="webxr scene-viewer quick-look" ...>
    <button slot="ar-button" class="detail-ar-btn" type="button">
      <svg class="icon"><use href="#i-cube"></use></svg> Ver no seu espaço
    </button>
  </model-viewer>

Criação do viewer no litofane (static/js/lithophane-editor.js, função _mostrarViewer ~210-235):
  viewer = document.createElement("model-viewer");
  viewer.id = "litho-viewer";
  viewer.setAttribute("camera-controls", "");
  viewer.setAttribute("auto-rotate", "");
  viewer.setAttribute("touch-action", "pan-y");
  viewer.setAttribute("shadow-intensity", "1");
  viewerWrap.appendChild(viewer);   // só no primeiro mostrar (if (!viewer))
  ...
  viewer.setAttribute("src", data.glb_url);
  → Adicionar os atributos AR e injetar o slot ar-button DENTRO do if (!viewer) {...},
    junto da criação (uma vez só), antes do appendChild.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAB de Instagram global no base.html + estilo .fab-ig</name>
  <files>apps/core/templates/base.html, static/css/theme.css</files>
  <action>
    Em apps/core/templates/base.html, logo APÓS `{% include "core/partials/footer.html" %}` e antes do `<script src=".../app.js">`, fora de qualquer {% block %}, adicionar um link FAB envolto em `{% if SITE.instagram %}` (CONV-FAB-IG): uma tag `<a class="fab-ig" href="{{ SITE.instagram }}" target="_blank" rel="noopener" aria-label="Fale com a L3D Labz no Instagram">` contendo `<svg class="icon"><use href="#i-instagram"></use></svg>`. O ícone i-instagram já existe no sprite — não adicionar.

    Em static/css/theme.css, adicionar a classe `.fab-ig`: position:fixed; canto inferior direito (right/bottom ~1.1rem, em mobile manter folga segura ~1rem para não cobrir conteúdo); círculo (largura/altura ~52px, border-radius:999px); display:grid; place-items:center; z-index acima do header sticky (header usa z-index:50 → usar z-index:60); usar tokens da marca para fundo (linear-gradient(135deg, var(--accent), var(--accent-strong)) ou var(--accent)), cor do ícone #fff, sombra var(--shadow) reforçada com --accent-glow; transição com var(--ease) e leve translateY/scale no :hover; `.fab-ig .icon { width:24px; height:24px; }`. Minimalista e elegante (CLAUDE.md). NÃO hardcodar verde solto onde existe token equivalente. Garantir contraste/tamanho de toque adequados no mobile e que o FAB não sobreponha conteúdo crítico (offset seguro + tamanho moderado).
  </action>
  <verify>
    <automated>python -c "import pathlib,re; b=pathlib.Path('apps/core/templates/base.html').read_text(encoding='utf-8'); c=pathlib.Path('static/css/theme.css').read_text(encoding='utf-8'); assert 'SITE.instagram' in b and 'fab-ig' in b and 'i-instagram' in b, 'base.html FAB ausente'; assert '.fab-ig' in c and 'position: fixed' in c.replace('position:fixed','position: fixed'), 'CSS .fab-ig ausente'; print('OK')"</automated>
  </verify>
  <done>base.html renderiza um `<a class="fab-ig">` fixo (dentro de {% if SITE.instagram %}) que aponta para {{ SITE.instagram }}, abre em nova aba com rel="noopener", tem aria-label pt-br e usa o ícone #i-instagram; theme.css tem `.fab-ig` com position:fixed, tokens da marca e z-index > 50.</done>
</task>

<task type="auto">
  <name>Task 2: AR no model-viewer do editor de litofane</name>
  <files>static/js/lithophane-editor.js</files>
  <action>
    Em static/js/lithophane-editor.js, dentro de `_mostrarViewer`, no bloco `if (!viewer) { ... }` (onde o `<model-viewer>` é criado uma única vez, ~linha 215), adicionar os atributos AR espelhando product_detail.html (CONV-LITHO-AR): `viewer.setAttribute("ar", "")` e `viewer.setAttribute("ar-modes", "webxr scene-viewer quick-look")`. Em seguida injetar o botão do slot ar-button antes do `viewerWrap.appendChild(viewer)`: criar um `<button>` com `setAttribute("slot","ar-button")`, `setAttribute("type","button")`, `className = "detail-ar-btn"` (classe já existente no theme.css — reusar, não criar CSS), e `innerHTML = '<svg class="icon"><use href="#i-cube"></use></svg> Ver no seu espaço'`, então `viewer.appendChild(btnAr)`. Manter todo o resto de `_mostrarViewer` intacto (camera-controls, auto-rotate, touch-action, shadow-intensity, src/poster/exposure, toggle de luz). Copy em pt-br.
  </action>
  <verify>
    <automated>python -c "import pathlib; s=pathlib.Path('static/js/lithophane-editor.js').read_text(encoding='utf-8'); assert 'ar-modes' in s and 'webxr scene-viewer quick-look' in s and 'ar-button' in s and 'Ver no seu espaço' in s and 'detail-ar-btn' in s, 'AR ausente no litofane'; print('OK')"</automated>
  </verify>
  <done>O model-viewer #litho-viewer criado em _mostrarViewer recebe os atributos `ar` e `ar-modes="webxr scene-viewer quick-look"` e contém um `<button slot="ar-button" class="detail-ar-btn">Ver no seu espaço</button>`, sem quebrar o fluxo existente (camera-controls, toggle de luz).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    (A) FAB de Instagram global fixo no canto inferior direito (base.html + .fab-ig em theme.css).
    (B) AR adicionado ao model-viewer do editor de litofane (lithophane-editor.js).
  </what-built>
  <how-to-verify>
    1. Rodar `python manage.py runserver` e abrir qualquer página (ex.: home `/`).
       - Confirmar via screenshot headless (Chrome) que o FAB de Instagram aparece fixo no canto inferior direito, com visual minimalista/verde da marca, e NÃO cobre conteúdo importante (testar também viewport mobile ~390px).
       - Clicar/inspecionar: deve abrir https://instagram.com/l3d_labz em nova aba.
    2. Abrir o editor de litofane (`/lithophane/` ou rota do "Faça meu Lithophane"), enviar uma foto e clicar "Gerar 3D".
       - Após o 3D aparecer, inspecionar o `<model-viewer id="litho-viewer">`: deve ter os atributos `ar` e `ar-modes="webxr scene-viewer quick-look"` e conter o botão "Ver no seu espaço".
       - NOTA: o botão AR / o lançamento do AR só funcionam de fato em dispositivo compatível (Android Scene Viewer / iOS Quick Look). No desktop headless o botão pode não ficar visível — isso é esperado. O que se valida aqui é a PRESENÇA do markup/atributos no HTML renderizado, igual ao critério do quick 260616-e8v.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que precisa ajustar (posição/visual do FAB, markup do AR).</resume-signal>
</task>

</tasks>

<verification>
- FAB: HTML estático presente (grep `SITE.instagram` + `fab-ig` + `i-instagram` em base.html), CSS `.fab-ig` com position:fixed e z-index>50. Layout confirmado por screenshot headless (desktop + mobile).
- Litofane AR: grep `ar-modes` + `ar-button` + `Ver no seu espaço` + `detail-ar-btn` em lithophane-editor.js. Funcionamento de AR depende de device → coberto pelo checkpoint humano (presença do markup é o critério verificável, como no e8v).
- Nada de cores hardcoded soltas onde há token equivalente; toda copy nova em pt-br.
</verification>

<success_criteria>
- Em todas as páginas (quando SITE.instagram preenchido), o FAB de Instagram aparece fixo no canto inferior direito, abre o Instagram em nova aba, é acessível (aria-label pt-br) e não cobre conteúdo no mobile.
- O preview 3D do editor de litofane oferece AR ("Ver no seu espaço") espelhando o padrão de product_detail.html, sem regredir o editor.
- Mudanças usam design tokens existentes e o ícone/sprite/classe CSS já presentes (sem recriação).
</success_criteria>

<output>
Create `.planning/quick/260616-f2c-conversao-leve-botao-flutuante-de-instag/260616-f2c-SUMMARY.md` when done
</output>
