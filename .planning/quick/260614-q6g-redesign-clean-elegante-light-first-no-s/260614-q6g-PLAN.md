---
phase: quick-260614-q6g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/css/theme.css
  - apps/catalog/services.py
  - apps/catalog/mappers.py
  - apps/core/views.py
  - apps/core/templates/core/home.html
autonomous: false
requirements: [Q6G-REDESIGN]
must_haves:
  truths:
    - "Home renderiza em tema light por padrao com paleta Clean & Elegante (off-white quente, verde Luigi contido, azul secundario real)"
    - "Hero da home mostra um <model-viewer> girando sozinho quando ha produto em destaque com model_3d"
    - "Hero faz fallback elegante (visual atual sem viewer) quando nenhum produto em destaque tem model_3d"
    - "Botao 'Baixar STL' aparece no hero quando o produto-3D tem model_stl"
    - "Tema dark continua funcionando pelo toggle, sem quebrar"
    - "product_detail, catalogo e checkout continuam legiveis com os novos tokens light"
  artifacts:
    - path: "static/css/theme.css"
      provides: "Bloco [data-theme=light] reconciliado com tokens-alvo + tokens azuis + sombras difusas + estilos do hero 3D e faixa de prova social"
      contains: "--accent-blue-soft"
    - path: "apps/catalog/services.py"
      provides: "Metodo que retorna 1 produto-3D em destaque para o hero da home (ou None)"
      contains: "model_3d"
    - path: "apps/core/templates/core/home.html"
      provides: "Hero split com <model-viewer> + fallback"
      contains: "model-viewer"
  key_links:
    - from: "apps/core/views.py"
      to: "apps/catalog/services.py"
      via: "HomeView.get_context_data chama CatalogService para hero_3d"
      pattern: "hero_3d"
    - from: "apps/core/templates/core/home.html"
      to: "model_3d.url"
      via: "src do <model-viewer>"
      pattern: "model-viewer"
---

<objective>
Aplicar o redesign visual "Clean & Elegante" (light-first, variante B do sketch vencedor) no site REAL, end-to-end, via TOKENS. Duas frentes:

1. **Rebrand via tokens** — reequilibrar o bloco `[data-theme="light"]` em `static/css/theme.css` para a paleta aprovada (off-white quente, verde Luigi contido, AZUL secundario real, sombras difusas). Reconciliar os 3 blocos `[data-theme="light"]` existentes que hoje se sobrescrevem com valores divergentes.
2. **Hero 3D na home** — trocar o bloco decorativo `.gh-deco` por um `<model-viewer>` com auto-rotate (texto/CTA a esquerda, viewer a direita), alimentado pelo GLB de um produto em destaque que tenha `model_3d`, com FALLBACK elegante (visual atual) quando nenhum tiver.

Purpose: o core value do L3D Labz e visualizar o modelo 3D de forma intuitiva num site bonito e minimalista com a marca. Esse plano coloca o 3D no hero e finaliza a identidade light.

Output: theme.css com paleta light reconciliada + tokens azuis + estilos do hero 3D/prova social; CatalogService expondo um produto-3D para o hero; mapper expondo `model_3d_url`/`model_stl_url`; HomeView passando `hero_3d`; home.html com `<model-viewer>` + fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

REGRAS DO DONO (obrigatorias):
- Commits SEM trailer "Co-Authored-By". Cada task = 1 commit atomico SO de codigo.
- NAO commitar SUMMARY/PLAN/STATE (o orquestrador cuida disso). NAO atualizar ROADMAP.md.
- pt-br em toda copy nova.
- NAO declarar UI pronta sem screenshot real (Chrome headless). A verificacao final e VISUAL.
- Trabalho pesado SEMPRE no server, nunca no desktop (vale para qualquer build/serving pesado de media 3D).
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contratos ja existentes no codebase — usar diretamente, sem explorar. -->

apps/catalog/queries.py — JA EXISTE query pronta para produtos com 3D:
```python
class ProductQuery(BaseQuery):
    @staticmethod
    def with_3d(limit: int | None = None) -> list[Product]:
        # Product.objects.active().with_relations().exclude(model_3d="").order_by("-sales_count")
```

apps/catalog/models.py — campos e propriedades:
```python
class Product(TimeStampedModel):
    model_3d  = FileField(upload_to="products/models/", blank=True)  # .glb/.gltf
    model_stl = FileField(upload_to="products/stl/", blank=True)     # .stl
    is_featured = BooleanField(default=False)
    @property
    def has_3d_model(self) -> bool: return bool(self.model_3d)
    @property
    def has_stl(self) -> bool:      return bool(self.model_stl)
```

apps/catalog/services.py — namespace de @staticmethod (ja tem get_models_3d que usa ProductQuery.with_3d()):
```python
class CatalogService(BaseService):
    @staticmethod
    def get_models_3d() -> dict:
        return {"products": ProductMapper.to_list(ProductQuery.with_3d())}
```

apps/catalog/mappers.py — ProductMapper.to_dict NAO expoe model_3d/model_stl hoje. `image_url` ja usa o padrao `instance.image.url if instance.image else ...`.

apps/core/views.py — HomeView.get_context_data monta: featured, new_arrivals, home_categories, hero_promo. Importa CatalogService localmente.

apps/core/templates/core/base? — base.html JA tem data-theme="light" default e boot script lendo localStorage 'l3d-theme'. Blocks disponiveis: {% block extra_head %} e {% block extra_js %}.

CDN do model-viewer JA usado no projeto (apps/lithophane/templates/lithophane/editor.html:128) — REUSAR exatamente:
https://cdn.jsdelivr.net/npm/@google/model-viewer@4.3.1/dist/model-viewer.min.js (carregado como <script type="module">)
</interfaces>

<theme_css_map>
<!-- ATENCAO: existem TRES blocos [data-theme="light"] em theme.css que se sobrescrevem.
     A cascata (ultimo vence) precisa terminar nos tokens-alvo travados. -->
- Linhas ~678-696: bloco principal de tokens light (hoje --bg #FFFFFF, --accent-2 #1E8C3E, sem tokens azuis extras nem sombra-alvo).
- Linhas ~1161-1163: override de --shadow / --shadow-soft.
- Linhas ~1171-1177: RE-override de --accent (#1eb457), --accent-2 (#15914a), --accent-strong, --accent-soft, --accent-glow — DIVERGE da paleta travada. Este e o que "vence" hoje para accent.
- Linha ~26 / ~693: --accent-blue ja existe (#2BA6E0) no :root e no light.
- .proof-strip ~1991-1999: hoje usa --bg-card + borda; alvo = fundo gradiente verde->azul claro.
- .geek-hero--clean ~1930-1967: hero verde vibrante; .gh-deco ~1932 = bloco decorativo a ser substituido pelo viewer no template (CSS do hero split fica aqui).
</theme_css_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliar tokens light + azul secundario + sombras difusas + prova social</name>
  <files>static/css/theme.css</files>
  <action>
Reequilibrar o tema light para a paleta Clean & Elegante TRAVADA, reconciliando os 3 blocos `[data-theme="light"]` que hoje divergem (a cascata final deve terminar nos valores-alvo, sem deixar override antigo "ganhar").

Tokens light alvo (ajustar/garantir nos blocos `[data-theme="light"]`):
- Superficies: `--bg #FAFBF9`, `--bg-soft #F2F5F0`, `--bg-card #FFFFFF`, `--bg-elevated #F1F4EF`.
- Bordas: `--border #E7EBE4`, `--border-soft #F0F3EC`.
- Texto: `--text #16201A`, `--text-muted #5C6B60`, `--text-dim #9AA89E`.
- Verde: `--accent #2FA84F`, `--accent-2 #43C266`, `--accent-strong #1E8C3E`, `--accent-soft #E7F6EC`. IMPORTANTE: o bloco ~1171-1177 hoje sobrescreve `--accent`/`--accent-2` para outro verde (#1eb457/#15914a) — alinhar esse bloco aos valores travados (ou remover os overrides divergentes) para que o verde da marca seja consistente. Decisao de overlap de cascata: ajustar o bloco que vence por ultimo.
- Sombras difusas (substituir os valores nos blocos ~678-696 e ~1161-1163): `--shadow 0 16px 40px -26px rgba(23,38,80,.20)`, `--shadow-soft 0 6px 20px -16px rgba(23,38,80,.16)`. Reduzir dependencia de bordas duras: onde o light usa `border: 1px solid var(--border)` em cards/superficies, preferir a sombra suave (manter a borda apenas como hairline `--border-soft` quando necessario para contraste).

Azul secundario REAL (no :root dark E no bloco light, conforme ja convive `--accent-blue`):
- Garantir `--accent-blue #2BA6E0` e ADICIONAR (se nao existirem) `--accent-blue-soft #E6F4FB` e `--accent-blue-ink #0E5E84`.
- Aplicar azul em: `.link` (links "Ver tudo/catalogo" da home usam classe `.link` -> hoje `color: var(--accent-2)`; trocar para `var(--accent-blue)` no light, mantendo hover). Badges de "novo"/3D (`.badge-new`, `.cat-badge` quando indicar 3D) recebem fundo/realce azul claro (`--accent-blue-soft` / `--accent-blue-ink`).

Faixa de prova social (`.proof-strip` ~1991): no light, trocar o fundo `var(--bg-card)` por gradiente sutil verde->azul claro: `linear-gradient(110deg, var(--accent-soft), var(--accent-blue-soft))`, mantendo `border-radius`/`box-shadow` difuso; os numeros `.proof b` continuam em verde forte e legiveis sobre o gradiente claro.

Chips de categoria: ja usam `--acc:{{ cat.accent }}` (variante B pede chips com cor de acento) — garantir que no light o chip respeita a cor de acento da categoria com bom contraste; NAO hardcodar cor unica.

NAO mexer no bloco `:root` dark (alternativa do toggle) alem de adicionar os 2 tokens azuis novos. NAO reescrever arquitetura de classes. Preservar o tema dark intacto.
  </action>
  <verify>
    <automated>python manage.py check 2>&1 | tail -5; grep -c -- '--accent-blue-soft' static/css/theme.css</automated>
  </verify>
  <done>theme.css contem `--accent-blue-soft`/`--accent-blue-ink`; os 3 blocos `[data-theme="light"]` resultam na paleta travada (verde da marca consistente, off-white #FAFBF9, sombras difusas); `.link` no light usa azul; `.proof-strip` no light tem gradiente verde->azul; `python manage.py check` sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Expor produto-3D do hero (service + mapper + view) e hero <model-viewer> com fallback</name>
  <files>apps/catalog/services.py, apps/catalog/mappers.py, apps/core/views.py, apps/core/templates/core/home.html</files>
  <action>
Levar o GLB de um produto em destaque ao hero da home, com fallback elegante.

**Mapper (apps/catalog/mappers.py):** em `ProductMapper.to_dict`, adicionar campos `model_3d_url` e `model_stl_url` (seguindo o padrao de `image_url`): `instance.model_3d.url if instance.model_3d else ""` e idem para `model_stl`. Adicionar tambem `has_3d` (`instance.has_3d_model`) e `has_stl` (`instance.has_stl`) para o template decidir. NAO duplicar logica; manter dentro de `to_dict` (assim `to_detail` herda).

**Service (apps/catalog/services.py):** adicionar `@staticmethod get_hero_3d_product() -> dict | None` que retorna 1 produto-3D para o hero. Regra de selecao: preferir destaque — usar `ProductQuery.with_3d(limit=1)` (ja ordena por `-sales_count` e exclui sem model_3d); dentre eles priorizar `is_featured` se houver (pode filtrar/ordenar por `is_featured` antes de cair no primeiro com 3D). Retornar `ProductMapper.to_dict(produto)` do escolhido, ou `None` se nenhum produto ativo tiver `model_3d`. Manter o estilo namespace/staticmethod e a query existente (NAO criar ORM novo no service — reusar `ProductQuery`).

**View (apps/core/views.py):** em `HomeView.get_context_data`, adicionar `context["hero_3d"] = CatalogService.get_hero_3d_product()` (import local ja existe). View continua fina.

**Template (apps/core/templates/core/home.html):** substituir o bloco decorativo `<div class="gh-deco">...</div>` (existe nos DOIS ramos do `{% if hero_promo %}`) por um stage de viewer condicional:
- Quando `hero_3d` existir: renderizar `<model-viewer src="{{ hero_3d.model_3d_url }}" auto-rotate camera-controls rotation-per-second="22deg" ... >` dentro de um wrapper `.gh-viewer` (texto/CTA continuam a esquerda, viewer a direita — o grid `.geek-hero--clean` ja e split). Incluir legenda "arraste pra girar" e, quando `hero_3d.has_stl`, um botao/link "Baixar STL" apontando para `{{ hero_3d.model_stl_url }}` (download). Copy em pt-br.
- Quando `hero_3d` for falsy: manter o `.gh-deco` atual (fallback elegante, visual de hoje) — nao mostrar viewer.

**Script do model-viewer (so na home):** carregar via `{% block extra_js %}` (ou `extra_head`) o MESMO CDN ja usado em lithophane/editor.html: `<script type="module" src="https://cdn.jsdelivr.net/npm/@google/model-viewer@4.3.1/dist/model-viewer.min.js"></script>`. NAO incluir global no base.html. Carregar so quando `hero_3d` existir (envolver o `<script>` no `{% if hero_3d %}`).

Estilos do `.gh-viewer`/`<model-viewer>` do hero (dimensoes, aspect-ratio, cantos, drop-shadow difusa, legenda) entram em static/css/theme.css junto ao bloco `.geek-hero--clean` — reusar tokens (raio, sombra). NAO usar borda dura; sombra difusa. Garantir responsivo: em <=760px o grid ja vira 1 coluna; o viewer deve continuar visivel (ou degradar) no mobile sem quebrar layout.
  </action>
  <verify>
    <automated>python manage.py check 2>&1 | tail -5; grep -n 'model-viewer' apps/core/templates/core/home.html; grep -n 'get_hero_3d_product\|model_3d_url' apps/catalog/services.py apps/catalog/mappers.py</automated>
  </verify>
  <done>`HomeView` passa `hero_3d`; `home.html` renderiza `<model-viewer>` quando ha produto-3D e mantem `.gh-deco` no fallback; botao "Baixar STL" condicional a `has_stl`; script model-viewer carrega so na home via block, com o CDN ja usado no projeto; `python manage.py check` sem erros.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Redesign Clean & Elegante (light-first) aplicado via tokens + hero 3D na home com fallback.
Task 1: paleta light reconciliada (off-white quente, verde Luigi contido, azul secundario em links/badges/prova social, sombras difusas).
Task 2: hero da home com `<model-viewer>` auto-rotate alimentado por produto-3D em destaque, botao "Baixar STL", legenda "arraste pra girar", e fallback elegante quando nao ha model_3d.
  </what-built>
  <how-to-verify>
Verificacao VISUAL obrigatoria (regra do dono — screenshot real, Chrome headless; build/serving pesado no SERVER, nao no desktop):

1. `python manage.py check` — sem erros.
2. `python manage.py runserver` (ou ambiente do server).
3. HOME em LIGHT (default), desktop:
   - Paleta off-white quente #FAFBF9, cards brancos com sombra difusa (sem bordas duras gritando), verde Luigi so em CTA/detalhe, AZUL visivel nos links "Ver tudo/catalogo" e na faixa de prova social (gradiente verde->azul).
   - Hero split: texto/CTA a esquerda. SE houver produto em destaque com model_3d -> `<model-viewer>` girando sozinho a direita, com "arraste pra girar" e "Baixar STL" (se tiver STL).
4. HOME em LIGHT, MOBILE (viewport ~390px): hero empilha, viewer/fallback nao quebra, faixa de prova social legivel.
5. FALLBACK: confirmar a home renderizando SEM nenhum produto-3D em destaque -> hero mantem o visual atual (`.gh-deco`), sem viewer, sem erro. (Se todos os destaques tiverem 3D, testar despromovendo temporariamente ou apontando para estado sem model_3d.)
6. Spot-check de regressao com novos tokens light: `catalogo/` (product_list), uma pagina product_detail, e o fluxo de checkout — tudo legivel e sem cor quebrada.
7. DARK pelo toggle (botao de tema): trocar para dark e confirmar que NADA quebrou (tema dark intacto).

Tirar screenshots: home light desktop, home light mobile, home dark (regressao). Anexar.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas (cores, viewer, fallback, regressao) para revisao.</resume-signal>
</task>

</tasks>

<verification>
- `python manage.py check` sem erros (apos cada task de codigo).
- Home renderiza em light COM model_3d (viewer aparece, gira, "Baixar STL" quando ha STL) e SEM model_3d (fallback `.gh-deco`, sem viewer).
- Script do model-viewer carrega so na home, usando o CDN @google/model-viewer@4.3.1 ja adotado no projeto.
- product_detail, product_list (catalogo) e checkout sem regressao visual com os novos tokens.
- Tema dark continua funcionando pelo toggle.
- Verificacao final VISUAL via screenshot real (Chrome headless): home light desktop+mobile + dark.
</verification>

<success_criteria>
- Tema light usa a paleta Clean & Elegante travada (off-white #FAFBF9, verde da marca consistente, azul secundario real em links/badges/prova social, sombras difusas) com os 3 blocos `[data-theme="light"]` reconciliados.
- Hero da home mostra `<model-viewer>` auto-rotate com produto-3D em destaque + botao "Baixar STL" condicional + legenda; fallback elegante quando nao ha model_3d.
- Arquitetura em camadas preservada (query reusada, service magro, view fina, mapper expoe os campos, template apresenta).
- pt-br em toda copy nova; tema dark intacto.
- Commits atomicos por task, SEM Co-Authored-By; sem tocar docs/ROADMAP.
</success_criteria>

<output>
Create `.planning/quick/260614-q6g-redesign-clean-elegante-light-first-no-s/260614-q6g-SUMMARY.md` when done.
</output>
