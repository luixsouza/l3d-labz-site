---
phase: 03-visualizador-3d-galeria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/catalog/queries.py
  - apps/catalog/services.py
  - apps/catalog/views.py
  - apps/catalog/urls.py
  - apps/catalog/templates/catalog/models_3d.html
  - apps/core/templates/core/partials/navbar.html
autonomous: true
requirements: [VIEW-04]
must_haves:
  truths:
    - "Existe uma rota /catalogo/modelos-3d/ (name catalog:models_3d) acessível pela navbar."
    - "A galeria lista APENAS produtos com GLB (ProductQuery.with_3d exclui model_3d='') ordenados por -sales_count."
    - "Cada card da galeria leva ao detalhe (onde está o viewer) — sem viewer embutido por card."
    - "Sem produtos com modelo, a galeria mostra um empty state pt-br, sem quebra."
  artifacts:
    - path: "apps/catalog/queries.py"
      provides: "ProductQuery.with_3d(limit) — só produtos com GLB"
      contains: "def with_3d"
    - path: "apps/catalog/services.py"
      provides: "CatalogService.gallery() — context com lista mapeada (ProductMapper)"
      contains: "def gallery"
    - path: "apps/catalog/views.py"
      provides: "models_3d(request) — render da galeria"
      contains: "def models_3d"
    - path: "apps/catalog/urls.py"
      provides: "rota modelos-3d/ name=models_3d"
      contains: "modelos-3d/"
    - path: "apps/catalog/templates/catalog/models_3d.html"
      provides: "Galeria: hero curto + grid reusando product_card + empty state"
      contains: "product_card.html"
    - path: "apps/core/templates/core/partials/navbar.html"
      provides: "Link 'Modelos 3D' na main-nav"
      contains: "catalog:models_3d"
  key_links:
    - from: "navbar.html (.main-nav)"
      to: "catalog:models_3d"
      via: "{% url 'catalog:models_3d' %}"
      pattern: "url 'catalog:models_3d'"
    - from: "views.models_3d"
      to: "CatalogService.gallery"
      via: "render(request, 'catalog/models_3d.html', CatalogService.gallery())"
      pattern: "CatalogService\\.gallery"
    - from: "CatalogService.gallery"
      to: "ProductQuery.with_3d"
      via: "ProductMapper.to_list(ProductQuery.with_3d())"
      pattern: "ProductQuery\\.with_3d"
  human_verification:
    - "Abrir /catalogo/modelos-3d/ pela navbar: aparecem só produtos com GLB; clicar num card abre o detalhe com o viewer. (Visual — phase verification cobre.)"
---

<objective>
Entregar a aba/galeria "Modelos 3D" (VIEW-04): uma página de navegação que lista somente os
produtos que têm modelo 3D, seguindo a arquitetura em camadas existente
(query → service → view → url → template) e adicionando o link na navbar. Cards levam ao
detalhe (onde está o viewer) — sem viewer embutido por card (perf).

Purpose: o cliente descobre os produtos com modelo 3D por um caminho dedicado.
Output: `with_3d` em queries, `gallery` em services, `models_3d` em views, rota em urls,
template `models_3d.html` (novo) e link na navbar.

Independente da Plan 01 (concerns diferentes; nenhum arquivo em comum) — roda em paralelo (wave 1).
A galeria NÃO carrega o script do model-viewer (cards só linkam ao detalhe).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visualizador-3d-galeria/03-CONTEXT.md
@.planning/phases/03-visualizador-3d-galeria/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@apps/catalog/queries.py
@apps/catalog/services.py
@apps/catalog/views.py
@apps/catalog/urls.py
@apps/catalog/templates/catalog/partials/product_card.html
@apps/core/templates/core/partials/navbar.html

<interfaces>
<!-- Padrões existentes a espelhar (já no repo). -->
# queries.py — espelhar ProductQuery.on_sale:
#   Product.objects.active().with_relations()  já é o queryset otimizado.
# services.py — espelhar CatalogService.browse (retorna dict de context).
#   ProductMapper.to_list(...) mapeia Model->dict para os cards.
#   ProductMapper.to_dict NÃO tem campos 3D — ok, a galeria sabe que todos têm modelo.
# views.py — finas: def x(request): return render(request, "...", Service.method())
# urls.py — app_name="catalog"; montado em /catalogo/ (config/urls.py).
# product_card.html — espera dict `product`; já tem badges/links próprios.
# navbar.html — links ficam em <nav class="main-nav" id="mainNav"> (linhas 9-14).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Camada Python — with_3d, gallery, models_3d, rota</name>
  <files>apps/catalog/queries.py, apps/catalog/services.py, apps/catalog/views.py, apps/catalog/urls.py</files>
  <read_first>
    - apps/catalog/queries.py (espelhar `ProductQuery.on_sale`, linhas 69-81)
    - apps/catalog/services.py (espelhar `CatalogService.browse` / `list_on_sale`, linhas 31-32)
    - apps/catalog/views.py (padrão fino, linhas 10-20)
    - apps/catalog/urls.py (urlpatterns, app_name="catalog")
    - .planning/phases/03-visualizador-3d-galeria/03-CONTEXT.md (bloco VIEW-04 com o Python exato)
    - .planning/phases/03-visualizador-3d-galeria/03-RESEARCH.md (seção "Django Side")
  </read_first>
  <action>
    (A) `apps/catalog/queries.py` — adicionar dentro de `class ProductQuery` (depois de `on_sale`)
    VERBATIM do CONTEXT/RESEARCH:

    ```python
        @staticmethod
        def with_3d(limit: int | None = None) -> list[Product]:
            qs = (
                Product.objects.active().with_relations()
                .exclude(model_3d="")
                .order_by("-sales_count")
            )
            return list(qs[:limit]) if limit else list(qs)
    ```
    `.exclude(model_3d="")` seleciona só quem tem GLB. Sem cache (locked: cache é opcional;
    preferir simplicidade). NÃO mexer em `invalidate_catalog_cache`.

    (B) `apps/catalog/services.py` — adicionar em `class CatalogService` (perto de `list_on_sale`),
    espelhando o shape de retorno simples:

    ```python
        # ---- galeria de modelos 3D ----
        @staticmethod
        def gallery() -> dict[str, Any]:
            return {"products": ProductMapper.to_list(ProductQuery.with_3d())}
    ```
    `ProductQuery` já está importado no topo. Usa `ProductMapper.to_dict` (via `to_list`) — correto
    para os cards (não precisa dos campos 3D; todos já têm modelo).

    (C) `apps/catalog/views.py` — adicionar a view fina (padrão de 3 linhas):

    ```python
    def models_3d(request):
        return render(request, "catalog/models_3d.html", CatalogService.gallery())
    ```

    (D) `apps/catalog/urls.py` — adicionar a rota em `urlpatterns` (pt-br, sob o mount /catalogo/):

    ```python
        path("modelos-3d/", views.models_3d, name="models_3d"),
    ```
  </action>
  <verify>
    <automated>python manage.py check --settings=config.settings.prod</automated>
  </verify>
  <acceptance_criteria>
    - queries.py contains 'def with_3d'
    - queries.py contains 'exclude(model_3d='
    - services.py contains 'def gallery'
    - services.py contains 'ProductQuery.with_3d'
    - views.py contains 'def models_3d'
    - urls.py contains 'modelos-3d/' e "name=\"models_3d\""
    - `python manage.py check --settings=config.settings.prod` sai 0
  </acceptance_criteria>
  <done>Cadeia query→service→view→url completa; `manage.py check` (prod) passa; rota catalog:models_3d resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Template da galeria + link na navbar</name>
  <files>apps/catalog/templates/catalog/models_3d.html, apps/core/templates/core/partials/navbar.html</files>
  <read_first>
    - apps/catalog/templates/catalog/product_list.html (referência de estrutura: extends base, section/container, product-grid reusando o partial, empty-state — linhas 75-103)
    - apps/catalog/templates/catalog/partials/product_card.html (espera dict `product`; cada card linka ao detalhe)
    - apps/core/templates/core/partials/navbar.html (main-nav, linhas 9-14)
    - .planning/phases/03-visualizador-3d-galeria/03-CONTEXT.md (bloco VIEW-04: hero curto "Modelos 3D", grid reusando product_card, empty state pt-br, link na navbar)
  </read_first>
  <action>
    (A) Criar `apps/catalog/templates/catalog/models_3d.html` — extends base, hero curto, grid
    reusando o partial, empty state pt-br. NÃO carregar model-viewer aqui (cards só linkam ao
    detalhe). Conteúdo:

    ```django
    {% extends "base.html" %}
    {% block title %}Modelos 3D · {{ SITE.name }}{% endblock %}
    {% block content %}
    <section class="section">
      <div class="container">
        <div class="search-hero reveal">
          <span class="eyebrow eyebrow-center">Visualização 3D</span>
          <h1 style="max-width:18ch;margin-inline:auto">Modelos 3D</h1>
          <p class="text-muted">Produtos com modelo 3D para girar, dar zoom e explorar antes de comprar.</p>
        </div>

        {% if products %}
          <div class="product-grid mt-2">
            {% for product in products %}
              <div class="reveal" style="--i:{{ forloop.counter0 }}">{% include "catalog/partials/product_card.html" with product=product %}</div>
            {% endfor %}
          </div>
        {% else %}
          <div class="empty-state">
            <div class="ph"><svg class="icon"><use href="#i-box"></use></svg></div>
            <h3>Nenhum modelo 3D ainda</h3>
            <p class="text-muted">Em breve teremos produtos com visualização 3D por aqui.</p>
            <a href="{% url 'catalog:product_list' %}" class="btn btn-ghost mt-2">Ver o catálogo</a>
          </div>
        {% endif %}
      </div>
    </section>
    {% endblock %}
    ```
    Reusar classes existentes (`section`, `container`, `search-hero`, `eyebrow-center`,
    `product-grid`, `empty-state`, `reveal`) — sem CSS novo. Texto pt-br.

    (B) Editar `apps/core/templates/core/partials/navbar.html` — adicionar dentro de
    `<nav class="main-nav" id="mainNav">` (linhas 9-14), após o link "Lançamentos":

    ```django
      <a href="{% url 'catalog:models_3d' %}">Modelos 3D</a>
    ```
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; g=pathlib.Path('apps/catalog/templates/catalog/models_3d.html').read_text(encoding='utf-8'); n=pathlib.Path('apps/core/templates/core/partials/navbar.html').read_text(encoding='utf-8'); miss=[]; [miss.append('gallery:'+s) for s in ['{% extends \"base.html\" %}','catalog/partials/product_card.html','empty-state','Modelos 3D'] if s not in g]; (\"url 'catalog:models_3d'\" in n) or miss.append('navbar:link'); ('<model-viewer' in g) and miss.append('gallery has viewer (should not)'); print('MISSING:',miss) or sys.exit(1) if miss else print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - models_3d.html exists
    - models_3d.html contains '{% extends "base.html" %}'
    - models_3d.html contains 'catalog/partials/product_card.html' (reuso do card)
    - models_3d.html contains 'empty-state' (empty state pt-br)
    - models_3d.html NÃO contém '<model-viewer' (sem viewer por card)
    - navbar.html contains "url 'catalog:models_3d'"
  </acceptance_criteria>
  <done>Galeria renderiza o grid (ou empty state) e está acessível pela navbar; cards levam ao detalhe; sem viewer embutido.</done>
</task>

</tasks>

<verification>
- `python manage.py check --settings=config.settings.prod` sai 0.
- Cadeia completa: queries.with_3d → services.gallery → views.models_3d → urls modelos-3d/ → models_3d.html.
- navbar tem o link `catalog:models_3d`; galeria reusa `product_card.html` e tem empty state; sem `<model-viewer>` na galeria.
- Visual (human_verification): /catalogo/modelos-3d/ lista só produtos com GLB; card → detalhe com viewer.
</verification>

<success_criteria>
- VIEW-04: aba "Modelos 3D" na navegação lista apenas produtos com modelo 3D; cards levam ao detalhe.
- Arquitetura em camadas respeitada (espelha on_sale/browse); galeria não embute viewer (perf).
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-visualizador-3d-galeria/03-02-SUMMARY.md`
</output>
