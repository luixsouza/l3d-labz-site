---
phase: 03-visualizador-3d-galeria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/catalog/templates/catalog/product_detail.html
  - static/css/theme.css
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]
must_haves:
  truths:
    - "Num produto com GLB, o detalhe renderiza um <model-viewer> interativo (rotacionar/zoom/pan via camera-controls)."
    - "Num produto sem GLB, a .detail-media mantém o monograma + <img> com onerror, sem quebra de layout."
    - "Num produto com STL, aparece um botão 'Baixar STL' (download) perto do bloco de compra; sem STL, o botão não aparece."
    - "O script CDN do model-viewer só é carregado no detalhe quando há GLB (gated em extra_js)."
    - "O container do viewer tem aspect-ratio/altura, radius e fundo por token — não colapsa nem fica preto."
  artifacts:
    - path: "apps/catalog/templates/catalog/product_detail.html"
      provides: "Viewer 3D (VIEW-01), fallback de imagem (VIEW-02), botão Baixar STL (VIEW-03), script CDN gated em extra_js"
      contains: "<model-viewer"
    - path: "static/css/theme.css"
      provides: "Estilo do container .detail-media model-viewer (aspect-ratio, radius, fundo por token)"
      contains: ".detail-media model-viewer"
  key_links:
    - from: "product_detail.html (.detail-media)"
      to: "product.model_3d_url / product.image_url"
      via: "<model-viewer src=... poster=...> dentro de {% if product.has_3d_model %}"
      pattern: "model-viewer.*src=.*model_3d_url"
    - from: "product_detail.html (extra_js)"
      to: "cdn.jsdelivr.net/.../model-viewer@4.3.1"
      via: "<script type=module> gated por has_3d_model"
      pattern: "model-viewer@4"
    - from: "product_detail.html (bloco de compra)"
      to: "product.stl_url"
      via: "<a href download> gated por has_stl"
      pattern: "stl_url"
  human_verification:
    - "Visual/interativo: abrir um produto com GLB e confirmar que dá pra rotacionar (arrastar), dar zoom (scroll/pinça) e mover (pan: botão direito ou Ctrl+arrastar / dois dedos). O poster aparece enquanto carrega; sem layout shift. Tema claro/escuro: o fundo do viewer acompanha. (Não automatizável — phase verification cobre o visual.)"
    - "Clicar 'Baixar STL' baixa o arquivo .stl em vez de navegar."
---

<objective>
Entregar a experiência 3D na página de detalhe do produto (o coração desta fase):
viewer interativo `<model-viewer>` quando há GLB (VIEW-01), fallback gracioso para a
imagem atual quando não há (VIEW-02), e botão "Baixar STL" quando há STL (VIEW-03).
O script CDN do model-viewer é carregado por página e só quando necessário.

Purpose: O core value do projeto — "o cliente visualiza o modelo 3D de forma intuitiva".
Output: `product_detail.html` editado (viewer + fallback + botão STL + script gated) e
regras CSS para o container do viewer em `theme.css`.

Os campos já existem (Fase 2): `ProductMapper.to_detail` entrega `model_3d_url`, `stl_url`,
`has_3d_model`, `has_stl`. Esta plan NÃO mexe em Python — é template + CSS apenas.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visualizador-3d-galeria/03-CONTEXT.md
@.planning/phases/03-visualizador-3d-galeria/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@apps/catalog/templates/catalog/product_detail.html
@static/css/theme.css

<interfaces>
<!-- Contratos já disponíveis no dict `product` (ProductMapper.to_detail). Usar direto. -->
product.has_3d_model : bool   # True quando há GLB
product.model_3d_url : str    # URL do .glb (ex.: /media/...glb) — "" se não houver
product.image_url    : str    # imagem do produto (usar como poster)
product.has_stl      : bool   # True quando há STL
product.stl_url      : str    # URL do .stl — "" se não houver
product.name, product.slug, product.accent, product.monogram  # já usados no template

# base.html expõe `{% block extra_js %}{% endblock %}` (vazio) na linha 41 — sobrescrever no detalhe.
# Ícones disponíveis no sprite (core/partials/icons.html): #i-box, #i-layers (ambos existem).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viewer 3D + fallback + script CDN gated no detalhe</name>
  <files>apps/catalog/templates/catalog/product_detail.html</files>
  <read_first>
    - apps/catalog/templates/catalog/product_detail.html (alvo — bloco `.detail-media` nas linhas 13-19; o template NÃO declara `extra_js` hoje)
    - .planning/phases/03-visualizador-3d-galeria/03-RESEARCH.md (seções "Code Examples", "Interaction Attributes", "Per-page script loading" — tag/atributos EXATOS)
  </read_first>
  <action>
    Editar `apps/catalog/templates/catalog/product_detail.html`.

    (A) Substituir o conteúdo atual da `.detail-media` (linhas 13-19) por um `{% if %}/{% else %}`.
    Quando há GLB, renderizar o `<model-viewer>` com a tag VERBATIM da pesquisa (03-RESEARCH.md,
    "Full .detail-media block (VIEW-01 + VIEW-02)"). O bloco final deve ser exatamente:

    ```django
      <div class="detail-media reveal">
        {% if product.has_3d_model %}
          <model-viewer src="{{ product.model_3d_url }}" poster="{{ product.image_url }}"
                        alt="Modelo 3D de {{ product.name }}"
                        camera-controls auto-rotate touch-action="pan-y"
                        shadow-intensity="1" loading="lazy" reveal="auto"
                        ar ar-modes="webxr scene-viewer quick-look" ar-scale="auto"></model-viewer>
        {% else %}
          <span class="thumb-mono" style="--acc:{{ product.accent }}">{{ product.monogram }}</span>
          {% if product.image_url %}
            <img src="{{ product.image_url }}" alt="{{ product.name }}"
                 onerror="this.onerror=null;this.src='https://picsum.photos/seed/{{ product.slug }}/800/800'">
          {% endif %}
        {% endif %}
      </div>
    ```
    (O ramo `{% else %}` preserva o comportamento atual — VIEW-02. NÃO alterar o monograma/img.)

    (B) Adicionar, no FIM do arquivo (depois do `{% endblock %}` do content), um bloco `extra_js`
    que carrega o script CDN do model-viewer SOMENTE quando há GLB (VERBATIM da pesquisa,
    "Per-page CDN script (gated)"):

    ```django
    {% block extra_js %}
      {% if product.has_3d_model %}
      <script type="module"
              src="https://cdn.jsdelivr.net/npm/@google/model-viewer@4.3.1/dist/model-viewer.min.js"></script>
      {% endif %}
    {% endblock %}
    ```
    NÃO carregar no base.html. NÃO usar `nomodule`. NÃO desafixar a versão (manter `@4.3.1`).
    Pan já vem ligado com `camera-controls` (não existe `enable-pan`); NÃO adicionar `disable-pan`.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('apps/catalog/templates/catalog/product_detail.html').read_text(encoding='utf-8'); req=['<model-viewer','camera-controls','touch-action=\"pan-y\"','model_3d_url','poster=\"{{ product.image_url }}\"','{% if product.has_3d_model %}','model-viewer@4.3.1','{% block extra_js %}','type=\"module\"']; missing=[s for s in req if s not in t]; print('MISSING:',missing) or sys.exit(1) if missing else print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - product_detail.html contains '<model-viewer'
    - product_detail.html contains 'camera-controls'
    - product_detail.html contains 'touch-action="pan-y"'
    - product_detail.html contains '{{ product.model_3d_url }}'
    - product_detail.html contains '{% if product.has_3d_model %}' (viewer gated)
    - product_detail.html contains 'model-viewer@4.3.1' (versão fixada)
    - product_detail.html contains '{% block extra_js %}' e 'type="module"'
    - product_detail.html NÃO contém 'nomodule' nem 'enable-pan'
    - O ramo {% else %} ainda contém 'thumb-mono' e o <img> com 'onerror' (fallback intacto)
  </acceptance_criteria>
  <done>Detalhe com GLB renderiza o viewer e carrega o script; detalhe sem GLB mostra o fallback de imagem; nenhuma alteração no base.html.</done>
</task>

<task type="auto">
  <name>Task 2: Botão "Baixar STL" + CSS do container do viewer</name>
  <files>apps/catalog/templates/catalog/product_detail.html, static/css/theme.css</files>
  <read_first>
    - apps/catalog/templates/catalog/product_detail.html (bloco de compra nas linhas 46-60; inserir o botão logo após o `</form>`/`{% endif %}` da compra, dentro da `.mt-3`)
    - static/css/theme.css (tokens em `:root` — usar `--radius-lg`, `--bg-soft`, `--border`, `--accent`; achar uma seção de produtos/detalhe para anexar a regra)
    - .planning/phases/03-visualizador-3d-galeria/03-RESEARCH.md (seções "STL Download (VIEW-03)" e "Sizing / CSS (VIEW-01)" — markup e CSS EXATOS)
  </read_first>
  <action>
    (A) STL — em `apps/catalog/templates/catalog/product_detail.html`, dentro da `<div class="mt-3">`
    (a que contém o form de compra, linhas 46-60), logo ABAIXO do bloco
    `{% if product.in_stock %}...{% else %}...{% endif %}` da compra, inserir VERBATIM
    (03-RESEARCH.md "STL Download (VIEW-03)"):

    ```django
          {% if product.has_stl %}
            <a href="{{ product.stl_url }}" download class="btn btn-ghost" style="margin-top:.8rem">
              <svg class="icon"><use href="#i-box"></use></svg> Baixar STL
            </a>
          {% endif %}
    ```
    Usar classes existentes (`btn btn-ghost`), atributo `download`, ícone `#i-box` (existe no sprite),
    texto pt-br "Baixar STL". Sem STL → nada renderiza. Sem JS.

    (B) CSS — em `static/css/theme.css`, adicionar (numa seção de detalhe/produtos, com comentário
    pt-br no padrão `/* --- ... */`) a regra VERBATIM da pesquisa (03-RESEARCH.md "Sizing / CSS"):

    ```css
    /* --- viewer 3D (detalhe) --- */
    .detail-media model-viewer {
      display: block;
      width: 100%;
      aspect-ratio: 1 / 1;
      border-radius: var(--radius-lg);
      background: var(--bg-soft);
      border: 1px solid var(--border);
      overflow: hidden;
      --poster-color: var(--bg-soft);
      --progress-bar-color: var(--accent);
      --progress-mask: transparent;
    }
    ```
    Usar APENAS tokens existentes (sem cores literais). O `aspect-ratio: 1 / 1` evita colapso/reflow
    (pitfall #2 da pesquisa) e casa com a convenção 800x800 do `<img>` fallback.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; h=pathlib.Path('apps/catalog/templates/catalog/product_detail.html').read_text(encoding='utf-8'); c=pathlib.Path('static/css/theme.css').read_text(encoding='utf-8'); miss=[]; [miss.append(s) for s in ['{% if product.has_stl %}','{{ product.stl_url }}','download','Baixar STL'] if s not in h]; [miss.append('css:'+s) for s in ['.detail-media model-viewer','aspect-ratio','var(--radius-lg)','--progress-bar-color'] if s not in c]; print('MISSING:',miss) or sys.exit(1) if miss else print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - product_detail.html contains '{% if product.has_stl %}'
    - product_detail.html contains '{{ product.stl_url }}' e 'download'
    - product_detail.html contains 'Baixar STL'
    - theme.css contains '.detail-media model-viewer'
    - theme.css contains 'aspect-ratio' e 'var(--radius-lg)' e '--progress-bar-color'
    - theme.css NÃO introduz cores literais hex no bloco do viewer (só tokens var(--...))
  </acceptance_criteria>
  <done>Produto com STL mostra "Baixar STL" (download); container do viewer tem tamanho/estilo por token e acompanha o tema.</done>
</task>

</tasks>

<verification>
- `python manage.py check --settings=config.settings.prod` sai 0 (dev falha por debug_toolbar — usar prod).
- product_detail.html: viewer gated por `has_3d_model`, fallback intacto no `{% else %}`, botão STL gated por `has_stl`, `extra_js` carrega `model-viewer@4.3.1` só com GLB.
- theme.css: `.detail-media model-viewer` com `aspect-ratio` e tokens.
- Visual (human_verification): rotacionar/zoom/pan num produto com GLB; fallback de imagem sem GLB; download do STL; tema claro/escuro no fundo do viewer.
</verification>

<success_criteria>
- VIEW-01: detalhe com GLB renderiza `<model-viewer>` interativo (camera-controls = rotate+zoom+pan).
- VIEW-02: detalhe sem GLB mantém monograma + `<img>` onerror, sem quebra de layout.
- VIEW-03: detalhe com STL exibe botão "Baixar STL" (download); sem STL, não exibe.
- Script CDN carregado por página, gated, versão fixada `@4.3.1`; base.html intacto.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-visualizador-3d-galeria/03-01-SUMMARY.md`
</output>
