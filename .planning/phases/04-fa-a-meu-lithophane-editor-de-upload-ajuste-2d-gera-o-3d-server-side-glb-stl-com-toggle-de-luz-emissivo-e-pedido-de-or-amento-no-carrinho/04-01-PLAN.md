---
phase: 04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - apps/lithophane/__init__.py
  - apps/lithophane/generation.py
  - apps/lithophane/tests/__init__.py
  - apps/lithophane/tests/test_generation.py
autonomous: true
requirements: [LITHO-GEN]
must_haves:
  truths:
    - "Uma foto real vira um GLB watertight com a foto embutida como emissiveTexture"
    - "A mesma foto vira um STL binário válido e fechado"
    - "O relevo é invertido: pixel claro = fino (espessura_min), pixel escuro = grosso (espessura_max)"
    - "O GLB gerado fica abaixo de 5 MB (controlado por resolução do heightmap + textura ≤512px)"
  artifacts:
    - path: "apps/lithophane/generation.py"
      provides: "Motor isolado LithophaneGenerator.gerar() → (glb_bytes, stl_bytes)"
      min_lines: 80
    - path: "apps/lithophane/tests/test_generation.py"
      provides: "Testes de bancada: watertight, emissive GLB, inversão, tamanho"
      contains: "is_watertight"
  key_links:
    - from: "apps/lithophane/generation.py"
      to: "trimesh.Scene.export(file_type='glb')"
      via: "PBRMaterial com emissiveTexture=PIL.Image"
      pattern: "emissiveTexture"
    - from: "apps/lithophane/generation.py"
      to: "mesh.is_watertight"
      via: "process=False + repair fill_holes se necessário"
      pattern: "is_watertight"
---

<objective>
Validar e construir o motor de geração 3D do lithophane (`generation.py`) — a parte mais nova e arriscada da fase — via TDD, ANTES de qualquer UI. O motor recebe uma `PIL.Image` ajustada + specs e devolve `(glb_bytes, stl_bytes)`: GLB com a foto embutida como `emissiveTexture` (para o toggle "com luz"), STL imprimível, ambos a partir de uma malha watertight com relevo invertido.

Purpose: Desbloquear cedo o risco técnico central. As três incertezas do RESEARCH (Open Questions 1 e 2) — `mesh.is_watertight` após montar paredes, e se trimesh 4.12.2 embute `emissiveTexture`/`KHR_materials_emissive_strength` no GLB — são resolvidas aqui por teste executável, não na UI.
Output: `apps/lithophane/generation.py` (motor isolado, espelha `apps/orders/payments.py`), suíte de testes de bancada, e `numpy`/`trimesh` adicionados ao `requirements.txt`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-RESEARCH.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-CONTEXT.md
@apps/orders/payments.py

<interfaces>
<!-- Contrato do motor (definir EXATAMENTE assim — Plan 02 consome via LithophaneService). -->
<!-- Fonte: 04-RESEARCH.md Pattern 1, 2, 3. -->

apps/lithophane/generation.py — assinatura pública:

```python
from dataclasses import dataclass
from typing import Literal
from PIL import Image

FormatoLitho = Literal["placa", "luminaria"]

@dataclass
class EspecsLitho:
    largura_mm: float        # ex: 100.0
    espessura_min_mm: float  # ex: 0.8  (partes claras / finas)
    espessura_max_mm: float  # ex: 3.0  (partes escuras / grossas)
    resolucao_px: int        # ex: 300  (maior lado do heightmap)
    formato: FormatoLitho

class LithophaneGenerator:
    @staticmethod
    def gerar(imagem_pil: Image.Image, specs: EspecsLitho) -> tuple[bytes, bytes]:
        """Devolve (glb_bytes, stl_bytes). NÃO toca em ORM/HTTP/FileField."""
```

NOTA: o motor NÃO importa Django nem trimesh nas camadas superiores — é boundary
swappable como apps/orders/payments.py. Recebe PIL.Image em memória, devolve bytes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar deps e criar o pacote do app com a estrutura de testes</name>
  <files>requirements.txt, apps/lithophane/__init__.py, apps/lithophane/tests/__init__.py, apps/lithophane/tests/test_generation.py</files>
  <read_first>
    - requirements.txt (ver o estilo de pinagem `==` e os comentários de seção em pt-br)
    - apps/orders/payments.py (padrão de boundary isolado a espelhar)
    - .planning/phases/04-.../04-RESEARCH.md (seção "Installation" e "Standard Stack" — versões exatas)
  </read_first>
  <action>
    1. Em `requirements.txt`, abaixo da seção `# --- Imagens / arquivos ---` (que já tem `Pillow==11.2.1`), adicionar uma nova seção:
       ```
       # --- Geração 3D do lithophane ---
       numpy==2.2.6
       trimesh==4.12.2
       ```
       Usar EXATAMENTE essas versões (verificadas no RESEARCH em 2026-06-07).
    2. Criar `apps/lithophane/__init__.py` vazio.
    3. Criar `apps/lithophane/tests/__init__.py` vazio.
    4. Criar `apps/lithophane/tests/test_generation.py` com testes que escrevem a especificação ANTES da implementação (RED). Usar `django.test.SimpleTestCase` (sem banco). Gerar a imagem de teste em memória com Pillow — um gradiente de alta variância (preto→branco) para garantir relevo não trivial:
       ```python
       import numpy as np
       from PIL import Image
       from django.test import SimpleTestCase

       def _foto_teste(w=240, h=180):
           # gradiente diagonal de alta variância (0..255)
           xx, yy = np.meshgrid(np.linspace(0, 255, w), np.linspace(0, 255, h))
           arr = ((xx + yy) / 2).astype("uint8")
           return Image.fromarray(arr, mode="L").convert("RGB")
       ```
    5. Escrever estes testes (todos devem FALHAR primeiro — generation.py ainda não existe):
       - `test_gerar_devolve_dois_bytes`: `glb, stl = LithophaneGenerator.gerar(foto, specs)`; assert `isinstance(glb, bytes)` e `len(glb) > 0`; idem stl.
       - `test_glb_carrega_e_eh_watertight`: recarregar o GLB com `trimesh.load(io.BytesIO(glb), file_type="glb")`; pegar a geometria (Scene → `mesh = list(scene.geometry.values())[0]`); `assert mesh.is_watertight`.
       - `test_stl_carrega_e_eh_watertight`: `trimesh.load(io.BytesIO(stl), file_type="stl")`; assert é Trimesh com faces > 0 e `is_watertight`.
       - `test_relevo_invertido`: gerar com foto onde metade esquerda é preta (0) e direita branca (255); recarregar STL; assert que a altura máxima (Z) na região escura > altura na região clara (espessura_max nos pixels escuros). Verificar via `mesh.vertices[:,2].max() >= specs.espessura_max_mm - 0.01` e `>= ... min`.
       - `test_glb_tem_textura_emissiva`: recarregar o GLB, pegar o material `mesh.visual.material`; assert que `getattr(material, "emissiveTexture", None) is not None` OU que o JSON do GLB contém a string `emissiveTexture` (ler `glb` bytes e `assert b"emissive" in glb` como fallback robusto).
       - `test_glb_abaixo_de_5mb`: `assert len(glb) < 5 * 1024 * 1024`.
       - Specs padrão usadas: `EspecsLitho(largura_mm=100.0, espessura_min_mm=0.8, espessura_max_mm=3.0, resolucao_px=200, formato="placa")`.
    6. Rodar a suíte e CONFIRMAR que falha por ImportError/NotImplemented (estado RED). Commit: `test(04-01): testes de bancada do motor de geração lithophane (RED)`.
  </action>
  <verify>
    <automated>python manage.py test apps.lithophane.tests.test_generation --settings=config.settings.prod 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "numpy==2.2.6" requirements.txt` retorna 1
    - `grep -c "trimesh==4.12.2" requirements.txt` retorna 1
    - `grep -c "is_watertight" apps/lithophane/tests/test_generation.py` retorna ≥ 2
    - `grep -c "def test_" apps/lithophane/tests/test_generation.py` retorna ≥ 6
    - A execução dos testes nesta task FALHA (RED) — generation.py ainda não existe
  </acceptance_criteria>
  <done>requirements.txt tem numpy+trimesh; suíte de 6+ testes existe e está em RED (falha por ausência de generation.py).</done>
</task>

<task type="auto">
  <name>Task 2: Implementar generation.py até a suíte passar (GREEN)</name>
  <files>apps/lithophane/generation.py</files>
  <read_first>
    - apps/lithophane/tests/test_generation.py (o contrato que precisa passar)
    - apps/orders/payments.py (docstring pt-br + padrão de classe boundary)
    - .planning/phases/04-.../04-RESEARCH.md (Pattern 1, 2, 3 — código de referência; Pitfalls 1, 2, 3, 7)
  </read_first>
  <action>
    Criar `apps/lithophane/generation.py` com docstring pt-br no topo ("Motor de geração de lithophane — isolado e swappável. Segue o padrão de apps/orders/payments.py..."). Implementar seguindo RESEARCH Pattern 1-3 LITERALMENTE:

    1. Imports: `from __future__ import annotations`, `io`, `dataclass`, `Literal`, `numpy as np`, `trimesh`, `trimesh.visual.material`, `from PIL import Image`.
    2. `FormatoLitho = Literal["placa", "luminaria"]` e o dataclass `EspecsLitho` EXATAMENTE como no bloco <interfaces>.
    3. `class LithophaneGenerator` com `@staticmethod gerar(imagem_pil, specs) -> tuple[bytes, bytes]` que orquestra:
       `heightmap = _imagem_para_heightmap(...)` → `mesh = _heightmap_para_mesh(...)` → `glb = _exportar_glb(mesh, imagem_pil)` → `stl = mesh.export(file_type="stl")` → `return glb, stl`.
    4. `_imagem_para_heightmap`: converter `img.convert("L")`, redimensionar mantendo proporção com o maior lado = `specs.resolucao_px` via `Image.LANCZOS`, normalizar `/255.0`, INVERTER (`arr = 1.0 - arr`, claro=fino/escuro=grosso — Pitfall: relevo invertido), mapear para faixa `arr * (espessura_max - espessura_min) + espessura_min`. Retorna `np.ndarray (H,W) float32`.
    5. `_heightmap_para_mesh`: montar malha WATERTIGHT vectorizada com numpy (RESEARCH Pattern 2): face frontal (grade de vértices com altura via `np.meshgrid`), base plana (z=0, faces invertidas), e 4 paredes laterais conectando bordas frontais à base. UVs normalizados (Y invertido OpenGL). Construir `trimesh.Trimesh(vertices, faces, visual=TextureVisuals(uv, material), process=False)` (Pitfall 1: `process=False` obrigatório). Após montar, se `not mesh.is_watertight`: chamar `trimesh.repair.fix_normals(mesh)` e `trimesh.repair.fill_holes(mesh)`; se ainda falhar, fundir vértices com `mesh.merge_vertices()` e revalidar. O teste `test_*_watertight` é o juiz.
    6. `_exportar_glb`: construir `PBRMaterial(baseColorFactor=[1,1,1,1], metallicFactor=0.0, roughnessFactor=1.0, doubleSided=False, emissiveTexture=imagem_pil.convert("RGB").resize((512,512) se maior), emissiveFactor=[1,1,1])` — `emissiveTexture` DEVE ser PIL.Image (Pitfall 2). Reduzir a textura para ≤512×512 antes de embutir (Pitfall 3, controla tamanho do GLB). Aplicar como `mesh.visual = TextureVisuals(uv=mesh.visual.uv, material=material)`, criar `Scene`, `scene.add_geometry(mesh)`, retornar `scene.export(file_type="glb")`.
    7. NÃO importar Django, nem tocar em FileField/ORM/HTTP — só PIL/numpy/trimesh.

    Iterar até TODOS os testes passarem (GREEN). Se `KHR_materials_emissive_strength` não for emitido pelo trimesh, deixar `emissiveFactor=[1,1,1]` e seguir (o controle de strength fica 100% no JS — Plan 04, Pitfall 7). Commit: `feat(04-01): motor generation.py — heightmap→malha watertight + GLB emissivo + STL (GREEN)`.
  </action>
  <verify>
    <automated>python manage.py test apps.lithophane.tests.test_generation --settings=config.settings.prod 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Os testes da suíte passam: saída contém `OK` e `grep -c "FAILED\|ERROR" ` na saída = 0
    - `grep -c "process=False" apps/lithophane/generation.py` retorna 1
    - `grep -c "emissiveTexture" apps/lithophane/generation.py` retorna ≥ 1
    - `grep -c "is_watertight" apps/lithophane/generation.py` retorna ≥ 1
    - `grep -c "class LithophaneGenerator" apps/lithophane/generation.py` retorna 1
    - `grep -c "def gerar" apps/lithophane/generation.py` retorna 1
    - `grep -c "import trimesh" apps/lithophane/generation.py` retorna ≥ 1 e `grep -c "import django\|from django" apps/lithophane/generation.py` retorna 0 (motor não conhece Django)
  </acceptance_criteria>
  <done>Suíte verde: GLB watertight com textura emissiva < 5MB, STL watertight, relevo invertido confirmado. Motor isolado sem dependência de Django.</done>
</task>

</tasks>

<verification>
- `python manage.py test apps.lithophane.tests.test_generation --settings=config.settings.prod` passa (OK)
- `apps/lithophane/generation.py` existe, isolado (sem import de Django), espelhando `apps/orders/payments.py`
- requirements.txt tem `numpy==2.2.6` e `trimesh==4.12.2`
- Open Questions 1 e 2 do RESEARCH resolvidas: watertight confirmado por teste; comportamento da emissiveTexture no GLB confirmado (embutida ou string presente)
</verification>

<success_criteria>
O motor de geração devolve `(glb_bytes, stl_bytes)` a partir de `(PIL.Image, EspecsLitho)`, com malha watertight, relevo invertido, GLB < 5MB com a foto como emissiveTexture, validado por suíte de testes verde. Pronto para ser consumido pelo `LithophaneService` (Plan 02) sem retrabalho.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-01-SUMMARY.md`
</output>
