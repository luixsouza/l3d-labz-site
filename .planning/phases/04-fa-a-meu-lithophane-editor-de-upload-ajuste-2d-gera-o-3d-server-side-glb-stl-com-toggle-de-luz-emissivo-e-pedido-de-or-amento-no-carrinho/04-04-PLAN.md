---
phase: 04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - apps/lithophane/views.py
  - apps/lithophane/urls.py
  - config/urls.py
  - apps/core/templates/core/partials/navbar.html
  - apps/lithophane/templates/lithophane/editor.html
  - static/css/theme.css
  - static/js/lithophane-editor.js
autonomous: true
requirements: [LITHO-UI]
must_haves:
  truths:
    - "O cliente acessa /lithophane/, sobe uma foto e a ajusta ao vivo no canvas (escala/brilho/inverter)"
    - "Ao clicar 'Gerar 3D', o navegador envia a foto ajustada (dataURL) + specs via fetch POST com CSRF e recebe glb_url/stl_url/draft_id"
    - "Apos gerar, o canvas e trocado pelo <model-viewer> (CDN @4.3.1) exibindo o lithophane rotacionavel"
    - "O toggle 'Com luz / Sem luz' acende a foto via setEmissiveFactor + setEmissiveStrength + exposure"
    - "Existem botoes 'Encomendar' (adiciona ao carrinho como orcamento) e 'Baixar STL'; o navbar tem o link 'Faca meu Lithophane'; o slogan aparece; tema claro/escuro e copy pt-br"
  artifacts:
    - path: "apps/lithophane/views.py"
      provides: "editor view (GET /lithophane/) + gerar endpoint (POST /lithophane/gerar/) decodifica dataURL -> PIL -> LithophaneService.gerar -> JSON"
      contains: "LithophaneService"
    - path: "apps/lithophane/templates/lithophane/editor.html"
      provides: "Editor: rail de ferramentas, canvas central, painel direito (formato/tamanho/espessura), area do viewer"
      contains: "Onde memorias preciosas ganham forma na luz"
    - path: "static/js/lithophane-editor.js"
      provides: "IIFE: upload->canvas, filtros, fetch gerar, swap p/ model-viewer, toggle emissivo"
      contains: "setEmissiveFactor"
  key_links:
    - from: "static/js/lithophane-editor.js"
      to: "POST /lithophane/gerar/"
      via: "fetch com X-CSRFToken do cookie csrftoken + FormData (dataURL + specs)"
      pattern: "X-CSRFToken"
    - from: "apps/lithophane/views.py (gerar)"
      to: "apps/lithophane.services.LithophaneService.gerar"
      via: "decode base64 dataURL -> PIL.Image -> service -> JsonResponse(LithophaneMapper.to_dict)"
      pattern: "LithophaneService\\.gerar"
    - from: "static/js/lithophane-editor.js"
      to: "model-viewer material[0]"
      via: "setEmissiveFactor([1,0.9,0.7]) + setEmissiveStrength(4) + exposure"
      pattern: "setEmissiveStrength"
    - from: "config/urls.py"
      to: "apps.lithophane.urls"
      via: "path('lithophane/', include('apps.lithophane.urls'))"
      pattern: "apps.lithophane.urls"
---

<objective>
Construir a camada de apresentacao completa da feature: o editor front-end (canvas 2D vanilla - escala/brilho/inverter), o endpoint de geracao, a troca para o `<model-viewer>` com o toggle de luz emissivo, os botoes "Encomendar"/"Baixar STL", a rota `/lithophane/`, o link no navbar, o slogan e o estilo com tokens Luigi + tema claro/escuro. Tudo em pt-br, sem framework JS pesado (RESEARCH Patterns 4 e 5).

Purpose: Entregar o valor visivel ao cliente - a experiencia "Faca meu Lithophane" do upload ao pedido de orcamento. Consome `LithophaneService.gerar` (Plan 02) no endpoint e `cart:add_litho` (Plan 03) no botao "Encomendar".
Output: views/urls do app, rota montada, navbar atualizado, template do editor, CSS e o JS do editor+viewer; `manage.py check` limpo e a pagina `/lithophane/` renderiza.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-CONTEXT.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-RESEARCH.md
@apps/catalog/templates/catalog/product_detail.html
@apps/core/templates/base.html
@apps/core/templates/core/partials/navbar.html
@config/urls.py

<interfaces>
<!-- CONTRATO consumido (Planos 02 e 03, ja implementados) -->

apps/lithophane/services.py:
```python
class LithophaneService(BaseService):
    @staticmethod
    def gerar(imagem_pil, *, formato, largura_mm, espessura_mm, user=None, session_key="") -> LithophaneDraft: ...
```
apps/lithophane/mappers.py:
```python
class LithophaneMapper:
    @classmethod
    def to_dict(cls, instance) -> dict:  # {"draft_id","format","format_label","size","thickness","image_url","glb_url","stl_url"}
```
Carrinho (Plan 03): rota `cart:add_litho` (POST /carrinho/lithophane/adicionar/<draft_id>/).

<!-- Padrao model-viewer a REPLICAR (apps/catalog/templates/catalog/product_detail.html) -->
```html
<model-viewer src="{{ url }}" poster="{{ img }}" camera-controls auto-rotate
              touch-action="pan-y" shadow-intensity="1" loading="lazy"></model-viewer>
<!-- script gated em extra_js: -->
<script type="module" src="https://cdn.jsdelivr.net/npm/@google/model-viewer@4.3.1/dist/model-viewer.min.js"></script>
```

<!-- model-viewer JS API (RESEARCH Pattern 4, api.ts verificado) -->
const mat = viewer.model.materials[0];
mat.setEmissiveFactor([1.0, 0.9, 0.7]); mat.setEmissiveStrength(4.0); viewer.setAttribute('exposure','0.4');
mat.setEmissiveFactor([0,0,0]);         mat.setEmissiveStrength(0);   viewer.setAttribute('exposure','1.0');

<!-- CSRF (RESEARCH Pattern 5): const csrf = document.cookie.match(/csrftoken=([^;]+)/)?.[1] ?? ''; -->
<!-- base.html: blocos {% block extra_head %}, {% block content %}, {% block extra_js %}; tema via data-theme + tokens var(--accent)/var(--bg)/... -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: views (editor + endpoint gerar) + urls do app + rota em config/urls + link no navbar</name>
  <files>apps/lithophane/views.py, apps/lithophane/urls.py, config/urls.py, apps/core/templates/core/partials/navbar.html</files>
  <read_first>
    - apps/catalog/views.py (padrao de view fina request->service->render)
    - apps/cart/views.py (padrao @require_POST + JsonResponse + _is_fetch)
    - apps/lithophane/services.py (LithophaneService.gerar - assinatura)
    - apps/lithophane/mappers.py (LithophaneMapper.to_dict - formato do JSON de resposta)
    - apps/catalog/urls.py (app_name + path)
    - config/urls.py (lista de includes; bloco DEBUG do media serving linhas 18-19 - ja serve MEDIA em dev)
    - apps/core/templates/core/partials/navbar.html (bloco main-nav linhas 10-16; padrao de link active por view_name com a var vn)
  </read_first>
  <action>
    1. `apps/lithophane/views.py` (docstring pt-br "Views do lithophane - finas. Editor (GET) + endpoint de geracao (POST)."):
       ```python
       from __future__ import annotations

       import base64
       import binascii
       import io

       from django.http import JsonResponse
       from django.shortcuts import render
       from django.views.decorators.http import require_POST
       from PIL import Image, UnidentifiedImageError

       from .mappers import LithophaneMapper
       from .services import LithophaneService


       def editor(request):
           return render(request, "lithophane/editor.html", {})


       @require_POST
       def gerar(request):
           data_url = request.POST.get("imagem", "")
           formato = request.POST.get("formato", "placa")
           try:
               largura_mm = float(request.POST.get("largura_mm", 100))
               espessura_mm = float(request.POST.get("espessura_max_mm", 3.0))
           except (TypeError, ValueError):
               return JsonResponse({"ok": False, "erro": "Specs invalidas."}, status=400)

           # dataURL "data:image/png;base64,...." -> PIL.Image
           if "," in data_url:
               data_url = data_url.split(",", 1)[1]
           try:
               raw = base64.b64decode(data_url)
               imagem = Image.open(io.BytesIO(raw))
               imagem.load()
           except (binascii.Error, UnidentifiedImageError, OSError, ValueError):
               return JsonResponse({"ok": False, "erro": "Imagem invalida."}, status=400)

           if not request.session.session_key:
               request.session.save()
           draft = LithophaneService.gerar(
               imagem,
               formato=formato,
               largura_mm=largura_mm,
               espessura_mm=espessura_mm,
               user=request.user,
               session_key=request.session.session_key or "",
           )
           return JsonResponse({"ok": True, **LithophaneMapper.to_dict(draft)})
       ```
       NOTA: a view e fina - toda a geracao/persistencia vive no service. O `session_key` garante dono do rascunho anonimo (consistente com o carrinho por sessao).

    2. `apps/lithophane/urls.py`:
       ```python
       from django.urls import path

       from . import views

       app_name = "lithophane"

       urlpatterns = [
           path("", views.editor, name="editor"),
           path("gerar/", views.gerar, name="gerar"),
       ]
       ```

    3. `config/urls.py` - adicionar a lista `urlpatterns` (apos a linha do `pedidos/`):
       `path("lithophane/", include("apps.lithophane.urls")),`
       (O `include` ja esta importado no topo. O bloco DEBUG de media serving ja existe - arquivos GLB/STL em MEDIA serao servidos em dev; prod e concern existente fora de escopo.)

    4. `apps/core/templates/core/partials/navbar.html` - no `<nav class="main-nav">` (apos o link "Modelos 3D", linha 13), adicionar:
       ```html
       <a href="{% url 'lithophane:editor' %}" class="{% if vn == 'lithophane:editor' %}active{% endif %}">Faca meu Lithophane</a>
       ```
       (usa a var `vn` ja definida no `{% with vn=request.resolver_match.view_name %}`. Texto com cedilha correta: "Faca" deve ser escrito "Faca" -> usar a forma com cedilha "Fa" + c-cedilha + "a meu Lithophane".)

    5. Validar: `python manage.py check --settings=config.settings.prod` e resolver as URLs.
  </action>
  <verify>
    <automated>python manage.py check --settings=config.settings.prod 2>&1 | tail -5 && python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.prod'); django.setup(); from django.urls import reverse; print(reverse('lithophane:editor'), reverse('lithophane:gerar'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def editor" apps/lithophane/views.py` retorna 1 e `grep -c "def gerar" apps/lithophane/views.py` retorna 1
    - `grep -c "LithophaneService.gerar" apps/lithophane/views.py` retorna 1
    - `grep -c "b64decode" apps/lithophane/views.py` retorna 1 e `grep -c "require_POST" apps/lithophane/views.py` retorna ≥ 1
    - `grep -c "app_name = \"lithophane\"" apps/lithophane/urls.py` retorna 1 e `grep -E "editor|gerar" apps/lithophane/urls.py` casa as 2 rotas
    - `grep -c "apps.lithophane.urls" config/urls.py` retorna 1
    - `grep -c "lithophane:editor" apps/core/templates/core/partials/navbar.html` retorna 1
    - O comando de reverse imprime `/lithophane/ /lithophane/gerar/`
    - `python manage.py check --settings=config.settings.prod` sem issues
  </acceptance_criteria>
  <done>Rota /lithophane/ e /lithophane/gerar/ montadas; view editor renderiza e endpoint gerar decodifica dataURL->PIL->LithophaneService.gerar->JSON; link "Faca meu Lithophane" no navbar.</done>
</task>

<task type="auto">
  <name>Task 2: Template do editor (rail + canvas + painel + area do viewer) + slogan + CSS com tokens Luigi</name>
  <files>apps/lithophane/templates/lithophane/editor.html, static/css/theme.css</files>
  <read_first>
    - apps/catalog/templates/catalog/product_detail.html (padrao model-viewer + bloco extra_js gated + botao "Baixar STL" linhas 68-72; estrutura section>container)
    - apps/core/templates/base.html (blocos extra_head/content/extra_js; load static)
    - static/css/theme.css (tokens de design: --bg, --bg-card, --bg-elevated, --border, --accent, --accent-2, --text, --text-muted, --radius, --shadow, --container, --ease; classes .btn .btn-primary .btn-ghost .card .section .container .chip)
    - .planning/phases/04-.../04-CONTEXT.md (referencia visual image.png: rail de ferramentas a esquerda, canvas central com moldura de crop, painel direito forma/tamanho; slogan obrigatorio)
  </read_first>
  <action>
    Criar `apps/lithophane/templates/lithophane/editor.html` estendendo `base.html`. Estrutura (inspirada em image.png - rail esquerda / canvas centro / painel direita), copy pt-br, sem hardcode de cores (usar tokens via classes/variaveis):

    1. `{% extends "base.html" %}` + `{% load static %}` + `{% block title %}Faca meu Lithophane - {{ SITE.name }}{% endblock %}`.

    2. No `{% block content %}`:
       - Cabecalho com o slogan OBRIGATORIO exatamente: "Onde memorias preciosas ganham forma na luz" (com acentuacao correta: "memorias" -> com acento, conforme CONTEXT). Eyebrow "Faca meu Lithophane" + `<h1>` + `<p class="text-muted">` com o slogan.
       - Container do editor com 3 colunas (grid CSS - classe `.litho-editor`):
         a) RAIL ESQUERDO (`.litho-rail`): botoes de ferramenta (input file "Enviar foto" estilizado como botao; botao "Inverter"). Cada um com icone do sprite (#i-... ja existentes; usar #i-box, #i-layers etc.).
         b) CANVAS CENTRAL (`.litho-stage`): um `<canvas id="litho-canvas" width="512" height="512">` com a moldura de crop, e um wrapper `#litho-viewer-wrap` (display:none inicialmente) que recebera o `<model-viewer id="litho-viewer">` apos gerar. Inclui o botao flutuante de luz `<button id="btn-luz" class="litho-luz">` (icone lampada - usar #i-spark ou similar do sprite) e, abaixo, os botoes "Encomendar" (form POST para `{% url 'cart:add_litho' 0 %}` com o id substituido por JS, OU um `<button id="btn-encomendar">` que faz fetch) e "Baixar STL" (`<a id="btn-stl" download>`).
            - Para "Encomendar": usar um `<form id="litho-order-form" method="post" action="">{% csrf_token %}<button>Encomendar</button></form>` cujo `action` o JS preenche com `/carrinho/lithophane/adicionar/<draft_id>/` apos gerar (reverse hardcoded do padrao da rota cart:add_litho).
         c) PAINEL DIREITO (`.litho-panel`): 
            - Presets de formato (radios): placa, luminaria (habilitados) + curvo, cubo (com atributo `disabled` e badge "em breve" - deferidos, nascem na UI). Default: placa.
            - Slider tamanho: `<input type="range" id="litho-size" min="60" max="200" value="100">` com label "Tamanho (mm)".
            - Slider espessura: `<input type="range" id="litho-thickness" min="2" max="5" step="0.5" value="3">` label "Espessura max (mm)".
            - Slider brilho: `<input type="range" id="litho-brightness" min="50" max="150" value="100">` label "Brilho".
            - Botao primario `<button id="btn-gerar" class="btn btn-primary btn-block">Gerar 3D</button>`.
       - Cada controle com `id` exato citado acima (o JS da Task 3 depende deles).

    3. No `{% block extra_js %}`: carregar o model-viewer (CDN @4.3.1, type=module, MESMO de product_detail.html) e depois `<script src="{% static 'js/lithophane-editor.js' %}" defer></script>`.

    4. `static/css/theme.css` - APENDAR ao final um bloco comentado `/* ---- Faca meu Lithophane ---- */` com layout responsivo usando SO tokens existentes:
       - `.litho-editor { display:grid; grid-template-columns: auto 1fr 280px; gap:1.2rem; }` e em mobile (`@media (max-width:860px)`) vira 1 coluna.
       - `.litho-rail` coluna de botoes; `.litho-stage` centraliza o canvas (`background:var(--bg-elevated); border:1px solid var(--border); border-radius:var(--radius);`); `#litho-canvas` `max-width:100%`.
       - `.litho-panel` (`background:var(--bg-card)`), `.litho-luz` botao flutuante (position absolute sobre o stage) com `background:var(--accent)` quando `.luz-ativa`.
       - `#litho-viewer-wrap` e `#litho-viewer` ocupam o stage (min-height ~420px). NAO introduzir cores literais - so `var(--...)`.
  </action>
  <verify>
    <automated>python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.prod'); django.setup(); from django.template.loader import get_template; get_template('lithophane/editor.html'); print('TEMPLATE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Onde mem" apps/lithophane/templates/lithophane/editor.html` retorna ≥ 1 (slogan presente)
    - `grep -c "litho-canvas" apps/lithophane/templates/lithophane/editor.html` retorna ≥ 1
    - `grep -c "litho-viewer" apps/lithophane/templates/lithophane/editor.html` retorna ≥ 1
    - `grep -E "btn-gerar|btn-luz|btn-stl|btn-encomendar|litho-order-form" apps/lithophane/templates/lithophane/editor.html` casa os controles
    - `grep -c "litho-size\|litho-thickness\|litho-brightness" apps/lithophane/templates/lithophane/editor.html` retorna ≥ 1
    - `grep -c "model-viewer@4.3.1" apps/lithophane/templates/lithophane/editor.html` retorna 1
    - `grep -c "lithophane-editor.js" apps/lithophane/templates/lithophane/editor.html` retorna 1
    - `grep -c "Faca meu Lithophane\|Litho Editor\|litho-editor" static/css/theme.css` retorna ≥ 1 (bloco CSS apendado)
    - `grep -c "#3b82f6\|#22c55e" apps/lithophane/templates/lithophane/editor.html` retorna 0 (sem cores literais no template)
    - Template carrega sem erro (`get_template` nao levanta TemplateSyntaxError)
  </acceptance_criteria>
  <done>Editor renderiza com rail/canvas/painel + slogan obrigatorio + presets de formato (4, com curvo/cubo desabilitados) + sliders + botoes Gerar/Encomendar/Baixar STL + area do viewer; CSS com tokens Luigi e responsivo; model-viewer e o JS carregados no extra_js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: JS do editor (upload->canvas, filtros, fetch gerar, swap p/ model-viewer, toggle emissivo, encomendar/STL)</name>
  <files>static/js/lithophane-editor.js</files>
  <read_first>
    - static/js/app.js (padrao IIFE 'use strict', leitura de DOM, sem framework)
    - apps/lithophane/templates/lithophane/editor.html (os ids dos controles definidos na Task 2 - btn-gerar, btn-luz, btn-stl, litho-canvas, litho-viewer-wrap, litho-size, litho-thickness, litho-brightness, litho-order-form, presets de formato)
    - .planning/phases/04-.../04-RESEARCH.md (Pattern 4 toggle emissivo VERBATIM; Pattern 5 editor canvas + fetch CSRF VERBATIM)
  </read_first>
  <action>
    Criar `static/js/lithophane-editor.js` como IIFE (`(function(){ 'use strict'; ... })();`), sem framework, copy/comentarios pt-br. Implementar (RESEARCH Patterns 4 e 5 LITERALMENTE):

    1. Referencias de DOM: canvas (`#litho-canvas`) + ctx 2d, input file (rail), sliders (`#litho-size`, `#litho-thickness`, `#litho-brightness`), botao inverter, `#btn-gerar`, `#btn-luz`, `#btn-stl`, `#litho-order-form`, `#litho-viewer-wrap`, radios de formato. Guard: se o canvas nao existir, sair (script so roda na pagina do editor).

    2. Estado: `let imgOriginal = null; let inverter = false;`.

    3. Upload: ao trocar o input file, ler via `FileReader.readAsDataURL`, criar `new Image()`, no `onload` guardar em `imgOriginal` e desenhar. Funcao `_render()` que aplica filtros (RESEARCH Pattern 5):
       ```js
       function _render() {
         if (!imgOriginal) return;
         const b = document.querySelector('#litho-brightness').value;
         ctx.filter = `brightness(${b}%) grayscale(100%)`;
         ctx.drawImage(imgOriginal, 0, 0, canvas.width, canvas.height);
         ctx.filter = 'none';
         if (inverter) {
           const id = ctx.getImageData(0, 0, canvas.width, canvas.height);
           for (let i = 0; i < id.data.length; i += 4) {
             id.data[i]=255-id.data[i]; id.data[i+1]=255-id.data[i+1]; id.data[i+2]=255-id.data[i+2];
           }
           ctx.putImageData(id, 0, 0);
         }
       }
       ```
       (grayscale aplicado no canvas para preview coerente com o lithophane; a foto colorida original ainda vai pro emissive via servidor.)
       Ligar `input`/`change` dos sliders de brilho e do botao inverter (toggle `inverter` + `_render()`).

    4. Gerar: no `#btn-gerar` click, montar e enviar (RESEARCH Pattern 5, CSRF do cookie):
       ```js
       async function _gerar() {
         if (!imgOriginal) { alert('Envie uma foto primeiro.'); return; }
         const formato = (document.querySelector('input[name="formato"]:checked')||{}).value || 'placa';
         const dataURL = canvas.toDataURL('image/png');
         const csrf = document.cookie.match(/csrftoken=([^;]+)/)?.[1] ?? '';
         const fd = new FormData();
         fd.append('imagem', dataURL);
         fd.append('formato', formato);
         fd.append('largura_mm', document.querySelector('#litho-size').value);
         fd.append('espessura_max_mm', document.querySelector('#litho-thickness').value);
         btnGerar.disabled = true; btnGerar.textContent = 'Gerando...';
         try {
           const resp = await fetch('/lithophane/gerar/', { method:'POST', headers:{'X-CSRFToken':csrf}, body:fd });
           const data = await resp.json();
           if (!data.ok) throw new Error(data.erro || 'Falha ao gerar.');
           _mostrarViewer(data);
         } catch (e) {
           alert(e.message);
         } finally {
           btnGerar.disabled = false; btnGerar.textContent = 'Gerar 3D';
         }
       }
       ```

    5. `_mostrarViewer(data)`: esconder o canvas (`canvas.style.display='none'`), criar/atualizar o `<model-viewer id="litho-viewer">` dentro de `#litho-viewer-wrap` (mostrar o wrap) com `src=data.glb_url`, `poster=data.image_url`, `camera-controls auto-rotate touch-action="pan-y" exposure="1.0" shadow-intensity="1"`. Preencher o `#btn-stl` href com `data.stl_url` e mostrar; preencher o `action` do `#litho-order-form` com `'/carrinho/lithophane/adicionar/' + data.draft_id + '/'` e mostrar o botao Encomendar; mostrar `#btn-luz`. Guardar `data.draft_id` em estado.

    6. Toggle emissivo (RESEARCH Pattern 4 VERBATIM): no `#btn-luz` click, `const viewer = document.querySelector('#litho-viewer'); if(!viewer.model) return; const mat = viewer.model.materials[0];` e alternar:
       - ligado: `mat.setEmissiveFactor([1.0,0.9,0.7]); mat.setEmissiveStrength(4.0); viewer.setAttribute('exposure','0.4'); viewer.setAttribute('shadow-intensity','0');`
       - desligado: `mat.setEmissiveFactor([0,0,0]); mat.setEmissiveStrength(0); viewer.setAttribute('exposure','1.0'); viewer.setAttribute('shadow-intensity','1');`
       - alternar a classe `.luz-ativa` no `#btn-luz` e atualizar o label (ex: "Com luz" / "Sem luz").

    7. NAO usar libs externas; so DOM + fetch + canvas. Comentarios em pt-br.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('static/js/lithophane-editor.js','utf8');new Function(s);console.log('JS SINTAXE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setEmissiveFactor" static/js/lithophane-editor.js` retorna ≥ 2 (ligar + desligar)
    - `grep -c "setEmissiveStrength" static/js/lithophane-editor.js` retorna ≥ 2
    - `grep -c "X-CSRFToken" static/js/lithophane-editor.js` retorna 1
    - `grep -c "/lithophane/gerar/" static/js/lithophane-editor.js` retorna 1
    - `grep -c "toDataURL" static/js/lithophane-editor.js` retorna ≥ 1
    - `grep -c "getImageData" static/js/lithophane-editor.js` retorna ≥ 1 (inverter)
    - `grep -c "/carrinho/lithophane/adicionar/" static/js/lithophane-editor.js` retorna 1 (encomendar)
    - `grep -c "stl_url\|glb_url" static/js/lithophane-editor.js` retorna ≥ 2
    - `grep -c "import \|require(" static/js/lithophane-editor.js` retorna 0 (sem libs - IIFE puro)
    - O arquivo passa no parse de sintaxe (node `new Function`)
  </acceptance_criteria>
  <done>JS vanilla: upload desenha no canvas com filtros (brilho/grayscale/inverter), Gerar 3D envia dataURL+specs com CSRF, resposta troca o canvas pelo model-viewer, toggle de luz acende a foto via setEmissiveFactor/Strength+exposure, botoes Encomendar (form para cart:add_litho) e Baixar STL funcionam. Sem framework.</done>
</task>

</tasks>

<verification>
- `python manage.py check --settings=config.settings.prod` sem issues
- reverse('lithophane:editor') == '/lithophane/' e reverse('lithophane:gerar') == '/lithophane/gerar/'
- `get_template('lithophane/editor.html')` carrega sem erro de sintaxe
- O JS passa no parse de sintaxe (node) e contem setEmissiveFactor/setEmissiveStrength/X-CSRFToken
- Link "Faca meu Lithophane" presente no navbar; slogan presente no editor; CSS apendado com tokens
</verification>

<success_criteria>
A experiencia completa "Faca meu Lithophane" esta no ar: o cliente acessa /lithophane/ (link no navbar), sobe e ajusta a foto no canvas, gera o 3D (server-side via LithophaneService), ve o lithophane no model-viewer com toggle de luz emissivo, e pode Encomendar (vira item de orcamento no carrinho via Plan 03) ou Baixar STL. Tokens Luigi + tema claro/escuro, copy pt-br, slogan obrigatorio, sem framework JS pesado.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-04-SUMMARY.md`
</output>
