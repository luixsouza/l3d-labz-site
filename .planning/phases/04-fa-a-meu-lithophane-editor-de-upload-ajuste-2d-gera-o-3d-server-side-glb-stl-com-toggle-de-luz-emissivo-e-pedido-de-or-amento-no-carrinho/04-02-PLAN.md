---
phase: 04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/lithophane/apps.py
  - apps/lithophane/models.py
  - apps/lithophane/queries.py
  - apps/lithophane/mappers.py
  - apps/lithophane/services.py
  - apps/lithophane/admin.py
  - apps/lithophane/migrations/__init__.py
  - apps/lithophane/migrations/0001_initial.py
  - config/settings/base.py
autonomous: true
requirements: [LITHO-DATA]
must_haves:
  truths:
    - "Existe um LithophaneDraft persistível com a foto, o GLB e o STL gerados, formato, tamanho, espessura e dono (user/session_key)"
    - "LithophaneService recebe uma PIL.Image ajustada + specs, chama o motor do Plan 01, salva os bytes nos FileFields e devolve o draft persistido — única camada que escreve"
    - "LithophaneQuery lê drafts por id (e por lista de ids, para o checkout)"
    - "LithophaneMapper converte o draft em dict pronto pra template/JSON (glb_url, stl_url, image_url, draft_id)"
    - "A migration 0001 aplica sem erro e o app aparece em INSTALLED_APPS"
  artifacts:
    - path: "apps/lithophane/models.py"
      provides: "LithophaneDraft(TimeStampedModel) com image/model_glb/model_stl/format/size/thickness/user/session_key"
      contains: "class LithophaneDraft"
    - path: "apps/lithophane/services.py"
      provides: "LithophaneService.gerar() — única escrita, chama LithophaneGenerator.gerar e salva via ContentFile"
      contains: "ContentFile"
    - path: "apps/lithophane/queries.py"
      provides: "LithophaneQuery.by_id / drafts_by_ids"
      contains: "class LithophaneQuery"
    - path: "apps/lithophane/mappers.py"
      provides: "LithophaneMapper.to_dict / to_detail"
      contains: "class LithophaneMapper"
    - path: "apps/lithophane/migrations/0001_initial.py"
      provides: "Migration inicial do LithophaneDraft"
      contains: "LithophaneDraft"
  key_links:
    - from: "apps/lithophane/services.py"
      to: "apps/lithophane/generation.py (LithophaneGenerator.gerar)"
      via: "import relativo + EspecsLitho montado a partir das specs do draft"
      pattern: "LithophaneGenerator\\.gerar"
    - from: "apps/lithophane/services.py"
      to: "LithophaneDraft.model_glb / model_stl (FileField)"
      via: "ContentFile(glb_bytes) / ContentFile(stl_bytes) salvos com save=False antes do .save()"
      pattern: "ContentFile"
---

<objective>
Construir a camada Django do app `apps/lithophane/` no padrão de camadas do projeto: o model `LithophaneDraft`, a migration, e as camadas `queries`/`mappers`/`services` que envolvem o motor isolado `generation.py` (Plan 01). O `LithophaneService` é a ponte entre o motor (que só conhece PIL/numpy/trimesh) e o Django (FileField/ORM): recebe a imagem ajustada + specs, chama `LithophaneGenerator.gerar`, e persiste o draft com os arquivos GLB/STL salvos.

Purpose: Dar ao Plan 04 (editor + endpoint) um `LithophaneService.gerar()` pronto para chamar, e ao Plan 03 (carrinho/pedido) um `LithophaneQuery.drafts_by_ids()` e um `LithophaneDraft` para referenciar via `draft_id`. Sem esta camada, o motor não tem como persistir nem ser consumido pelas views.
Output: app `apps/lithophane/` com model+migration+queries+mappers+services+admin, registrado em INSTALLED_APPS, `manage.py check` e `makemigrations --check` limpos.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-CONTEXT.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-RESEARCH.md
@apps/lithophane/generation.py
@apps/catalog/models.py
@apps/catalog/queries.py
@apps/catalog/mappers.py
@apps/core/layers.py

<interfaces>
<!-- CONTRATO consumido (Plan 01, já implementado) — NÃO redefinir, importar de generation.py -->

apps/lithophane/generation.py:
```python
FormatoLitho = Literal["placa", "luminaria"]

@dataclass
class EspecsLitho:
    largura_mm: float
    espessura_min_mm: float
    espessura_max_mm: float
    resolucao_px: int
    formato: FormatoLitho

class LithophaneGenerator:
    @staticmethod
    def gerar(imagem_pil: Image.Image, specs: EspecsLitho) -> tuple[bytes, bytes]:
        """Devolve (glb_bytes, stl_bytes). NÃO toca em ORM/HTTP/FileField."""
```

<!-- CONTRATO produzido (Plan 03 e Plan 04 consomem) — definir EXATAMENTE assim -->

apps/lithophane/services.py:
```python
class LithophaneService(BaseService):
    @staticmethod
    def gerar(imagem_pil, *, formato, largura_mm, espessura_mm, user=None, session_key=None) -> LithophaneDraft:
        """Gera GLB+STL via LithophaneGenerator, persiste e devolve o LithophaneDraft."""
```

apps/lithophane/queries.py:
```python
class LithophaneQuery:
    @staticmethod
    def by_id(draft_id: int) -> LithophaneDraft | None: ...
    @staticmethod
    def drafts_by_ids(ids) -> list[LithophaneDraft]: ...   # consumido por OrderService no Plan 03
```
</interfaces>

<!-- Padrões existentes a espelhar:
  - FileField + upload_to + FileExtensionValidator: apps/catalog/models.py:77-86
  - TimeStampedModel base + Meta + __str__: apps/catalog/models.py:53-113
  - BaseQuery/BaseService/BaseMapper: apps/core/layers.py
  - Mapper Model->dict com .url guard (instance.x.url if instance.x else ""): apps/catalog/mappers.py:63-66
  - AppConfig.name = "apps.<app>": apps/catalog/apps.py
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o pacote do app, o model LithophaneDraft, AppConfig e registrar em INSTALLED_APPS</name>
  <files>apps/lithophane/apps.py, apps/lithophane/models.py, apps/lithophane/migrations/__init__.py, apps/lithophane/migrations/0001_initial.py, config/settings/base.py</files>
  <read_first>
    - apps/catalog/models.py (FileField+upload_to+FileExtensionValidator linhas 71-86; TimeStampedModel + Meta + __str__)
    - apps/catalog/apps.py (padrão AppConfig; NOTA: lithophane NÃO tem signals, então o ready() pode ser omitido)
    - apps/orders/models.py (padrão de TextChoices aninhado dentro do model)
    - config/settings/base.py (bloco LOCAL_APPS linhas 46-54)
    - apps/lithophane/__init__.py (já existe do Plan 01)
  </read_first>
  <action>
    1. Criar `apps/lithophane/apps.py` com docstring pt-br:
       ```python
       from django.apps import AppConfig


       class LithophaneConfig(AppConfig):
           default_auto_field = "django.db.models.BigAutoField"
           name = "apps.lithophane"
           verbose_name = "Lithophane"
       ```
       (sem `ready()` — não há signals.)

    2. Criar `apps/lithophane/models.py` com docstring pt-br no topo ("Modelo do rascunho de lithophane. Guarda a foto original e os arquivos 3D gerados (GLB para o viewer, STL para impressão) + as specs e o dono do rascunho.") e o model:
       ```python
       from __future__ import annotations

       from django.conf import settings
       from django.core.validators import FileExtensionValidator
       from django.db import models

       from apps.core.models import TimeStampedModel


       class LithophaneDraft(TimeStampedModel):
           class Format(models.TextChoices):
               PLACA = "placa", "Placa plana"
               LUMINARIA = "luminaria", "Luminária"
               CURVO = "curvo", "Curvo"     # deferido — enum nasce pronto
               CUBO = "cubo", "Cubo"         # deferido — enum nasce pronto

           # dono do rascunho (cliente logado OU sessão anônima)
           user = models.ForeignKey(
               settings.AUTH_USER_MODEL, on_delete=models.SET_NULL,
               null=True, blank=True, related_name="lithophane_drafts", verbose_name="cliente",
           )
           session_key = models.CharField("sessão", max_length=40, blank=True)

           image = models.ImageField("foto enviada", upload_to="lithophane/fotos/")
           model_glb = models.FileField(
               "modelo GLB", upload_to="lithophane/glb/", blank=True,
               validators=[FileExtensionValidator(allowed_extensions=["glb"])],
           )
           model_stl = models.FileField(
               "arquivo STL", upload_to="lithophane/stl/", blank=True,
               validators=[FileExtensionValidator(allowed_extensions=["stl"])],
           )

           format = models.CharField(
               "formato", max_length=12, choices=Format.choices, default=Format.PLACA
           )
           size = models.DecimalField("tamanho (mm)", max_digits=6, decimal_places=1, default=100.0)
           thickness = models.DecimalField("espessura máx (mm)", max_digits=4, decimal_places=1, default=3.0)

           class Meta:
               verbose_name = "rascunho de lithophane"
               verbose_name_plural = "rascunhos de lithophane"
               ordering = ["-created_at"]

           def __str__(self) -> str:
               return f"Lithophane #{self.pk} ({self.get_format_display()})"
       ```
       NOTA: `image` é obrigatório; `model_glb`/`model_stl` são `blank=True` porque são preenchidos pelo service DEPOIS de criar o registro (salvos via ContentFile). `size`/`thickness` são DecimalField (consistente com o padrão de Decimal do projeto em preços/dimensões).

    3. Criar `apps/lithophane/migrations/__init__.py` vazio.

    4. Registrar o app: em `config/settings/base.py`, adicionar `"apps.lithophane",` ao final da lista `LOCAL_APPS` (depois de `"apps.seller",`).

    5. Gerar a migration: rodar `python manage.py makemigrations lithophane --settings=config.settings.prod`. Isso cria `apps/lithophane/migrations/0001_initial.py`. NÃO escrever a migration à mão — deixar o Django gerar.

    6. Aplicar e validar: `python manage.py migrate lithophane --settings=config.settings.prod` e `python manage.py check --settings=config.settings.prod`.
  </action>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run --settings=config.settings.prod 2>&1 | tail -5 && python manage.py check --settings=config.settings.prod 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apps.lithophane" config/settings/base.py` retorna ≥ 1
    - `grep -c "class LithophaneDraft" apps/lithophane/models.py` retorna 1
    - `grep -c "model_glb" apps/lithophane/models.py` retorna ≥ 1 e `grep -c "model_stl" apps/lithophane/models.py` retorna ≥ 1
    - `grep -c "session_key" apps/lithophane/models.py` retorna ≥ 1
    - `grep -E "PLACA|LUMINARIA|CURVO|CUBO" apps/lithophane/models.py` casa as 4 escolhas de formato
    - O arquivo `apps/lithophane/migrations/0001_initial.py` existe e contém `LithophaneDraft`
    - `python manage.py check --settings=config.settings.prod` termina com "System check identified no issues"
    - `python manage.py makemigrations --check --dry-run --settings=config.settings.prod` reporta "No changes detected" (migration já cobre o model)
  </acceptance_criteria>
  <done>App registrado, LithophaneDraft criado com os 4 formatos + FileFields GLB/STL + dono user/session_key, migration 0001 gerada e aplicada, `check` limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Camadas queries + mappers + services (service envolve o motor e persiste) + admin</name>
  <files>apps/lithophane/queries.py, apps/lithophane/mappers.py, apps/lithophane/services.py, apps/lithophane/admin.py</files>
  <read_first>
    - apps/lithophane/generation.py (assinatura de LithophaneGenerator.gerar e EspecsLitho — o que o service monta e chama)
    - apps/lithophane/models.py (o LithophaneDraft criado na Task 1)
    - apps/catalog/queries.py (padrão *Query com @staticmethod; sem cache aqui — drafts são por usuário, não cacheáveis)
    - apps/catalog/mappers.py (padrão BaseMapper + guard `.url if x else ""` linhas 63-66)
    - apps/orders/services.py (padrão BaseService + @staticmethod; aqui SEM @transaction pois é escrita única simples)
    - apps/catalog/admin.py (padrão admin)
  </read_first>
  <action>
    1. `apps/lithophane/queries.py` (docstring pt-br "Consultas de rascunhos de lithophane — só ORM, sem cache (dados por usuário/sessão)."):
       ```python
       from __future__ import annotations

       from apps.core.layers import BaseQuery

       from .models import LithophaneDraft


       class LithophaneQuery(BaseQuery):
           @staticmethod
           def by_id(draft_id: int) -> LithophaneDraft | None:
               return LithophaneDraft.objects.filter(pk=draft_id).first()

           @staticmethod
           def drafts_by_ids(ids) -> list[LithophaneDraft]:
               """Consumido pelo OrderService no checkout (Plan 03)."""
               return list(LithophaneDraft.objects.filter(pk__in=list(ids)))
       ```

    2. `apps/lithophane/mappers.py` (docstring pt-br):
       ```python
       from __future__ import annotations

       from typing import Any

       from apps.core.layers import BaseMapper

       from .models import LithophaneDraft


       class LithophaneMapper(BaseMapper[LithophaneDraft]):
           @classmethod
           def to_dict(cls, instance: LithophaneDraft) -> dict[str, Any]:
               return {
                   "draft_id": instance.pk,
                   "format": instance.format,
                   "format_label": instance.get_format_display(),
                   "size": float(instance.size),
                   "thickness": float(instance.thickness),
                   "image_url": instance.image.url if instance.image else "",
                   "glb_url": instance.model_glb.url if instance.model_glb else "",
                   "stl_url": instance.model_stl.url if instance.model_stl else "",
               }
       ```
       (Plan 04 usa este dict como JSON de resposta do endpoint `gerar`.)

    3. `apps/lithophane/services.py` — a ÚNICA camada que escreve. Docstring pt-br ("Serviço do lithophane — única camada de escrita. Faz a ponte entre o motor isolado generation.py (PIL/numpy/trimesh) e o Django (FileField/ORM)."):
       ```python
       from __future__ import annotations

       from django.core.files.base import ContentFile

       from apps.core.layers import BaseService

       from .generation import EspecsLitho, LithophaneGenerator
       from .models import LithophaneDraft

       # espessura mínima fixa do lithophane (parte clara/fina) — discricionário
       ESPESSURA_MIN_MM = 0.8
       # resolução do heightmap (maior lado) — equilíbrio qualidade/tamanho do GLB (RESEARCH Pitfall 3)
       RESOLUCAO_PX = 200


       class LithophaneService(BaseService):
           @staticmethod
           def gerar(
               imagem_pil,
               *,
               formato: str,
               largura_mm: float,
               espessura_mm: float,
               user=None,
               session_key: str = "",
           ) -> LithophaneDraft:
               """Gera GLB+STL via LithophaneGenerator, persiste e devolve o draft."""
               # formato do enum do motor: só placa/luminaria são suportados na Fase 1
               formato_motor = formato if formato in ("placa", "luminaria") else "placa"
               specs = EspecsLitho(
                   largura_mm=float(largura_mm),
                   espessura_min_mm=ESPESSURA_MIN_MM,
                   espessura_max_mm=float(espessura_mm),
                   resolucao_px=RESOLUCAO_PX,
                   formato=formato_motor,
               )
               glb_bytes, stl_bytes = LithophaneGenerator.gerar(imagem_pil, specs)

               draft = LithophaneDraft(
                   user=user if (user and user.is_authenticated) else None,
                   session_key=session_key or "",
                   format=formato,
                   size=largura_mm,
                   thickness=espessura_mm,
               )
               # salva a foto original (a imagem ajustada que veio do canvas)
               foto = ContentFile(b"", name="foto.png")
               imagem_pil.convert("RGB").save(foto, format="PNG")
               draft.image.save("foto.png", foto, save=False)
               # salva os arquivos gerados (save=False: um único INSERT no fim)
               draft.model_glb.save("modelo.glb", ContentFile(glb_bytes), save=False)
               draft.model_stl.save("modelo.stl", ContentFile(stl_bytes), save=False)
               draft.save()
               return draft
       ```
       NOTA pt-br no código explicando o `save=False` (escreve os FileFields em memória e persiste tudo num único `draft.save()`).

    4. `apps/lithophane/admin.py` (docstring pt-br): registrar `LithophaneDraft` com `list_display = ("__str__", "format", "size", "thickness", "user", "created_at")`, `list_filter = ("format",)`, `readonly_fields = ("created_at", "updated_at")`. A equipe vê a foto + specs (per CONTEXT: "Admin mostra foto + specs; equipe confirma o valor depois").

    5. Validar: `python manage.py check --settings=config.settings.prod`.
  </action>
  <verify>
    <automated>python manage.py check --settings=config.settings.prod 2>&1 | tail -5 && python -c "import ast; ast.parse(open('apps/lithophane/services.py').read()); ast.parse(open('apps/lithophane/queries.py').read()); ast.parse(open('apps/lithophane/mappers.py').read()); print('SINTAXE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class LithophaneQuery" apps/lithophane/queries.py` retorna 1 e `grep -c "def drafts_by_ids" apps/lithophane/queries.py` retorna 1
    - `grep -c "class LithophaneMapper" apps/lithophane/mappers.py` retorna 1
    - `grep -c "class LithophaneService" apps/lithophane/services.py` retorna 1 e `grep -c "def gerar" apps/lithophane/services.py` retorna 1
    - `grep -c "LithophaneGenerator.gerar" apps/lithophane/services.py` retorna 1
    - `grep -c "ContentFile" apps/lithophane/services.py` retorna ≥ 3 (glb, stl, foto)
    - `grep -c "save=False" apps/lithophane/services.py` retorna ≥ 3
    - `grep -c "LithophaneDraft" apps/lithophane/admin.py` retorna ≥ 1
    - `grep -c "import django\|from django" apps/lithophane/generation.py` retorna 0 (motor permanece isolado — não foi tocado)
    - `python manage.py check --settings=config.settings.prod` termina sem issues
  </acceptance_criteria>
  <done>queries/mappers/services/admin existem e seguem as camadas; LithophaneService.gerar chama o motor do Plan 01 e persiste GLB/STL/foto via ContentFile num único save; LithophaneQuery.drafts_by_ids pronto para o Plan 03; `check` limpo.</done>
</task>

</tasks>

<verification>
- `python manage.py check --settings=config.settings.prod` passa sem issues
- `python manage.py makemigrations --check --dry-run --settings=config.settings.prod` → "No changes detected"
- `apps/lithophane/` tem models, migrations/0001_initial, queries, mappers, services, admin, apps registrado em INSTALLED_APPS
- `generation.py` (Plan 01) permanece intocado e isolado de Django
- O contrato `LithophaneService.gerar(...)` e `LithophaneQuery.drafts_by_ids(...)` estão prontos para os Planos 03 e 04
</verification>

<success_criteria>
A camada Django do lithophane existe e respeita a arquitetura em camadas: model `LithophaneDraft` persistível com migration aplicada, `LithophaneService.gerar` como única escrita (envolve o motor + salva os bytes nos FileFields), `LithophaneQuery`/`LithophaneMapper` para leitura/serialização, app registrado. Pronto para ser consumido pelo carrinho/pedido (Plan 03) e pelo editor/endpoint (Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-02-SUMMARY.md`
</output>
