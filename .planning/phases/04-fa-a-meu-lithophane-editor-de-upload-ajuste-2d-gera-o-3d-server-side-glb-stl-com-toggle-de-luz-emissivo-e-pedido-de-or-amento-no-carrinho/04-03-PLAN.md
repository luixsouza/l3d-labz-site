---
phase: 04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - apps/cart/cart.py
  - apps/cart/services.py
  - apps/cart/views.py
  - apps/cart/urls.py
  - apps/cart/templates/cart/detail.html
  - apps/orders/models.py
  - apps/orders/migrations/0002_orcamento_lithophane.py
  - apps/orders/services.py
  - apps/orders/mappers.py
  - apps/orders/templates/orders/checkout.html
autonomous: true
requirements: [LITHO-ORDER]
must_haves:
  truths:
    - "O carrinho guarda itens lithophane numa chave de sessão separada (cart_litho), sem colidir com os produtos normais"
    - "O cliente vê o item lithophane no carrinho e no checkout marcado como 'A combinar' (sem preço), e pode removê-lo"
    - "O checkout cria um Order com status orcamento quando há item lithophane, e NÃO captura pagamento (pula PaymentService)"
    - "Cada OrderItem lithophane guarda snapshot (draft_id + litho_specs JSON com formato/tamanho/foto)"
    - "Carrinho/checkout toleram item sem preço sem quebrar o total dos produtos normais"
  artifacts:
    - path: "apps/cart/cart.py"
      provides: "SessionCart.add_litho/remove_litho/litho_draft_ids + clear() limpando cart_litho"
      contains: "cart_litho"
    - path: "apps/orders/models.py"
      provides: "Order.Status.ORCAMENTO + OrderItem.draft_id + OrderItem.litho_specs"
      contains: "ORCAMENTO"
    - path: "apps/orders/services.py"
      provides: "create_from_cart com branch de orçamento que pula PaymentService"
      contains: "litho_draft_ids"
    - path: "apps/orders/migrations/0002_orcamento_lithophane.py"
      provides: "Migration dos campos novos do OrderItem + nada de schema p/ Status (TextChoices não migra)"
      contains: "draft_id"
  key_links:
    - from: "apps/orders/services.py (create_from_cart)"
      to: "apps/lithophane.queries.LithophaneQuery.drafts_by_ids"
      via: "import deferido dentro da função (padrão anti-ciclo do projeto) + request.cart.litho_draft_ids"
      pattern: "drafts_by_ids"
    - from: "apps/orders/services.py (create_from_cart)"
      to: "PaymentService.process"
      via: "if not tem_orcamento: PaymentService.process(order)"
      pattern: "if not tem_orcamento"
    - from: "apps/cart/services.py (CartService.build)"
      to: "request.cart.litho_draft_ids + LithophaneQuery"
      via: "monta lista litho_items separada (preço 'A combinar') sem afetar subtotal"
      pattern: "litho_items"
---

<objective>
Estender o carrinho e o pedido para suportar o item lithophane "a combinar" sem quebrar o fluxo de produtos normais. A cirurgia foi mapeada no RESEARCH (Patterns 6 e 7, Pitfalls 4 e 5): chave de sessão separada `cart_litho`, novo `Order.Status.ORCAMENTO`, campos nullable `draft_id`/`litho_specs` no `OrderItem`, branch no `OrderService.create_from_cart` que pula `PaymentService`, e tolerância a item sem preço no carrinho/checkout.

Purpose: Fechar o fluxo "Encomendar" → carrinho → checkout → pedido de orçamento. O Plan 04 (UI) vai chamar `request.cart.add_litho(draft_id)` no botão "Encomendar"; este plano garante que o carrinho e o checkout aceitem esse item e produzam um Order de orçamento sem cobrança.
Output: cart.py/services/views/urls + orders models/migration/services/mappers + templates de carrinho e checkout estendidos; `manage.py check` e `migrate` limpos.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-CONTEXT.md
@.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-RESEARCH.md
@apps/cart/cart.py
@apps/cart/services.py
@apps/orders/models.py
@apps/orders/services.py
@apps/orders/mappers.py

<interfaces>
<!-- CONTRATO consumido (Plan 02, já implementado) -->

apps/lithophane/queries.py:
```python
class LithophaneQuery:
    @staticmethod
    def drafts_by_ids(ids) -> list[LithophaneDraft]: ...
```
LithophaneDraft tem: .pk, .format, .get_format_display(), .size (Decimal), .thickness (Decimal),
.image (ImageField → .url), .model_glb, .model_stl.

<!-- ESTADO ATUAL a estender (NÃO reescrever — adicionar) -->

apps/cart/cart.py — SessionCart já tem: SESSION_KEY="cart", COUPON_KEY="cart_coupon",
add/set_quantity/remove/clear/coupon/raw_items/total_quantity. Adicionar cart_litho ao lado.

apps/orders/models.py — Order.Status tem PENDING/PAID/PROCESSING/SHIPPED/DELIVERED/CANCELLED
(max_length=12, "orcamento"=9 chars cabe). OrderItem tem order/product/product_name/unit_price/
quantity/line_total. unit_price e line_total são DecimalField NOT NULL (Pitfall 5 → usar Decimal("0.00")).

apps/orders/services.py — create_from_cart(request, data) é @transaction.atomic; cria Order,
bulk_create OrderItems de produto, baixa estoque, consome cupom, PaymentService.process, cart.clear().

apps/cart/services.py — CartService.build(request) devolve {"items": lines, "summary": ...};
subtotal/shipping calculados só sobre produtos.
</interfaces>

<!-- Padrões do projeto:
  - import deferido anti-ciclo dentro da função: apps/catalog/services.py:58 (from apps.promotions...)
  - F() para updates atômicos: apps/orders/services.py:67-75
  - mapper guard de status_tone: apps/orders/mappers.py:12-19
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionCart.cart_litho + CartService.build expõe itens "A combinar" + views/urls de add/remove litho + template do carrinho</name>
  <files>apps/cart/cart.py, apps/cart/services.py, apps/cart/views.py, apps/cart/urls.py, apps/cart/templates/cart/detail.html</files>
  <read_first>
    - apps/cart/cart.py (estado atual completo — SESSION_KEY, COUPON_KEY, clear())
    - apps/cart/services.py (CartService.build — onde injetar a lista litho)
    - apps/cart/views.py (padrão @require_POST + _is_fetch + redirect; padrão de view add/remove)
    - apps/cart/urls.py (app_name="cart"; rotas existentes)
    - apps/cart/templates/cart/detail.html (bloco de itens linhas 14-57; bloco resumo linhas 60-97)
    - apps/lithophane/queries.py (LithophaneQuery.drafts_by_ids)
  </read_first>
  <action>
    1. `apps/cart/cart.py` — adicionar (RESEARCH Pattern 6, VERBATIM):
       - constante module-level: `LITHO_KEY = "cart_litho"` (junto de SESSION_KEY/COUPON_KEY).
       - métodos no SessionCart:
         ```python
         def add_litho(self, draft_id: int) -> None:
             """Adiciona (ou ignora se já existe) um item lithophane ao carrinho."""
             drafts = self.session.get(LITHO_KEY, [])
             if draft_id not in drafts:
                 drafts.append(draft_id)
             self.session[LITHO_KEY] = drafts
             self.session.modified = True

         def remove_litho(self, draft_id: int) -> None:
             drafts = [d for d in self.session.get(LITHO_KEY, []) if d != draft_id]
             self.session[LITHO_KEY] = drafts
             self.session.modified = True

         @property
         def litho_draft_ids(self) -> list[int]:
             return list(self.session.get(LITHO_KEY, []))
         ```
       - em `clear()`, adicionar `self.session.pop(LITHO_KEY, None)` antes do `self._save()`.

    2. `apps/cart/services.py` — em `CartService.build`, DEPOIS de montar `lines`/`subtotal` e ANTES do `summary`, montar a lista de itens lithophane separada (import DEFERIDO anti-ciclo dentro da função):
       ```python
       from apps.lithophane.queries import LithophaneQuery  # import local (evita ciclo)
       litho_items = [
           {
               "draft_id": d.pk,
               "name": f"Lithophane {d.get_format_display()} {d.size}mm",
               "format": d.format,
               "image_url": d.image.url if d.image else "",
               "price_display": "A combinar",
           }
           for d in LithophaneQuery.drafts_by_ids(request.cart.litho_draft_ids)
       ]
       ```
       Retornar `{"items": lines, "litho_items": litho_items, "summary": summary}`.
       IMPORTANTE: `subtotal`/`shipping`/`total` continuam calculados SÓ sobre `lines` (produtos) — itens litho NÃO entram no total (são "a combinar"). O `shipping` atual usa `not lines` para decidir frete grátis; manter como está (frete só se aplica a produtos).

    3. `apps/cart/views.py` — adicionar duas views (espelham `add`/`remove`):
       ```python
       @require_POST
       def add_litho(request, draft_id: int):
           request.cart.add_litho(draft_id)
           if _is_fetch(request):
               return JsonResponse({"ok": True})
           messages.success(request, "Lithophane adicionado ao carrinho.")
           return redirect("cart:detail")

       @require_POST
       def remove_litho(request, draft_id: int):
           request.cart.remove_litho(draft_id)
           messages.info(request, "Lithophane removido.")
           return redirect("cart:detail")
       ```

    4. `apps/cart/urls.py` — adicionar rotas:
       ```python
       path("lithophane/adicionar/<int:draft_id>/", views.add_litho, name="add_litho"),
       path("lithophane/remover/<int:draft_id>/", views.remove_litho, name="remove_litho"),
       ```

    5. `apps/cart/templates/cart/detail.html`:
       - O bloco `{% if items %}` atualmente esconde TODO o carrinho quando não há produtos. Trocar a condição de entrada do layout para `{% if items or litho_items %}` (linha 11) para que um carrinho só-lithophane também apareça.
       - Dentro do card de itens (após o `{% for item in items %}...{% endfor %}`, antes do bloco "Continuar comprando" na linha 49), adicionar um loop dos litho:
         ```html
         {% for litho in litho_items %}
           <div class="cart-item">
             <span class="thumb">
               {% if litho.image_url %}<img src="{{ litho.image_url }}" alt="{{ litho.name }}">{% endif %}
             </span>
             <div>
               <span class="product-name">{{ litho.name }}</span>
               <div class="text-dim" style="font-size:.85rem">{{ litho.price_display }}</div>
               <form action="{% url 'cart:remove_litho' litho.draft_id %}" method="post" class="mt-1">
                 {% csrf_token %}
                 <button class="link-danger" type="submit"><svg class="icon"><use href="#i-trash"></use></svg> Remover</button>
               </form>
             </div>
             <div style="text-align:right"><strong>A combinar</strong></div>
           </div>
         {% endfor %}
         ```
       - No resumo (após a linha do Total, ~linha 79), adicionar dentro do card: `{% if litho_items %}<div class="summary-row"><span>Lithophane(s)</span><span>A combinar</span></div>{% endif %}`. O botão "Finalizar compra" continua válido.
  </action>
  <verify>
    <automated>python manage.py check --settings=config.settings.prod 2>&1 | tail -5 && python -c "import ast; ast.parse(open('apps/cart/cart.py').read()); ast.parse(open('apps/cart/services.py').read()); ast.parse(open('apps/cart/views.py').read()); print('SINTAXE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cart_litho" apps/cart/cart.py` retorna ≥ 1
    - `grep -c "def add_litho" apps/cart/cart.py` retorna 1 e `grep -c "def litho_draft_ids" apps/cart/cart.py` retorna 1
    - `grep -c "LITHO_KEY" apps/cart/cart.py` retorna ≥ 3 (constante + add + remove + clear)
    - `grep -c "litho_items" apps/cart/services.py` retorna ≥ 1
    - `grep -c "drafts_by_ids" apps/cart/services.py` retorna 1
    - `grep -c "add_litho\|remove_litho" apps/cart/urls.py` retorna ≥ 2
    - `grep -c "A combinar" apps/cart/templates/cart/detail.html` retorna ≥ 1
    - `grep -c "litho_items" apps/cart/templates/cart/detail.html` retorna ≥ 1
    - `python manage.py check --settings=config.settings.prod` sem issues
  </acceptance_criteria>
  <done>SessionCart guarda drafts em cart_litho; CartService.build expõe litho_items "A combinar" sem afetar o total; views/urls de add/remove litho existem; carrinho mostra o item lithophane e permite remover; carrinho só-lithophane aparece.</done>
</task>

<task type="auto">
  <name>Task 2: Order.Status.ORCAMENTO + campos draft_id/litho_specs no OrderItem + migration + OrderService pula pagamento + checkout/mappers toleram orçamento</name>
  <files>apps/orders/models.py, apps/orders/migrations/0002_orcamento_lithophane.py, apps/orders/services.py, apps/orders/mappers.py, apps/orders/templates/orders/checkout.html</files>
  <read_first>
    - apps/orders/models.py (Order.Status linhas 18-24; OrderItem linhas 94-109; max_length=12 do status)
    - apps/orders/services.py (create_from_cart completo linhas 25-82 — onde inserir o branch)
    - apps/orders/mappers.py (STATUS_TONE linhas 12-19; OrderItemMapper.to_dict; OrderMapper)
    - apps/orders/templates/orders/checkout.html (bloco resumo linhas 80-104 — onde mostrar litho "A combinar")
    - .planning/phases/04-.../04-RESEARCH.md (Pattern 7 — código do branch VERBATIM; Pitfall 5 — Decimal("0.00"))
    - apps/lithophane/queries.py (LithophaneQuery.drafts_by_ids)
  </read_first>
  <action>
    1. `apps/orders/models.py`:
       - Adicionar à `Order.Status` (RESEARCH Pattern 7): `ORCAMENTO = "orcamento", "Orçamento pendente"`. (max_length=12 já comporta "orcamento" — NÃO mexer no max_length.)
       - Em `OrderItem`, adicionar dois campos nullable (retrocompatíveis) após `line_total`:
         ```python
         draft_id = models.PositiveIntegerField("rascunho lithophane", null=True, blank=True)
         litho_specs = models.JSONField("specs do lithophane", null=True, blank=True)
         ```
       - `OrderItem.product` já é `null=True` (linha 96-98) — item litho terá `product=None`. OK.

    2. Gerar a migration: `python manage.py makemigrations orders --name orcamento_lithophane --settings=config.settings.prod`. Confirmar que o arquivo gerado se chama `apps/orders/migrations/0002_orcamento_lithophane.py` (se o número diferir por migrations pré-existentes, renomear referências no frontmatter não é necessário — mas garantir que o `--name orcamento_lithophane` seja usado). NÃO escrever à mão. A migration deve adicionar `draft_id` e `litho_specs` ao OrderItem; o novo valor do TextChoices NÃO gera mudança de schema (só altera `choices`, que o Django registra como `AlterField` no `status` — aceitável).

    3. `apps/orders/services.py` — estender `create_from_cart` (RESEARCH Pattern 7), com import DEFERIDO anti-ciclo:
       - Logo no início, após `summary = cart["summary"]`, NÃO exigir produtos se houver litho. Trocar a guarda `if not items: raise EmptyCartError()` por:
         ```python
         tem_orcamento = bool(request.cart.litho_draft_ids)
         if not items and not tem_orcamento:
             raise EmptyCartError()
         ```
       - No `Order.objects.create(...)`, definir `status=Order.Status.ORCAMENTO if tem_orcamento else Order.Status.PENDING`.
       - Após o `bulk_create` dos itens de produto, adicionar a criação dos itens lithophane (import local):
         ```python
         from decimal import Decimal
         from apps.lithophane.queries import LithophaneQuery  # import local anti-ciclo
         litho_drafts = LithophaneQuery.drafts_by_ids(request.cart.litho_draft_ids)
         OrderItem.objects.bulk_create([
             OrderItem(
                 order=order,
                 product=None,
                 product_name=f"Lithophane {d.get_format_display()} {d.size}mm",
                 unit_price=Decimal("0.00"),   # "a combinar" (Pitfall 5: NOT NULL)
                 quantity=1,
                 line_total=Decimal("0.00"),
                 draft_id=d.pk,
                 litho_specs={
                     "formato": d.format,
                     "largura_mm": float(d.size),
                     "espessura_max_mm": float(d.thickness),
                     "foto_url": d.image.url if d.image else "",
                 },
             )
             for d in litho_drafts
         ])
         ```
       - Envolver o branch de pagamento: `if not tem_orcamento: PaymentService.process(order)` (NÃO capturar pagamento em orçamento).
       - `request.cart.clear()` já limpa `cart_litho` (Task 1) — mantém.

    4. `apps/orders/mappers.py` — adicionar `Order.Status.ORCAMENTO: "warn"` ao dict `STATUS_TONE` (para o badge não cair no default silenciosamente).

    5. `apps/orders/templates/orders/checkout.html` — o checkout recebe `items` e `summary` da view. A view `checkout` (apps/orders/views.py) hoje passa `cart["items"]`; para mostrar os litho no resumo, o executor deve: na view `checkout`, passar também `"litho_items": cart.get("litho_items", [])` no contexto do `render`, e no template, dentro do card de resumo (após o loop `{% for item in items %}`, ~linha 93), adicionar:
       ```html
       {% for litho in litho_items %}
         <div style="display:flex;gap:.7rem;align-items:center">
           <span class="cat-icon" style="width:36px;height:36px;flex:none"><svg class="icon" style="width:18px;height:18px"><use href="#i-box"></use></svg></span>
           <div style="flex:1;min-width:0"><div class="product-name" style="font-size:.85rem">{{ litho.name }}</div><div class="text-dim" style="font-size:.78rem">A combinar</div></div>
         </div>
       {% endfor %}
       ```
       E após a linha do Total (~linha 99): `{% if litho_items %}<div class="summary-row"><span>Lithophane(s)</span><span>A combinar</span></div>{% endif %}`.
       NOTA: a view `apps/orders/views.py` `checkout` precisa do ajuste do contexto (`litho_items`); incluir essa edição nesta task. Também ajustar a guarda `if not cart["items"]:` da view para `if not cart["items"] and not cart.get("litho_items"):` para permitir checkout só-lithophane.

    6. Validar: `python manage.py migrate --settings=config.settings.prod` e `python manage.py check --settings=config.settings.prod`.
  </action>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run --settings=config.settings.prod 2>&1 | tail -5 && python manage.py check --settings=config.settings.prod 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ORCAMENTO" apps/orders/models.py` retorna ≥ 1
    - `grep -c "draft_id" apps/orders/models.py` retorna ≥ 1 e `grep -c "litho_specs" apps/orders/models.py` retorna ≥ 1
    - A migration `apps/orders/migrations/0002_orcamento_lithophane.py` existe e contém `draft_id` e `litho_specs`
    - `grep -c "tem_orcamento" apps/orders/services.py` retorna ≥ 2
    - `grep -c "if not tem_orcamento" apps/orders/services.py` retorna 1 (pagamento gated)
    - `grep -c "litho_draft_ids\|drafts_by_ids" apps/orders/services.py` retorna ≥ 1
    - `grep -c "Decimal(\"0.00\")" apps/orders/services.py` retorna ≥ 2
    - `grep -c "ORCAMENTO" apps/orders/mappers.py` retorna 1
    - `grep -c "litho_items\|A combinar" apps/orders/templates/orders/checkout.html` retorna ≥ 1
    - `python manage.py makemigrations --check --dry-run --settings=config.settings.prod` → "No changes detected"
    - `python manage.py check --settings=config.settings.prod` sem issues
  </acceptance_criteria>
  <done>Order.Status.ORCAMENTO + OrderItem.draft_id/litho_specs com migration aplicada; create_from_cart cria itens litho snapshot (Decimal 0.00 + specs JSON) e pula PaymentService quando há orçamento; checkout aceita carrinho só-lithophane e mostra "A combinar"; badge de status mapeado.</done>
</task>

</tasks>

<verification>
- `python manage.py migrate --settings=config.settings.prod` aplica a 0002 sem erro
- `python manage.py makemigrations --check --dry-run --settings=config.settings.prod` → "No changes detected"
- `python manage.py check --settings=config.settings.prod` sem issues
- Fluxo lógico: add_litho → carrinho mostra "A combinar" → checkout → Order status orcamento, OrderItem com draft_id+litho_specs, sem PaymentService
- Produtos normais continuam intactos (subtotal/frete/cupom/pagamento inalterados quando não há litho)
</verification>

<success_criteria>
O carrinho e o checkout toleram o item lithophane "a combinar": chave de sessão `cart_litho` isolada, item exibido sem preço e removível, checkout gera um Order de status `orcamento` com OrderItem(s) de snapshot (draft_id + litho_specs) e SEM captura de pagamento. O fluxo de produtos normais permanece inalterado. Pronto para o botão "Encomendar" do Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fa-a-meu-lithophane-editor-de-upload-ajuste-2d-gera-o-3d-server-side-glb-stl-com-toggle-de-luz-emissivo-e-pedido-de-or-amento-no-carrinho/04-03-SUMMARY.md`
</output>
