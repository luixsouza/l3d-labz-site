---
phase: 01-rebrand-ui-minimalista
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - static/css/theme.css
  - apps/core/templates/core/home.html
  - apps/catalog/templates/catalog/product_list.html
  - apps/catalog/templates/catalog/product_detail.html
autonomous: false
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "A home tem tratamento minimalista: hero mais enxuto, menos gradientes pesados, mais espaço em branco, mantendo categorias, destaques, lançamentos e newsletter."
    - "O catálogo e o detalhe de produto têm o mesmo respiro/limpeza visual, coerente com a home."
    - "Os destaques de palavra (.grad-text) usam verde sólido/sutil em vez do gradiente azul→roxo animado."
    - "Os botões primários usam o verde da marca."
  artifacts:
    - path: "apps/core/templates/core/home.html"
      provides: "Home minimalista"
      contains: "hero"
    - path: "static/css/theme.css"
      provides: "Ajuste minimalista de .grad-text e respiro de seções"
      contains: ".grad-text"
  key_links:
    - from: "static/css/theme.css"
      to: "apps/core/templates/core/home.html"
      via: "classes minimalistas reutilizadas (.grad-text, .section, .hero-banner)"
      pattern: "grad-text"
---

<objective>
Aplicar o tratamento minimalista (mais whitespace, menos ornamento/gradiente, verde sólido nos realces) na home e nas páginas-chave (catálogo e detalhe), reutilizando os tokens já recoloridos no Plan 01 — sem reescrever o sistema de CSS nem mudar estrutura de páginas.

Purpose: Fecha o critério de minimalismo da Fase 1 (UI-01, UI-02), entregando um visual L3D Labz coeso e enxuto.
Output: home/catálogo/detalhe com respiro maior e ornamentação reduzida; .grad-text em verde; checkpoint humano confirmando a aparência.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-rebrand-ui-minimalista/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Fatos REAIS do theme.css. Plan 01 já trocou os valores de cor; aqui só forma/ornamento. -->

`.grad-text` (theme.css linhas ~80-85) hoje:
  background: linear-gradient(100deg, var(--accent-2), #a78bfa, var(--accent-2));  /* azul→roxo→azul, animado via grad-shift */
Para minimalismo verde: trocar por verde sólido ou gradiente verde sutil e remover a animação roxa.

`.hero-banner .bg::after` (linhas ~506) aplica overlay escuro forte (rgba(10,13,21,...)) — no light fica pesado; aliviável.
Seções usam `.section { padding: clamp(2.5rem, 4vw, 4rem) 0; }` (linha ~182) — pode-se aumentar o respiro vertical.
home.html usa muitos `style="padding-top:0"` inline encadeando seções coladas (linhas 33,45,68,96,117) — afrouxar para dar whitespace.
Existe override v3 (linha ~659): `--radius:0px` e botões outline transparentes (linhas ~673-684). MANTER esse estilo de forma (cantos retos + botão outline já é minimalista); apenas garantir que a cor do outline use o verde (já vem dos tokens do Plan 01).
home.html hero: `.grad-text` na linha 16; hero-cta linhas 18-21 (1 primário + 1 ghost — já enxuto).
product_list.html e product_detail.html herdam as mesmas classes (.section, .product-grid, .detail-media, .search-hero) — refino é via classes/spacing, sem reescrever.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimalismo na home + ajuste de .grad-text para verde</name>
  <files>static/css/theme.css, apps/core/templates/core/home.html</files>
  <read_first>
    - apps/core/templates/core/home.html (todo o arquivo, foco no hero linhas 6-30 e seções)
    - static/css/theme.css (.grad-text linhas ~80-85; .section linha ~182; .hero-banner .bg::after linha ~506)
    - .planning/phases/01-rebrand-ui-minimalista/01-CONTEXT.md (seção "Minimalismo (UI-01...)")
  </read_first>
  <action>
    Implementa UI-01.

    A) static/css/theme.css — `.grad-text` (linhas ~80-85): substituir o gradiente azul→roxo animado por verde sólido da marca, removendo a dependência da animação grad-shift. Resultado:
      .grad-text {
        color: var(--accent);
        -webkit-background-clip: initial; background-clip: initial;
        background: none;
      }
    (Mantém legibilidade no light com o verde #2FA84F sobre fundo escuro do hero-banner; se o hero-banner overlay garantir fundo escuro, o verde fica legível.)

    B) static/css/theme.css — `.section` (linha ~182): aumentar o respiro vertical para um visual mais arejado:
      .section { padding: clamp(3rem, 5vw, 5.5rem) 0; }

    C) home.html — afrouxar o encadeamento de seções coladas: nas seções que hoje têm `style="padding-top:0"` (benefícios linha 33, categorias linha 45, destaques linha 96, lançamentos linha 117), remover o `padding-top:0` OU trocar por `style="padding-top:1rem"` para dar whitespace entre blocos. Manter TODAS as seções (hero, benefícios, categorias, carrossel de promoções, destaques, lançamentos, newsletter) — nada é removido.

    D) home.html — hero: manter 1 CTA primário (verde) + 1 ghost (já está assim, linhas 18-21). Os hero-stats podem ficar (CONTEXT permite, "mais discretos") — sem mudança obrigatória.

    E) Reduzir ornamento pesado: se o overlay do hero-banner (`.hero-banner .bg::after`, theme.css linha ~506) estiver escuro demais para o clima minimalista, suavizar o gradiente para um overlay mais leve (ex.: reduzir as opacidades de `rgba(10,13,21,.97)`/`.86` para `.85`/`.6`). Opcional, só se melhorar o respiro sem prejudicar a legibilidade do texto branco do hero.

    NÃO mudar a estrutura de blocos/rotas; NÃO remover seções essenciais; NÃO reintroduzir gradientes animados.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if(-not(Select-String -Path static/css/theme.css -Pattern 'color:\s*var\(--accent\)' -Quiet)){$ok=$false}; if(-not(Select-String -Path static/css/theme.css -Pattern '\.grad-text' -Quiet)){$ok=$false}; if(-not(Select-String -Path apps/core/templates/core/home.html -Pattern 'newsletter' -Quiet)){$ok=$false}; if($ok){'PASS'}else{'FAIL'}"</automated>
  </verify>
  <acceptance_criteria>
    - static/css/theme.css: o bloco `.grad-text` NÃO contém mais `#a78bfa` (roxo) nem `animation: grad-shift`.
    - static/css/theme.css: `.section` usa `clamp(3rem, 5vw, 5.5rem)` (respiro aumentado).
    - apps/core/templates/core/home.html ainda contém as seções `benefits`, `cat-grid`, `product-grid` (destaques) e `newsletter` (nada removido).
    - apps/core/templates/core/home.html: nenhuma seção essencial foi excluída (grep por `newsletter`, `cat-grid`, `benefits` retorna match).
    - `python manage.py check` roda sem erros.
  </acceptance_criteria>
  <done>A home está mais arejada (mais whitespace entre seções), .grad-text é verde sólido sem animação roxa, todas as seções essenciais preservadas, botões primários em verde (via tokens do Plan 01).</done>
</task>

<task type="auto">
  <name>Task 2: Minimalismo consistente em catálogo e detalhe</name>
  <files>static/css/theme.css, apps/catalog/templates/catalog/product_list.html, apps/catalog/templates/catalog/product_detail.html</files>
  <read_first>
    - apps/catalog/templates/catalog/product_list.html (todo o arquivo)
    - apps/catalog/templates/catalog/product_detail.html (todo o arquivo; foco no .detail-media linhas 12-19)
    - static/css/theme.css (.detail-media linha ~321; .hero-visual/radial-gradients azuis se ainda restarem)
  </read_first>
  <action>
    Implementa UI-02 — mesmo tratamento de cor/spacing da home, sem redesenho funcional.

    A) static/css/theme.css — `.detail-media` (linha ~321) e `.hero-visual` (linha ~169-174) ainda contêm radial-gradients azuis literais `rgba(59,130,246,...)` que NÃO são tokens (foram esquecidos no Plan 01). Trocar essas cores literais por verde: `rgba(59,130,246,.25)` → `rgba(47,168,79,.18)` (detail-media) e `rgba(59,130,246,.32)` → `rgba(47,168,79,.22)` (hero-visual). Procurar QUALQUER `rgba(59, 130, 246` ou `rgba(59,130,246` remanescente no arquivo e trocar pela versão verde `rgba(47,168,79, <mesma opacidade>)`. Também `radial-gradient(...rgba(59,130,246...))` em status-badge.info/alert-info se existirem — trocar p/ verde ou deixar como acento; priorizar os de mídia (.detail-media, .hero-visual).

    B) product_list.html — garantir respiro: a search-hero (linha 33-45) e o filter-bar (linha 48-71) já usam classes existentes; não reescrever. Apenas confirmar visualmente no checkpoint. Se algum espaçamento estiver apertado, ajustar via os tokens/classes existentes (ex.: margem do `.product-grid mt-2`). Sem mudança estrutural.

    C) product_detail.html — confirmar que `.detail-media` usa o monograma/imagem (linhas 13-18) com o novo fundo verde; chips e botão primário herdam o verde dos tokens. Sem mudança estrutural.

    D) Verificação final de cor: rodar uma busca por `#3B82F6`, `#5b8def`, `#2f68d8`, `rgba(91, 141, 239`, `rgba(59, 130, 246` em theme.css — não devem restar como cor de acento/mídia ativa (exceto, se houver, em contextos claramente neutros). Trocar os remanescentes de mídia/acento pelo verde correspondente.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if(Select-String -Path static/css/theme.css -Pattern 'rgba\(59,\s*130,\s*246' -Quiet){$ok=$false}; if(Select-String -Path static/css/theme.css -Pattern '#5b8def','#2f68d8' -Quiet){$ok=$false}; if($ok){'PASS'}else{'FAIL'}"</automated>
  </verify>
  <acceptance_criteria>
    - static/css/theme.css NÃO contém mais nenhum `rgba(59, 130, 246` / `rgba(59,130,246` (azul de mídia em .detail-media/.hero-visual trocado por `rgba(47,168,79,...)`).
    - static/css/theme.css NÃO contém mais `#5b8def` nem `#2f68d8` como cor ativa.
    - static/css/theme.css contém `rgba(47,168,79` (verde nas superfícies de mídia).
    - product_list.html e product_detail.html mantêm a estrutura (grep por `product-grid` e `detail-media` retorna match).
    - `python manage.py check` roda sem erros.
  </acceptance_criteria>
  <done>Catálogo e detalhe ficam visualmente coerentes com a home minimalista; superfícies de mídia (detail-media, hero-visual) usam glow verde em vez de azul; nenhuma cor azul de acento remanescente no CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do minimalismo + identidade L3D Labz</name>
  <files>(nenhum — tarefa de verificação humana, não modifica arquivos)</files>
  <action>Pausar para verificação humana. O executor apenas garante que o dev server está rodando (e seed aplicado se necessário) e apresenta os passos abaixo ao usuário. Não modifica código.</action>
  <what-built>Rebrand completo (Plan 01) + tratamento minimalista da home, catálogo e detalhe (Plan 02): paleta verde Luigi nos dois temas, emblema "L", wordmark L3D Labz, .grad-text verde, mais whitespace, superfícies de mídia em verde.</what-built>
  <how-to-verify>
    1. Rodar `python manage.py runserver` (se houver dados, `python manage.py seed_demo` antes).
    2. Abrir http://127.0.0.1:8000/ — conferir: navbar com emblema verde "L" + "L3D Labz"; hero arejado; .grad-text em verde (não roxo/animado); seções com respiro; nenhum "Nexora" visível; footer L3D Labz.
    3. Abrir o Catálogo (/catalogo/) e um produto (detalhe) — conferir respiro consistente, mídia com fundo verde, botões primários verdes.
    4. Clicar no toggle de tema (sol/lua): alternar claro↔escuro; conferir que o verde fica legível nos dois temas e que a escolha persiste ao recarregar (F5).
    5. Confirmar que nenhuma seção essencial sumiu (categorias, destaques, lançamentos, newsletter).
  </how-to-verify>
  <acceptance_criteria>
    - O usuário confirma "approved" OU descreve ajustes específicos.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "python manage.py check"</automated>
  </verify>
  <done>O usuário confirma "approved" (ou os ajustes pedidos foram aplicados e reverificados).</done>
  <resume-signal>Digite "approved" ou descreva os ajustes necessários.</resume-signal>
</task>

</tasks>

<verification>
- Home, catálogo e detalhe visualmente arejados e coerentes; identidade verde L3D Labz nos dois temas.
- Toggle de tema funciona e persiste (chave `l3d-theme`, garantida no Plan 01).
- `python manage.py check` sem erros.
- grep em theme.css: sem azul de acento remanescente (`#5b8def`, `#2f68d8`, `rgba(59,130,246`).
</verification>

<success_criteria>
- UI-01: home minimalista (mais whitespace, .grad-text verde, menos ornamento) com todas as seções essenciais.
- UI-02: catálogo e detalhe com o mesmo tratamento de cor/spacing, sem redesenho funcional.
- Checkpoint humano aprovado.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand-ui-minimalista/01-02-SUMMARY.md`
</output>
