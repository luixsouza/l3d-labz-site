---
phase: 01-rebrand-ui-minimalista
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings/base.py
  - static/css/theme.css
  - apps/core/templates/base.html
  - apps/core/templates/core/partials/icons.html
  - apps/core/templates/core/partials/navbar.html
  - apps/core/templates/core/partials/footer.html
  - apps/core/templates/core/partials/field.html
  - apps/core/templates/core/home.html
  - apps/promotions/templates/promotions/list.html
  - static/js/app.js
  - apps/catalog/management/commands/seed_demo.py
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, THEME-01, THEME-02, THEME-03]

must_haves:
  truths:
    - "Nenhum texto 'Nexora' visível aparece em qualquer página renderizada (navbar, footer, home, promoções)."
    - "O navbar e o footer exibem o wordmark 'L3D Labz' ao lado de um emblema 'L' em SVG inline (não mais o koala)."
    - "A paleta verde/branco/azul do Luigi está aplicada via tokens CSS no tema escuro e no tema claro."
    - "O toggle de tema alterna claro/escuro e a escolha persiste entre páginas/recargas (chave localStorage migrada para 'l3d-theme' em ambos os arquivos)."
    - "O dict SITE em settings expõe name 'L3D Labz', tagline L3D e accent verde."
  artifacts:
    - path: "config/settings/base.py"
      provides: "Dict SITE com a marca L3D Labz"
      contains: "L3D Labz"
    - path: "static/css/theme.css"
      provides: "Tokens de acento verde Luigi em :root e [data-theme=light]"
      contains: "#2FA84F"
    - path: "apps/core/templates/core/partials/icons.html"
      provides: "Símbolo SVG do emblema L (#i-l3d-mark)"
      contains: "i-l3d-mark"
    - path: "apps/core/templates/core/partials/navbar.html"
      provides: "Wordmark L3D Labz + emblema L"
      contains: "L3D"
    - path: "static/js/app.js"
      provides: "Escrita da preferência de tema na chave l3d-theme"
      contains: "l3d-theme"
    - path: "apps/core/templates/base.html"
      provides: "Boot script lendo a chave l3d-theme + default light"
      contains: "l3d-theme"
  key_links:
    - from: "apps/core/templates/base.html"
      to: "static/js/app.js"
      via: "chave localStorage compartilhada"
      pattern: "l3d-theme"
    - from: "apps/core/templates/core/partials/navbar.html"
      to: "apps/core/templates/core/partials/icons.html"
      via: "use href do emblema"
      pattern: "#i-l3d-mark"
---

<objective>
Aplicar a identidade visual L3D Labz (marca, paleta Luigi, emblema, tema) em todo o site, substituindo 100% das ocorrências de "Nexora", trocando os tokens de cor azul→verde nos dois temas, instalando o emblema "L" no navbar/footer e migrando a chave de tema do localStorage sem quebrar a persistência.

Purpose: Entrega sozinha a identidade L3D Labz — é o coração da Fase 1. Sem isso, nada do minimalismo (Plan 02) faz sentido visual.
Output: SITE dict rebrandado, theme.css recolorido (dark + light), emblema SVG novo, navbar/footer/home/promoções sem "Nexora", chave de tema migrada para `l3d-theme`, default do tema = light.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rebrand-ui-minimalista/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Estrutura REAL do theme.css verificada. NÃO explorar o codebase — usar estes fatos. -->

Em static/css/theme.css há DOIS blocos de tokens que precisam dos novos verdes:
- `:root { ... }` (linhas ~6-43) = TEMA ESCURO. Tokens de acento atuais (azul):
  `--accent: #5b8def;  --accent-2: #82a8f7;  --accent-strong: #3f6cd6;  --accent-soft: #14213f;  --accent-glow: rgba(91, 141, 239, 0.26);`
  E os radial-gradients do `body` (linhas ~56-58) usam `rgba(91, 141, 239, ...)`.
- `[data-theme="light"] { ... }` (linhas ~614-631) = TEMA CLARO. Tokens atuais (azul):
  `--bg: #f3f6fc; --bg-soft:#fff; --bg-card:#fff; --bg-elevated:#eef2fa; --border:#e0e6f1; --border-soft:#eaeef7; --text:#161c28; --text-muted:#54607a; --text-dim:#8a93a8; --accent:#2f68d8; --accent-2:#2f68d8; --accent-strong:#2453b6; --accent-soft:#e7eefc; --accent-glow:rgba(47,104,216,0.20);`
  E `[data-theme="light"] body` (linhas ~632-636) tem radial-gradients `rgba(47,104,216,...)`.
- ATENÇÃO: existe um override v3 em `:root { --radius:0px; ... }` (linha ~659) e botões outline (linhas ~673-684). NÃO mexer nesse bloco neste plano (é cosmético de forma, tratado no Plan 02). Só trocar valores de COR.

navbar.html (linha 4-7) — wordmark hand-split, NÃO dirigível por {{ SITE.name }}:
  `<a ... aria-label="Nexora — início"> <svg class="brand-logo" ...><use href="#i-koala"></use></svg> <span class="brand-word">nex<b>ora</b></span> </a>`
footer.html (linha 5-8) repete o mesmo bloco brand com `#i-koala` e `nex<b>ora</b>`.
CSS já existente: `.brand-word { color: var(--text); }` e `.brand-word b { color: var(--accent-2); }` — então o "L3D" dentro de <b> já herda a cor de acento.

base.html (linha 3): `<html lang="pt-br" data-theme="dark">`
base.html (linha 5): `<script>(function(){try{var t=localStorage.getItem('nexora-theme');if(t){document.documentElement.setAttribute('data-theme',t);}}catch(e){}})();</script>`
app.js (linha 26): `try { localStorage.setItem("nexora-theme", next); } catch (e) {}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolorir tokens para a paleta Luigi (dark + light) e migrar chave de tema</name>
  <files>config/settings/base.py, static/css/theme.css, apps/core/templates/base.html, static/js/app.js</files>
  <read_first>
    - static/css/theme.css (inspecionar os blocos :root linhas ~6-43, body linhas ~56-58, [data-theme="light"] linhas ~614-636)
    - apps/core/templates/base.html (linhas 3 e 5)
    - static/js/app.js (linha 26)
    - config/settings/base.py (linhas 218-222)
  </read_first>
  <action>
    Implementa BRAND-03, THEME-01, THEME-02, THEME-03.

    A) static/css/theme.css — bloco `:root` (TEMA ESCURO, linhas ~6-43): trocar os tokens de acento azul pelos verdes Luigi:
      --accent:        #2FA84F;
      --accent-2:      #43C266;
      --accent-strong: #1E8C3E;
      --accent-soft:   #13251A;
      --accent-glow:   rgba(47, 168, 79, 0.26);
    Trocar também --danger para #E23B3B. Adicionar uma nova linha de token logo após --accent-glow:
      --accent-blue:   #2BA6E0;
    No `body` (linhas ~56-58) substituir as duas cores `rgba(91, 141, 239, 0.10)` e `rgba(91, 141, 239, 0.05)` por `rgba(47, 168, 79, 0.10)` e `rgba(47, 168, 79, 0.05)` (glow verde sutil no topo).

    B) static/css/theme.css — bloco `[data-theme="light"]` (TEMA CLARO, linhas ~614-631): aplicar os valores light/clean do CONTEXT (verde legível AA):
      --bg:            #FFFFFF;
      --bg-soft:       #F6F8F5;
      --bg-card:       #FFFFFF;
      --bg-elevated:   #F1F4EF;
      --border:        #E3E8E0;
      --border-soft:   #EEF1EB;
      --text:          #16201A;
      --text-muted:    #5C6B60;
      --text-dim:      #9AA89E;
      --accent:        #2FA84F;
      --accent-2:      #1E8C3E;   /* no light, usar verde-escuro p/ texto/links (contraste AA) */
      --accent-strong: #1E8C3E;
      --accent-soft:   #E7F6EC;
      --accent-glow:   rgba(47, 168, 79, 0.20);
    Manter as linhas --shadow/--shadow-soft existentes do bloco light (ajustar a cor dentro dos rgba para um tom neutro-esverdeado é opcional; pode deixar como está). Adicionar `--accent-blue: #2BA6E0;` neste bloco também.
    No bloco `[data-theme="light"] body` (linhas ~632-636) trocar `rgba(47, 104, 216, 0.08)` e `rgba(47, 104, 216, 0.05)` por `rgba(47, 168, 79, 0.08)` e `rgba(47, 168, 79, 0.05)`.

    C) Atualizar o comentário-cabeçalho do arquivo (linha 2) de "Nexora — tema dark + azul" para "L3D Labz — tema claro/escuro + verde (paleta Luigi)".

    D) config/settings/base.py — dict SITE (linhas 218-222): trocar para:
      SITE = {
          "name": "L3D Labz",
          "tagline": "Impressão 3D com acabamento de laboratório.",
          "accent": "#2FA84F",
      }

    E) MIGRAR CHAVE DE TEMA (atomicamente, nos dois arquivos):
      - apps/core/templates/base.html linha 5: trocar `localStorage.getItem('nexora-theme')` por `localStorage.getItem('l3d-theme')`.
      - static/js/app.js linha 26: trocar `localStorage.setItem("nexora-theme", next)` por `localStorage.setItem("l3d-theme", next)`.

    F) DEFAULT LIGHT: apps/core/templates/base.html linha 3: trocar `data-theme="dark"` por `data-theme="light"` (a estética base agora é light/clean). O boot script (linha 5) continua sobrescrevendo se houver preferência salva.

    G) Atualizar o comentário-cabeçalho de static/js/app.js (linha 1) de "Nexora — interações..." para "L3D Labz — interações e animações (sem framework)".

    NÃO mexer no override v3 (`:root { --radius:0px ... }`, linha ~659) nem nos estilos de botão outline — isso é forma, tratada no Plan 02.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if(-not(Select-String -Path static/css/theme.css -Pattern '--accent:\s*#2FA84F' -Quiet)){$ok=$false}; if(-not(Select-String -Path static/css/theme.css -Pattern '#E7F6EC' -Quiet)){$ok=$false}; if(-not(Select-String -Path static/js/app.js -Pattern 'l3d-theme' -Quiet)){$ok=$false}; if(-not(Select-String -Path apps/core/templates/base.html -Pattern 'l3d-theme' -Quiet)){$ok=$false}; if(-not(Select-String -Path config/settings/base.py -Pattern 'L3D Labz' -Quiet)){$ok=$false}; if(Select-String -Path static/js/app.js,apps/core/templates/base.html -Pattern 'nexora-theme' -Quiet){$ok=$false}; if($ok){'PASS'}else{'FAIL'}"</automated>
  </verify>
  <acceptance_criteria>
    - static/css/theme.css contém `--accent: #2FA84F` no bloco :root e `--accent-soft: #E7F6EC` no bloco [data-theme="light"].
    - static/css/theme.css contém `rgba(47, 168, 79, 0.26)` (accent-glow verde).
    - static/css/theme.css NÃO contém mais `#5b8def` nem `#2f68d8` como valor de token de acento.
    - config/settings/base.py contém `"name": "L3D Labz"` e `"accent": "#2FA84F"`.
    - static/js/app.js contém `l3d-theme` e NÃO contém `nexora-theme`.
    - apps/core/templates/base.html contém `l3d-theme`, NÃO contém `nexora-theme`, e a tag html abre com `data-theme="light"`.
    - `python manage.py check` roda sem erros.
  </acceptance_criteria>
  <done>Os dois temas usam a paleta verde Luigi; SITE expõe a marca L3D Labz; a preferência de tema é lida e escrita na chave `l3d-theme` e persiste entre recargas; default = light.</done>
</task>

<task type="auto">
  <name>Task 2: Emblema "L", wordmark L3D Labz no navbar/footer e remover toda string "Nexora" visível</name>
  <files>apps/core/templates/core/partials/icons.html, apps/core/templates/core/partials/navbar.html, apps/core/templates/core/partials/footer.html, apps/core/templates/core/home.html, apps/promotions/templates/promotions/list.html, apps/core/templates/core/partials/field.html, apps/catalog/management/commands/seed_demo.py</files>
  <read_first>
    - apps/core/templates/core/partials/icons.html (símbolo #i-koala, linhas 14-24; gradientes linhas 6-12)
    - apps/core/templates/core/partials/navbar.html (linhas 4-7)
    - apps/core/templates/core/partials/footer.html (linhas 5-8 e 34)
    - apps/core/templates/core/home.html (linha 139)
    - apps/promotions/templates/promotions/list.html (linha 34)
    - apps/core/templates/core/partials/field.html (linha 1)
    - apps/catalog/management/commands/seed_demo.py (linha 94)
    - static/css/theme.css (.brand-word e .brand-word b — regra de cor do wordmark)
  </read_first>
  <action>
    Implementa BRAND-01 e BRAND-02.

    A) icons.html — adicionar um novo símbolo de emblema "L" estilo boné do Luigi (círculo branco com letra "L" verde), logo após o símbolo `#i-koala` (depois da linha 24). Usar este markup exato:
      <!-- Emblema da marca: círculo + letra L (estilo boné do Luigi) -->
      <symbol id="i-l3d-mark" viewBox="0 0 64 64">
        <circle cx="32" cy="32" r="30" fill="#2FA84F"/>
        <circle cx="32" cy="32" r="22" fill="#ffffff"/>
        <path d="M26 19h6v20h12v6H26z" fill="#1E8C3E"/>
      </symbol>
    Pode MANTER o símbolo `#i-koala` no arquivo (não é mais referenciado), ou removê-lo; preferir mantê-lo para não quebrar referências esquecidas. (Os gradientes kFace/kBody etc. podem ficar.)

    B) navbar.html — linha 4: trocar `aria-label="Nexora — início"` por `aria-label="L3D Labz — início"`.
       linha 5: trocar `<use href="#i-koala"></use>` por `<use href="#i-l3d-mark"></use>`.
       linha 6: trocar `<span class="brand-word">nex<b>ora</b></span>` por `<span class="brand-word"><b>L3D</b> Labz</span>` (o "L3D" em <b> herda --accent-2 pela regra `.brand-word b` já existente).

    C) footer.html — linha 6: trocar `<use href="#i-koala"></use>` por `<use href="#i-l3d-mark"></use>`.
       linha 7: trocar `<span class="brand-word">nex<b>ora</b></span>` por `<span class="brand-word"><b>L3D</b> Labz</span>`.
       linha 34: trocar `© 2026 Nexora — Impressão 3D.` por `© 2026 L3D Labz — Impressão 3D.` (manter o resto da frase).

    D) home.html — linha 139: trocar `<span class="eyebrow">Comunidade Nexora</span>` por `<span class="eyebrow">Comunidade L3D Labz</span>`.

    E) promotions/list.html — linha 34: trocar `Ofertas Nexora` por `Ofertas L3D Labz`.

    F) field.html — linha 1: trocar o comentário `estilo Nexora` por `estilo L3D Labz`.

    G) seed_demo.py — linha 94: trocar o help `"Popula o banco com dados de demonstração da Nexora."` por `"Popula o banco com dados de demonstração da L3D Labz."`.

    NÃO tocar (fora do escopo de "visível", são internos e não bloqueiam): NexoraLoginView/NexoraLogoutView, KEY_PREFIX/LocMem location, payments.py PIX merchant, cart/models.py docstring, seeds de imagem picsum `nexorahero`/`aboutnexora`. Esses são internos; o critério da fase é "nenhum 'Nexora' VISÍVEL ao cliente". (Se o executor tiver folga de contexto, pode alinhar os comentários/docstrings, mas não é obrigatório.)
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if(-not(Select-String -Path apps/core/templates/core/partials/icons.html -Pattern 'i-l3d-mark' -Quiet)){$ok=$false}; if(-not(Select-String -Path apps/core/templates/core/partials/navbar.html -Pattern 'L3D' -Quiet)){$ok=$false}; if(Select-String -Path apps/core/templates/core/partials/navbar.html,apps/core/templates/core/partials/footer.html,apps/core/templates/core/home.html,apps/promotions/templates/promotions/list.html -Pattern 'Nexora','i-koala' -Quiet){$ok=$false}; if($ok){'PASS'}else{'FAIL'}"</automated>
  </verify>
  <acceptance_criteria>
    - apps/core/templates/core/partials/icons.html contém um `<symbol id="i-l3d-mark"` com `fill="#2FA84F"`.
    - apps/core/templates/core/partials/navbar.html contém `#i-l3d-mark`, contém `<b>L3D</b> Labz`, contém `aria-label="L3D Labz — início"` e NÃO contém `i-koala` nem `Nexora`.
    - apps/core/templates/core/partials/footer.html contém `#i-l3d-mark`, contém `L3D Labz` no copyright e NÃO contém `Nexora` nem `i-koala`.
    - apps/core/templates/core/home.html contém `Comunidade L3D Labz` e NÃO contém `Comunidade Nexora`.
    - apps/promotions/templates/promotions/list.html contém `Ofertas L3D Labz` e NÃO contém `Ofertas Nexora`.
    - grep por `Nexora` em navbar.html, footer.html, home.html e promotions/list.html retorna vazio.
    - `python manage.py check` roda sem erros.
  </acceptance_criteria>
  <done>O navbar e o footer mostram o emblema "L" + wordmark "L3D Labz"; nenhuma string "Nexora" visível resta em navbar, footer, home (newsletter) ou página de promoções; o emblema referencia #i-l3d-mark.</done>
</task>

</tasks>

<verification>
- Subir o dev server (`python manage.py runserver`) e abrir a home: navbar mostra emblema verde "L" + "L3D Labz"; footer idem; nenhum "Nexora" visível.
- Alternar o toggle de tema: claro↔escuro funcionam, ambos com identidade verde; recarregar a página mantém a escolha (DevTools → Application → Local Storage → chave `l3d-theme`).
- `python manage.py check` sem erros.
- grep global `Nexora` em arquivos VISÍVEIS (navbar, footer, home, promoções) retorna vazio.
</verification>

<success_criteria>
- BRAND-01: nenhum "Nexora" visível em navbar/footer/home/promoções (verificado por grep).
- BRAND-02: navbar exibe wordmark "L3D Labz" + emblema #i-l3d-mark (não mais koala).
- BRAND-03: chave de tema migrada para `l3d-theme` em base.html E app.js; persistência intacta.
- THEME-01/02: paleta verde aplicada via tokens em :root (dark) e [data-theme="light"] (light), com #2FA84F.
- THEME-03: toggle alterna e persiste.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand-ui-minimalista/01-01-SUMMARY.md`
</output>
