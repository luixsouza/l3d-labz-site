---
phase: 05-catalogo-makerworld
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
must_haves:
  truths:
    - "Existe um pipeline Pillow que gera um card de produto padronizado na identidade L3D Labz (verde Luigi + wordmark)"
    - "O comando seed_makerworld cria 8 categorias + ~24 produtos curados com atributos, cada um com a imagem branded salva em Product.image"
    - "Os produtos aparecem no catálogo com a foto padronizada e filtram por categoria e por material"
  artifacts:
    - path: "apps/catalog/branding.py"
      provides: "gerar_card(nome, accent) -> bytes (PNG branded)"
      contains: "def gerar_card"
    - path: "apps/catalog/management/commands/seed_makerworld.py"
      provides: "comando idempotente que popula categorias+produtos+imagens branded"
      contains: "seed_makerworld"
---

<objective>
Encher a loja com catálogo relevante (estilo MakerWorld) com fotos padronizadas na identidade L3D Labz e filtros por categoria + material.
</objective>

<tasks>
<task>Task 1: pipeline de branding (apps/catalog/branding.py, Pillow) — gradiente verde Luigi + tile com monograma na cor da categoria + nome + wordmark; gerar_card(nome, accent, subtitulo) -> bytes PNG. Teste de bancada (gera PNG válido).</task>
<task>Task 2: filtro de material — ProductQuery.search aceita material; CatalogService.browse lê params['material']; product_list.html ganha <select name="material">; contexto materials/active_material.</task>
<task>Task 3: comando seed_makerworld — 8 categorias + ~24 produtos curados (atributos completos); para cada produto gera o card branded e salva em Product.image via ContentFile; idempotente. Rodar e validar no catálogo.</task>
</tasks>

<verification>
- python manage.py check limpo
- python manage.py seed_makerworld roda e cria categorias+produtos com imagem
- catálogo filtra por ?categoria= e ?material=
</verification>

<output>
Create 05-01-SUMMARY.md
</output>
