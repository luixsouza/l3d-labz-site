# --- Core ---
Django==5.2.12
djangorestframework==3.16.1
djangorestframework-camel-case==1.4.2

# --- Banco ---
psycopg[binary,pool]==3.3.3     # Postgres (produção) + connection pool
# SQLite é nativo no Python (dev)

# --- Cache ---
django-redis==6.0.0
redis==6.4.0

# --- Imagens / arquivos ---
Pillow==11.2.1

# --- Geração 3D do lithophane ---
numpy==2.2.6
trimesh==4.12.2
fast-simplification           # decimação de malha (STL gigante -> GLB leve p/ o viewer)
networkx                      # requerido pelo trimesh p/ ler .3mf (grafo de cena)
lxml                          # requerido pelo trimesh p/ parsear o XML do .3mf
matplotlib                    # render headless (Agg) da foto padronizada do modelo

# --- Geração de PDF (orçamentos) ---
reportlab==4.5.1          # geração de PDF pura-python (orçamentos da calculadora, sem deps nativas)

# --- Descrições por IA (Claude Vision) ---
anthropic==0.109.1        # SDK oficial Anthropic; usado por gerar_descricoes (roda no server)

# --- Filtros de catálogo ---
django-filter==25.1

# --- Dev / qualidade ---
django-debug-toolbar==6.3.0

# --- Servidor / estáticos (prod) ---
gunicorn==23.0.0
whitenoise==6.9.0
